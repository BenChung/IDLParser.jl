var documenterSearchIndex = {"docs":
[{"category":"section","location":"getting-started/#Getting-Started","page":"Getting Started","text":"ROSNode brings ROS 2 publish/subscribe, services, and actions to Julia over Zenoh, mirroring rmw_zenoh on the wire. This page sets up the package, starts a router, walks the Context / Node / spin lifecycle, and runs the bundled examples.","title":"Getting Started"},{"category":"section","location":"getting-started/#Adding-ROSNode","page":"Getting Started","text":"ROSNode ships as part of this workspace. Activate the workspace project and load the package:\n\nusing ROSNode\n\nRun any example with the workspace project active so ROSNode and its dependencies resolve:\n\njulia --project=. ROSNode/examples/publisher.jl","title":"Adding ROSNode"},{"category":"section","location":"getting-started/#Starting-a-router","page":"Getting Started","text":"ROSNode is built on Zenoh and mirrors rmw_zenoh, whose discovery is router-based. Start a router once, then point examples at it:\n\nzenohd -l tcp/localhost:7447 &\n\nThe peers = [\"tcp/localhost:7447\"] argument to @context selects that router. Omit peers (and set localhost_only / domain_id as needed) to use environment discovery. localhost_only = true confines discovery to the configured router; omit it to reach peers on other hosts.","title":"Starting a router"},{"category":"section","location":"getting-started/#The-Context-/-Node-/-spin-model","page":"Getting Started","text":"Every program follows one lifecycle skeleton: open a Context, group entities under a Node, then spin. Here is a publisher reduced to that shape.\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"talker\")\n    pub  = Publisher(node, \"/chatter\", std_msgs.msg.String)\n\n    publish(pub, std_msgs.msg.String(data = \"hello world\"))\n    spin(ctx; handle_signals = true)\nend\n\nContext owns the Zenoh session, discovery, and shutdown; the @context do ctx … end form drains in-flight work, closes the session on exit, and binds this module as the type-resolution home.\nNode groups the entities you create under it: publishers, subscriptions, servers, and clients.\nspin parks the main task and runs until Ctrl-C requests shutdown. handle_signals = true turns Ctrl-C into a graceful drain that flips isopen(ctx) for background loops to observe.","title":"The Context / Node / spin model"},{"category":"section","location":"getting-started/#Running-the-examples","page":"Getting Started","text":"Run a router, then start the two halves in separate Julia processes pointed at the same router:\n\nzenohd -l tcp/localhost:7447 &\n\njulia --project=. ROSNode/examples/publisher.jl     # in one terminal\njulia --project=. ROSNode/examples/subscriber.jl    # in another\n\nExamples that bundle both halves (service.jl, action.jl) take a role argument to split across two processes — the ROLE env var or the first CLI arg, defaulting to both:\n\njulia --project=. ROSNode/examples/service.jl server    # serve only (Ctrl-C to stop)\njulia --project=. ROSNode/examples/service.jl client    # call only (needs a server)\n\nservice.jl, action.jl, and parameters.jl each run both halves in one self-contained process; point them at the router and run.","title":"Running the examples"},{"category":"section","location":"getting-started/#Interoperating-with-ROS-2","page":"Getting Started","text":"ROSNode mirrors rmw_zenoh: with rmw_zenoh selected and a matching router plus ROS_DOMAIN_ID, ROSNode entities talk to real ROS 2 C++/Python nodes over the wire:\n\nros2 topic echo /chatter std_msgs/msg/String\nros2 service call /add_two_ints example_interfaces/srv/AddTwoInts \"{a: 2, b: 40}\"\nros2 action send_goal /fibonacci example_interfaces/action/Fibonacci \"{order: 8}\"\n\n@ros_import statically generates the interface type by name from ROSNode's vendored tree or, inside a sourced ROS 2 environment, from AMENT_PREFIX_PATH. Authored types (@ros_message / @ros_service / @ros_action) carry the same RIHS01 identity as the equivalent .msg / .srv / .action, which makes both wire-compatible with ROS 2.","title":"Interoperating with ROS 2"},{"category":"section","location":"getting-started/#Next","page":"Getting Started","text":"Interface Types\nPublisher and Subscriber\nService and Client\nActions","title":"Next"},{"category":"section","location":"interfaces/#Interface-Types","page":"Interface Types","text":"Every topic, service, and action carries a typed message. ROSNode brings those types in two ways: import an existing interface by name, or author one directly in Julia. Both forms produce fully registered ROS types — real RIHS01, keyexpr resolution, wire compatibility — and interoperate with each other and with C++/Python ROS 2 nodes.","title":"Interface Types"},{"category":"section","location":"interfaces/#Importing-existing-interfaces","page":"Interface Types","text":"@ros_import bakes an existing interface by its fully-qualified name. The from=\"…\" argument adds a local source root, so your own .msg/.srv/.action definitions resolve alongside the vendored tree and a sourced ROS 2 environment.\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n@ros_import \"example_interfaces/srv/AddTwoInts\" from=\"interfaces\"\n@ros_import \"action_tutorials_interfaces/action/Fibonacci\" from=\"interfaces\"\n\nThe binding follows the interface kind:\n\nA message binds the bare leaf type, e.g. std_msgs.msg.String. Construct it by that name: std_msgs.msg.String(data = \"hello\").\nA service binds the bare namespace, exposing .Request and .Response. Reference it by .Request; ROSNode resolves the Response automatically.\nAn action binds the bare namespace, which serves as the action handle and exposes .Goal, .Result, and .Feedback.\n\n# message: bare leaf type\npub = Publisher(node, \"/chatter\", std_msgs.msg.String)\n\n# service: reference by .Request\nsrv = Service(node, \"/add_two_ints\", AddTwoInts.Request) do req\n    AddTwoInts.Response(sum = req.a + req.b)\nend\n\n# action: bare namespace is the handle; sections by short name\nserver = ActionServer(node, \"/fibonacci\", Fibonacci) do goal\n    feedback!(goal, Fibonacci.Feedback(partial_sequence = seq))\n    Fibonacci.Result(sequence = seq)\nend","title":"Importing existing interfaces"},{"category":"section","location":"interfaces/#Authoring-interfaces-in-Julia","page":"Interface Types","text":"@ros_package names the ROS package the types belong to, giving each one its wire identity. @ros_message, @ros_service, and @ros_action then define the interface entirely in Julia source. Authored types compose — a struct referencing another authored type by its Julia type resolves and hashes the nested ref exactly as a .msg nested field would.\n\nA message is a struct. @ros_message turns each one into <pkg>/msg/<Name>:\n\nmodule Msgs\n    using ROSNode\n    @ros_package \"demo_msgs\"\n    @ros_message struct Header;  stamp::Float64; frame::String;          end\n    @ros_message struct Chatter; header::Header; data::String; seq::Int32; end   # nested authored ref\nend\n\nA service is a function: the parameters are the request fields, the @NamedTuple return type is the response. The body is the handler; throw to fail the call:\n\nmodule Srv\n    using ROSNode\n    @ros_package \"example_interfaces\"\n    @ros_service function AddTwoInts(a::Int64, b::Int64)::@NamedTuple{sum::Int64}\n        (sum = a + b,)\n    end\nend\n\nAn action is a function with a FeedbackSink parameter: the goal fields are parameters, the FeedbackSink{@NamedTuple{…}} parameter is the feedback channel, the @NamedTuple return type is the result. Calling the sink publishes Feedback and checkpoints cancellation; returning the result settles SUCCEEDED:\n\nmodule Act\n    using ROSNode\n    @ros_package \"action_tutorials_interfaces\"\n    @ros_action function Fibonacci(order::Int32,\n            fb::FeedbackSink{@NamedTuple{partial_sequence::Vector{Int32}}})::@NamedTuple{sequence::Vector{Int32}}\n        seq = Int32[0, 1]\n        for _ in 1:order\n            push!(seq, seq[end] + seq[end-1])\n            fb((partial_sequence = copy(seq),))   # publish feedback + cancellation yield point\n        end\n        (sequence = seq,)\n    end\nend\n\nPass the authored function itself as the marker to the server and client constructors, e.g. Service(node, \"/add_two_ints\", Srv.AddTwoInts) and ActionServer(node, \"/fibonacci\", Act.Fibonacci).","title":"Authoring interfaces in Julia"},{"category":"section","location":"interfaces/#Mapping-onto-an-existing-identity","page":"Interface Types","text":"The annotate form @ros_message \"pkg/msg/Name\" T keeps your Julia name T and gives it the wire type — and matching RIHS01 — of pkg/msg/Name. A one-field struct matches std_msgs/msg/String (string data), deriving the identical hash, so it interoperates with the stock /chatter endpoint. The annotate form registers the struct as written, so mark it @kwdef yourself for keyword construction:\n\nmodule Msgs\n    using ROSNode\n    Base.@kwdef struct Chatter; data::String; end\n    @ros_message \"std_msgs/msg/String\" Chatter      # Julia `Chatter` ⇔ ROS std_msgs/msg/String\nend","title":"Mapping onto an existing identity"},{"category":"section","location":"interfaces/#Discovering-types-at-runtime","page":"Interface Types","text":"A type-less Subscription resolves its message type at runtime from discovery, so tooling subscribes to a topic whose type it learns over the wire. The msg is the real decoded value, with type-stable field access:\n\nsub = Subscription(node, \"/chatter\") do msg\n    @info \"heard\" typeof(msg.data) msg.data\nend\n\n@ros_cache makes that discovery stick across runs: it persists each discovered type description in a project-local, RIHS01-keyed store under <project>/ros_typesupport, records a per-node warm-up manifest, and bakes wire-discovered types into static ones at precompile.\n\n@ros_cache\n\nThe dynamic subscription records every type it handles into the manifest. The next run replays that manifest at startup, warming each recorded type's codec before the first message arrives:\n\nsub = Subscription(node, \"/readings\"; warmup = :precompile) do msg\n    @info \"recorded\" sensor = msg.sensor_id value = msg.value\nend\n\nThe framework logs a one-time hint naming the discovered type and the static spelling to graduate to — Subscription(node, \"/readings\", Reading) — which drops the per-message invokelatest hop and unlocks the zero-copy view = true fast path. Pass warmup_sync = true to block construction until the codec is warm.","title":"Discovering types at runtime"},{"category":"section","location":"interfaces/#Wire-identity-(RIHS01)","page":"Interface Types","text":"A ROS interface's wire type is identified by its name plus its RIHS01 hash, derived from the field layout. Two definitions sharing both the interface name and the RIHS01 hash are the same wire type. This identity is what lets authored and imported Julia types interoperate: an authored @ros_message/@ros_service/@ros_action carries the same RIHS01 as the equivalent .msg/.srv/.action, so it is wire-compatible with its imported twin and with C++/Python ROS 2 nodes.\n\nWith rmw_zenoh selected and a matching router plus ROS_DOMAIN_ID, ROSNode entities exchange messages with real ROS 2 nodes:\n\nros2 topic echo /chatter std_msgs/msg/String\nros2 service call /add_two_ints example_interfaces/srv/AddTwoInts \"{a: 2, b: 40}\"\nros2 action send_goal /fibonacci action_tutorials_interfaces/action/Fibonacci \"{order: 8}\"\n\nSee Getting started for the router setup, and the publisher and subscriber, service and client, and action server and client tutorials for the end-to-end walkthroughs.","title":"Wire identity (RIHS01)"},{"category":"section","location":"tutorials/pubsub/#Writing-a-Publisher-and-Subscriber","page":"Publisher and Subscriber","text":"A topic is a named channel. Publishers send messages on it, and subscribers receive every message that arrives.\n\nThis page builds a talker that publishes a std_msgs/String on /chatter and a listener that prints each one, then authors the message type in Julia and discovers a type at runtime.","title":"Writing a Publisher and Subscriber"},{"category":"section","location":"tutorials/pubsub/#Imported-interfaces","page":"Publisher and Subscriber","text":"@ros_import bakes a standard interface by name and lands it at std_msgs.msg.String in the calling module. The publisher opens a context, creates a node, and publishes a fresh String each second:\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"talker\")\n    pub  = Publisher(node, \"/chatter\", std_msgs.msg.String)\n\n    for i in 0:typemax(Int)\n        isopen(ctx) || break\n        publish(pub, std_msgs.msg.String(data = \"hello world $i\"))\n        sleep(1.0)\n    end\nend\n\nThe subscriber runs as a separate process. The do-block handler runs once per message, receiving an owned, decoded std_msgs.msg.String. The type-less Subscription(node, \"/chatter\") resolves the concrete type at runtime against the calling module's @ros_import closure, so a single import serves both halves:\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"listener\")\n\n    sub = Subscription(node, \"/chatter\") do msg\n        @info \"heard\" msg.data\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThe default concurrency = Serial() delivers messages in order on one task; pass Parallel(n) to fan out across threads. spin parks the main task to keep the process alive while the scheduler delivers each message on its own task, and handle_signals = true turns Ctrl-C into a graceful drain.","title":"Imported interfaces"},{"category":"section","location":"tutorials/pubsub/#Authoring-the-message-in-Julia","page":"Publisher and Subscriber","text":"A hand-written Julia struct marked @ros_message becomes a fully registered ROS type, carrying a real RIHS01 identity and keyexpr resolution, with no .msg file and no codegen step. @ros_package names the package, and authored types compose: Chatter references Header by its Julia type, and ROSNode hashes that nested ref into Chatter's identity exactly as a .msg nested field would be:\n\nusing ROSNode\n\nmodule Msgs\n    using ROSNode\n    @ros_package \"demo_msgs\"\n    @ros_message struct Header;  stamp::Float64; frame::String;        end\n    @ros_message struct Chatter; header::Header; data::String; seq::Int32; end\nend\n\nThe talker publishes the authored type by its Julia name, passing the type explicitly to resolve it straight from the registry:\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"authored_talker\")\n    pub  = Publisher(node, \"/chatter\", Msgs.Chatter)\n    sleep(0.4)   # let discovery match sub ↔ pub before the first publish\n\n    for i in 0:4\n        publish(pub, Msgs.Chatter(\n            header = Msgs.Header(stamp = 1.0 * i, frame = \"demo\"),\n            data   = \"hello world $i\",\n            seq    = Int32(i)))\n        sleep(0.5)\n    end\nend\n\nThe listener subscribes to the same authored type and reads its nested fields:\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"authored_listener\")\n\n    sub = Subscription(node, \"/chatter\", Msgs.Chatter) do msg\n        @info \"heard\" msg.data seq = msg.seq frame = msg.header.frame\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThe authored RIHS01 matches the equivalent .msg, so the type is wire-compatible with imported twins and with C++/Python ROS 2 nodes.","title":"Authoring the message in Julia"},{"category":"section","location":"tutorials/pubsub/#Discovering-the-type-at-runtime","page":"Publisher and Subscriber","text":"A type-less subscription learns its message type off the wire, so a generic recorder handles a stream whose headers it has never seen. @ros_cache adds project-local persistence and static baking, so each run warms the codec faster than the last:\n\nusing ROSNode\n\n@ros_cache\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"recorder\")\n\n    # type-less: resolves each sample's type at runtime; `warmup = :precompile`\n    # warms the decode→handler chain so a repeat run pre-warms it at startup\n    sub = Subscription(node, \"/readings\"; warmup = :precompile) do msg\n        @info \"recorded\" sensor = msg.sensor_id value = msg.value\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nRun 1 discovers the type and warms its codec on first sight, recording every type it handles into a per-node manifest under ros_typesupport/manifests/. Run 2 replays that manifest at startup and warms the codec, so the first sample arrives hot.","title":"Discovering the type at runtime"},{"category":"section","location":"tutorials/pubsub/#Running-it","page":"Publisher and Subscriber","text":"ROSNode is built on Zenoh and mirrors rmw_zenoh, whose discovery is router-based. Start a router once, then point examples at it:\n\nzenohd -l tcp/localhost:7447 &\n\nThe peers = [\"tcp/localhost:7447\"] argument to @context selects that router. Omit it (and set localhost_only / domain_id as needed) for environment discovery. localhost_only = true confines discovery to the configured router; omit it to reach peers on other hosts.\n\nRun a router, then start the two halves in separate Julia processes pointed at the same router:\n\nzenohd -l tcp/localhost:7447 &\n\njulia --project=. ROSNode/examples/publisher.jl     # in one terminal\njulia --project=. ROSNode/examples/subscriber.jl    # in another\n\nExamples that bundle both halves (service.jl, action.jl) take a role argument to split across two processes — the ROLE env var, then the first CLI arg, then both by default:\n\njulia --project=. ROSNode/examples/service.jl server    # serve only (Ctrl-C to stop)\njulia --project=. ROSNode/examples/service.jl client    # call only (needs a server)\n\nservice.jl, action.jl, and parameters.jl each also run both halves in one process, so they're self-contained — just point them at the router.","title":"Running it"},{"category":"section","location":"tutorials/pubsub/#Next-steps","page":"Publisher and Subscriber","text":"Define and customize your own messages in Interface Types. Set up a project and router in Getting Started.","title":"Next steps"},{"category":"section","location":"tutorials/service/#Writing-a-Service-and-Client","page":"Service and Client","text":"A service is a request/response call: the client blocks for the reply. The handler settles each request once. A normal return replies success; a failure surfaces to the client as a ServiceError.","title":"Writing a Service and Client"},{"category":"section","location":"tutorials/service/#Imported-interface","page":"Service and Client","text":"@ros_import of a fully-qualified service binds the bare AddTwoInts namespace, exposing AddTwoInts.Request and AddTwoInts.Response. Reference the service by its .Request type; the Response resolves automatically. The handler receives the decoded request and returns the response message.\n\nusing ROSNode\n\n@ros_import \"example_interfaces/srv/AddTwoInts\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"add_two_ints\")\n\n    srv = Service(node, \"/add_two_ints\", AddTwoInts.Request) do req\n        AddTwoInts.Response(sum = req.a + req.b)\n    end\n\n    spin(ctx; handle_signals = true)   # serve until Ctrl-C\nend\n\nThe client constructs a ServiceClient over the same request type, waits for a server to route-match, then calls. call blocks for the reply.\n\nusing ROSNode\n\n@ros_import \"example_interfaces/srv/AddTwoInts\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"add_two_ints\")\n    client = ServiceClient(node, \"/add_two_ints\", AddTwoInts.Request)\n\n    if wait_for_service(client; timeout = 5)\n        resp = call(client, AddTwoInts.Request(a = 2, b = 40); timeout_ms = 5000)\n        @info \"response\" sum = resp.sum   # → 42\n    end\n    close(client)\nend\n\nwait_for_service returns true once a server route-matches within the timeout, gating the call on a reachable server. For concurrent requests, call(client, req; async = true) returns a Task you fetch later.","title":"Imported interface"},{"category":"section","location":"tutorials/service/#Authoring-the-service-in-Julia","page":"Service and Client","text":"Write the service as a Julia function: the parameters are the request fields, and the @NamedTuple return type is the response. @ros_package gives it the wire identity example_interfaces/srv/AddTwoInts — the same RIHS01 as the imported .srv — so an authored server interoperates with an imported client, a C++/Python add_two_ints, or ros2 service call. The function body is the handler; throw to fail the call.\n\nusing ROSNode\n\nmodule Srv\n    using ROSNode\n    @ros_package \"example_interfaces\"\n    @ros_service function AddTwoInts(a::Int64, b::Int64)::@NamedTuple{sum::Int64}\n        (sum = a + b,)\n    end\nend\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"add_two_ints\")\n\n    srv = Service(node, \"/add_two_ints\", Srv.AddTwoInts)   # the function is the marker\n\n    spin(ctx; handle_signals = true)\nend\n\nThe client passes the authored function as the marker and supplies request fields as keyword arguments; call returns a NamedTuple.\n\nclient = ServiceClient(node, \"/add_two_ints\", Srv.AddTwoInts)\n\nif wait_for_service(client; timeout = 5)\n    resp = call(client; a = Int64(2), b = Int64(40))   # request fields as kwargs\n    @info \"response\" sum = resp.sum                     # NamedTuple → 42\nend\nclose(client)","title":"Authoring the service in Julia"},{"category":"section","location":"tutorials/service/#Discovering-the-service-at-runtime","page":"Service and Client","text":"The request and response sections resolve off the wire like message types. The home module supplies the resolution context that lets a client reach a service whose type it learned from discovery. See Interface Types for how that resolution works.","title":"Discovering the service at runtime"},{"category":"section","location":"tutorials/service/#Running-it-and-handling-errors","page":"Service and Client","text":"ROSNode is built on Zenoh and mirrors rmw_zenoh, whose discovery is router-based. Start a router once, then point examples at it:\n\nzenohd -l tcp/localhost:7447 &\n\nThe peers = [\"tcp/localhost:7447\"] argument to @context selects that router. localhost_only = true confines discovery to the configured router; the default reaches peers on other hosts. Set domain_id to match the ROS domain.\n\nRun the server and client in separate Julia processes pointed at the same router:\n\njulia --project=. ROSNode/examples/service.jl server    # serve only (Ctrl-C to stop)\njulia --project=. ROSNode/examples/service.jl client    # call only (needs a server)\n\nThe server settles every request: a normal return replies success, and respond!(req, failed, msg) sends an explicit failure. A failed reply raises a ServiceError at the client's call, surfacing the error where the request was made.\n\nSee Interface Types for authoring and discovering service types.","title":"Running it and handling errors"},{"category":"section","location":"tutorials/action/#Creating-an-Action-Server-and-Client","page":"Actions","text":"An action runs a long goal that streams feedback while it works and ends in a result. The client tracks progress as feedback arrives and can cancel the goal mid-flight. This page tracks the ROS 2 \"Writing an action server and client\" tutorial using the Fibonacci action.","title":"Creating an Action Server and Client"},{"category":"section","location":"tutorials/action/#Imported-interface","page":"Actions","text":"@ros_import of a fully-qualified action binds the bare Fibonacci namespace: a tag type that serves as the action handle and exposes the sections by short name (Fibonacci.Goal, Fibonacci.Feedback, Fibonacci.Result).\n\nThe server's handler runs once per accepted goal. goal.request is the decoded Goal, feedback! publishes a Feedback message and checkpoints cancellation, and returning a Result settles the goal SUCCEEDED.\n\nusing ROSNode\n\n@ros_import \"action_tutorials_interfaces/action/Fibonacci\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"fibonacci\")\n\n    server = ActionServer(node, \"/fibonacci\", Fibonacci) do goal\n        seq = Int32[0, 1]\n        for i in 1:goal.request.order\n            push!(seq, seq[end] + seq[end-1])\n            feedback!(goal, Fibonacci.Feedback(partial_sequence = copy(seq)))\n            sleep(0.3)  # also a cancellation checkpoint\n        end\n        Fibonacci.Result(sequence = seq)\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThe client sends a goal, streams feedback until the goal settles, then reads the cached result. send blocks until the server accepts or rejects and returns a goal handle.\n\nusing ROSNode\n\n@ros_import \"action_tutorials_interfaces/action/Fibonacci\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"fibonacci\")\n    client = ActionClient(node, \"/fibonacci\", Fibonacci)\n\n    if wait_for_action_server(client; timeout = 5)\n        gh = send(client, Fibonacci.Goal(order = Int32(8)))\n        @info \"goal\" state = state(gh)   # :accepted (or :rejected)\n\n        for fb in feedback(gh)           # loop ends when the goal settles\n            @info \"feedback\" fb.partial_sequence\n        end\n\n        result = fetch(gh)               # cached result after settle\n        @info \"result\" sequence = result.sequence\n        @info \"final state\" state = state(gh)   # :succeeded\n    end\n    close(client)\nend","title":"Imported interface"},{"category":"section","location":"tutorials/action/#Authoring-the-action-in-Julia","page":"Actions","text":"@ros_action defines an action as a Julia function. Goal fields become parameters, a FeedbackSink{@NamedTuple{…}} parameter is the feedback channel, and the @NamedTuple return type is the result. @ros_package \"action_tutorials_interfaces\" gives the function the wire identity action_tutorials_interfaces/action/Fibonacci.\n\nThis schema names its feedback field partial_sequence. Calling the feedback sink fb((partial_sequence = …,)) publishes a Feedback message and checkpoints cancellation; returning the result NamedTuple settles SUCCEEDED.\n\nmodule Act\n    using ROSNode\n    @ros_package \"action_tutorials_interfaces\"\n    @ros_action function Fibonacci(order::Int32,\n            fb::FeedbackSink{@NamedTuple{partial_sequence::Vector{Int32}}})::@NamedTuple{sequence::Vector{Int32}}\n        seq = Int32[0, 1]\n        for _ in 1:order\n            push!(seq, seq[end] + seq[end-1])\n            fb((partial_sequence = copy(seq),))   # publish feedback + cancellation checkpoint\n            sleep(0.3)\n        end\n        (sequence = seq,)\n    end\nend\n\nPass the authored function as the marker. ROSNode runs its body per goal and drives the accept/feedback/result protocol around it.\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"fibonacci\")\n    server = ActionServer(node, \"/fibonacci\", Act.Fibonacci)\n    spin(ctx; handle_signals = true)\nend\n\nThe client passes goal fields as keyword arguments and receives feedback as NamedTuples carrying partial_sequence.\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"fibonacci\")\n    client = ActionClient(node, \"/fibonacci\", Act.Fibonacci)\n\n    if wait_for_action_server(client; timeout = 5)\n        gh = send(client; order = Int32(8))   # goal fields as kwargs\n        @info \"goal\" state = state(gh)\n\n        for fb in feedback(gh)\n            @info \"feedback\" fb.partial_sequence\n        end\n\n        result = fetch(gh)                    # NamedTuple result\n        @info \"result\" result.sequence\n    end\n    close(client)\nend\n\nBoth examples target action_tutorials_interfaces/action/Fibonacci with identical fields: a partial_sequence feedback array and a sequence result. A type's RIHS01 identity follows its name and field structure, so the imported and authored forms share one wire identity and interoperate with C++/Rust/Python nodes.","title":"Authoring the action in Julia"},{"category":"section","location":"tutorials/action/#Discovering-the-action-at-runtime","page":"Actions","text":"Inside a @context do-block, the calling module is the type-resolution home, so ROSNode resolves an action's sections from discovery when given the bare name. See Interface Types for how runtime resolution and authored types share one wire identity.","title":"Discovering the action at runtime"},{"category":"section","location":"tutorials/action/#Running-it-and-cancelling-a-goal","page":"Actions","text":"ROSNode is built on Zenoh and mirrors rmw_zenoh, whose discovery is router-based. Start a router once, then point examples at it:\n\nzenohd -l tcp/localhost:7447 &\n\nThe peers = [\"tcp/localhost:7447\"] argument to @context selects that router. For environment discovery, omit peers and set localhost_only / domain_id as needed. localhost_only = true confines discovery to the configured router; omit it to reach peers on other hosts.\n\naction.jl runs both halves in one process, so it is self-contained — just point it at the router. The ROLE env var or first CLI arg (defaulting to both) splits the halves across two processes:\n\nzenohd -l tcp/localhost:7447 &\n\njulia --project=. ROSNode/examples/action.jl server    # serve only (Ctrl-C to stop)\njulia --project=. ROSNode/examples/action.jl client    # send a goal (needs a server)\n\nWith rmw_zenoh selected and a matching router plus ROS_DOMAIN_ID, the server answers a real ROS 2 client over the wire:\n\nros2 action send_goal /fibonacci action_tutorials_interfaces/action/Fibonacci \"{order: 8}\"\n\nTo cancel a running goal, call cancel(gh) on the client. The server's next feedback! (imported) or fb(…) (authored) checkpoint throws Cancelled, and the goal settles :canceled.\n\nSee Interface Types for how @ros_import, authored types, and runtime discovery resolve to one wire identity.","title":"Running it and cancelling a goal"},{"category":"section","location":"#ROSNode.jl","page":"Home","text":"ROSNode is a pure-Julia ROS 2 client library built on Zenoh. It mirrors rmw_zenoh on the wire, so its nodes interoperate with C++ and Python ROS 2 nodes sharing the same router and ROS_DOMAIN_ID.\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"talker\")\n    pub  = Publisher(node, \"/chatter\", std_msgs.msg.String)\n    publish(pub, std_msgs.msg.String(data = \"hello world\"))\nend\n\nROSNode also resolves message types at runtime. A type-less subscription discovers the concrete wire type from the publisher and decodes it on the fly, handing the handler a real typed struct — with no @ros_import:\n\nusing ROSNode\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"listener\")\n\n    # No @ros_import: the message type is resolved at runtime from discovery.\n    Subscription(node, \"/chatter\") do msg\n        @info \"heard\" msg.data\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThis powers generic tooling — recorders, bridges, introspection — over types it never imported. @ros_cache persists each discovered type to warm it at startup on the next run. See Interface Types.","title":"ROSNode.jl"},{"category":"section","location":"#What-it-provides","page":"Home","text":"Nodes group communication entities under a name.\nTopics carry messages from publishers to subscribers.\nServices answer a request with a response.\nActions run long-running goals that stream feedback and return a result.\nParameters expose named, typed values that peers read and set.","title":"What it provides"},{"category":"section","location":"#Defining-interface-types","page":"Home","text":"Two paths produce the same wire types:\n\nImport an existing ROS interface by name with @ros_import.\nAuthor one in Julia with @ros_message, @ros_service, or @ros_action.\n\nBoth carry the same RIHS01 identity, so an imported type and its authored twin are wire-compatible. See Interface Types.","title":"Defining interface types"},{"category":"section","location":"#Runtime-model","page":"Home","text":"A Context owns the Zenoh session, discovery, and shutdown; a Node groups the entities you create against it; spin(ctx; handle_signals = true) parks the main task and runs until shutdown, turning Ctrl-C into a graceful drain. Julia's scheduler is the executor, so handlers run on tasks. See Getting Started for the full setup.\n\nTyped subscriptions select a view mode — Owned copies the payload into Julia memory, Checked exposes a zero-copy view guarded against escape, Unchecked exposes the view directly for maximum throughput. Callbacks run with Serial() concurrency by default; Parallel(n) fans delivery across n tasks.","title":"Runtime model"},{"category":"section","location":"#Tutorials","page":"Home","text":"Getting Started — start a router, open a context, run an example.\nInterface Types — import or author message, service, and action types.\nPublisher and Subscriber — publish and subscribe on a topic.\nService and Client — serve and call a request/response service.\nAction Server and Client — run an action server and client with feedback.","title":"Tutorials"},{"category":"section","location":"#Mapping-to-the-ROS-2-tutorials","page":"Home","text":"ROSNode page ROS 2 rolling beginner tutorial\ntutorials/pubsub.md Writing a simple publisher and subscriber (C++/Python)\ntutorials/service.md Writing a simple service and client (C++/Python)\ntutorials/action.md Writing an action server and client\ninterfaces.md Creating custom msg and srv files + Implementing custom interfaces\ngetting-started.md Configuring environment / Creating a workspace / Creating a package / Using colcon\nindex.md (overview of the client library)","title":"Mapping to the ROS 2 tutorials"}]
}
