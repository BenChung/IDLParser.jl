var documenterSearchIndex = {"docs":
[{"category":"section","location":"communication/topics/#Topics","page":"Topics","text":"A topic is a named channel. Publishers send messages on it, and subscribers receive every message that arrives.\n\nThis page builds a talker that publishes a std_msgs/String on /chatter and a listener that prints each one, using an imported message type for both halves.","title":"Topics"},{"category":"section","location":"communication/topics/#Imported-interfaces","page":"Topics","text":"@ros_import bakes a standard interface by name and lands it at std_msgs.msg.String in the calling module. The from= root names where the macro reads the .msg source. Resolve std_msgs one of three ways:\n\nrun from the repository's examples/ directory, where interfaces/std_msgs/msg/String.msg ships (from=\"interfaces\");\npoint from= at your own interface tree;\nsource a ROS 2 environment, which resolves std_msgs without from=.\n\nThe talker opens a context, creates a node, and publishes a fresh String each second:\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"talker\")\n    pub  = Publisher(node, \"/chatter\", std_msgs.msg.String)\n\n    for i in 0:typemax(Int)\n        isopen(ctx) || break\n        publish(pub, std_msgs.msg.String(data = \"hello world $i\"))\n        sleep(1.0)\n    end\nend\n\nThe listener runs as a separate process. The do-block handler runs once per message, receiving an owned, decoded std_msgs.msg.String:\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"listener\")\n\n    sub = Subscription(node, \"/chatter\") do msg\n        @info \"heard\" msg.data\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThe type-less Subscription(node, \"/chatter\") resolves each sample's concrete type at runtime against the home module's imports, which @context binds from the calling module. This single @ros_import \"std_msgs/msg/String\" therefore serves both the publisher's explicit type and the subscriber's resolution. See Interface Types for how that resolution works, and Runtime Type Discovery for a recorder that handles any type off the wire.\n\nConcurrency has two modes: the default concurrency = Serial() delivers messages in order on one task, while Parallel(n) fans out across threads. See Message Delivery for view modes and concurrency in depth.\n\nspin parks the main task to keep the process alive while the scheduler delivers each message on its own task, and handle_signals = true turns Ctrl-C into a graceful drain.\n\nPublisher and Subscription are singleton instances of distinct endpoint kinds. Each accepts a qos and the warmup family plus kind-specific options; see PublisherKind and SubscriptionKind for the full argument lists.\n\nTo define your own message type directly in Julia, see Authoring Interfaces in Julia. Both halves are wire-compatible with ROS 2 nodes — see Interoperating with ROS 2 to echo /chatter from a sourced ROS 2 environment, and Getting Started to start the router these examples point at.","title":"Imported interfaces"},{"category":"section","location":"communication/topics/#Next","page":"Topics","text":"Services extend the topic model to request/response calls.","title":"Next"},{"category":"section","location":"communication/topics/#API-reference","page":"Topics","text":"","title":"API reference"},{"category":"type","location":"communication/topics/#ROSNode.PublisherHandle","page":"Topics","text":"PublisherHandle{T}\n\nThe handle returned by Publisher(node, topic, T) — a long-lived publisher for ROS 2 message type T. Send with publish. Wraps an Entity and owns the per-endpoint attachment sequence counter. isopen/close-able and reaped when its node closes.\n\nThe second type parameter pins the concrete Zenoh route (a plain or Advanced publisher, set by the QoS durability) so the put stays monomorphic; callers only spell PublisherHandle{T}.\n\n\n\n\n\n","title":"ROSNode.PublisherHandle"},{"category":"function","location":"communication/topics/#ROSNode.publish","page":"Topics","text":"publish(pub::PublisherHandle{T}, msg::T) -> nothing\n\nSerialize and publish msg on pub's topic. The CDR payload is encoded into the publisher's reused buffer (grown on demand for a variable-size msg) and copied into a held Zenoh payload box; the put then transmits it. At steady state this allocates nothing. Returns nothing.\n\nEach message carries the rmwzenoh per-message attachment `(sequencenumber, sourcetimestamp, sourcegid): the sequence increments once per publisher per call, the timestamp is publish-time wall-clock nanoseconds — distinct from a message's ownheader.stamp— and the gid identifies this endpoint. A subscriber'sonmessagelost` listener reads the sequence to detect gaps.\n\nPublishing is a no-op (returns nothing without sending) in three cases:\n\nthe publisher is closed,\nits node is closed, or\nisactive is false.\n\nThis is the drop-on-publish point of the managed-node lifecycle gate (LifecycleNode outside Active).\n\nWhen the intra-process short-circuit is enabled, same-Context publisher↔subscriber pairs hand the message over directly, bypassing serialize/Zenoh/decode. Those subscribers open their Zenoh subscriber with a remote-only origin filter, so the direct hand-off is their only copy of this publish; the put still goes out and reaches subscribers in other Contexts normally.\n\nPassing a msg whose type differs from T throws an ArgumentError naming the expected and actual types.\n\npub = Publisher(node, \"/chatter\", std_msgs.msg.String)\npublish(pub, std_msgs.msg.String(data = \"hello\"))\n\n\n\n\n\n","title":"ROSNode.publish"},{"category":"type","location":"communication/topics/#ROSNode.SubscriptionHandle","page":"Topics","text":"SubscriptionHandle{T}\n\nThe handle returned by Subscription(node, topic, T) do msg … end — a subscription for ROS 2 message type T. Wraps the generic Entity, whose consumer task decodes each received sample and runs the stored handler under the subscription's Concurrency policy (Serial or Parallel). isopen/close-able and reaped when its node closes.\n\n\n\n\n\n","title":"ROSNode.SubscriptionHandle"},{"category":"type","location":"communication/topics/#ROSNode.DynamicSubscriptionHandle","page":"Topics","text":"DynamicSubscriptionHandle\n\nThe handle returned by the type-less Subscription(node, topic) do msg … end — a subscription whose message type is resolved at runtime. The data route is wildcarded and each sample's type is resolved per sample, walking these sources in order until one resolves:\n\nthe registry,\nthe project cache,\nament,\na wire GetTypeDescription query.\n\nWraps the generic Entity; isopen/close-able and reaped when its node closes.\n\n\n\n\n\n","title":"ROSNode.DynamicSubscriptionHandle"},{"category":"section","location":"foundations/runtime-model/#The-Runtime-Model","page":"The Runtime Model","text":"Every ROSNode program follows one lifecycle skeleton: open a Context, group entities under a Node, then spin. This page covers that skeleton and the discovery options that shape it.","title":"The Runtime Model"},{"category":"section","location":"foundations/runtime-model/#The-skeleton","page":"The Runtime Model","text":"Open a Context, create a Node, build entities under it, and spin. Here is a publisher reduced to that shape:\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"talker\")\n    pub  = Publisher(node, \"/chatter\", std_msgs.msg.String)\n\n    publish(pub, std_msgs.msg.String(data = \"hello world\"))\n    spin(ctx; handle_signals = true)\nend\n\nThose four pieces — Context, Node, the entities, and spin — appear in every program. Topics, services, and actions each fill in the entity step with their own constructors.\n\nThe from=\"interfaces\" argument tells @ros_import where to find the std_msgs/msg/String definition: it adds a local source root holding the .msg files. Drop it to resolve a name from the vendored tree and a sourced ROS 2 environment. See Interface Types for how @ros_import and from= resolve names.","title":"The skeleton"},{"category":"section","location":"foundations/runtime-model/#Contexts","page":"The Runtime Model","text":"A Context owns the Zenoh session, discovery, and shutdown. The @context do ctx … end form, on exit:\n\ndrains in-flight work,\ncloses the Zenoh session,\nand binds the calling module as the type-resolution home, so wire types resolve against that module's imported structs.\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\nend\n\nThe resolution-home binding is what lets a type-less subscription decode a message into the right Julia struct; see Runtime Type Discovery for how that resolution proceeds.","title":"Contexts"},{"category":"section","location":"foundations/runtime-model/#Nodes","page":"The Runtime Model","text":"A Node(ctx, \"name\") groups the entities you create under it: publishers, subscriptions, servers, and clients. The name is the node's identity on the graph, and every entity built against the node inherits it.\n\nnode = Node(ctx, \"talker\")","title":"Nodes"},{"category":"section","location":"foundations/runtime-model/#spin","page":"The Runtime Model","text":"spin(ctx) parks the main task to keep the process alive while the scheduler delivers messages on their own tasks. The scheduler is the executor: each handler runs on its own task, so a publish loop and a subscription callback make progress independently.\n\nspin(ctx; handle_signals = true)\n\nhandle_signals = true turns Ctrl-C into a graceful drain: ROSNode departs liveliness cleanly and closes the session, and background loops observe isopen(ctx) flip to false so they can stop. Plain spin(ctx) parks the task and leaves signal handling to Julia's default SIGINT path.","title":"spin"},{"category":"section","location":"foundations/runtime-model/#Reaction-errors","page":"The Runtime Model","text":"A fire-and-forget reaction — a subscription handler or an every/timer callback — runs with no caller to return an error to. When one throws an exception it did not catch, the Context's on_reaction_error policy decides what happens:\n\nShutdownOnError (:shutdown, the default) logs the exception once and gracefully drains the Context — the same drain Ctrl-C runs — so spin returns and the process exits cleanly. This stops the failing reaction rather than looping on it: a handler that throws on every message, or a timer that throws every tick, would otherwise flood the log fast enough to starve the interrupt and leave the process effectively unkillable.\nContinueOnError (:continue) logs the exception (rate-limited so it cannot flood) and keeps the reaction running — for a node that must survive a bad message or tick.\n\nSet it on the Context (Context(; on_reaction_error = :continue)) or pass it to run (run(schema; on_reaction_error = :continue)). Services and actions are unaffected: a throwing request or goal handler replies with an error, it does not bring the node down. A message decode failure — malformed or off-type wire data, not your code — is always logged (rate-limited) and dropped, never a shutdown, so a misbehaving peer cannot take the node down.","title":"Reaction errors"},{"category":"section","location":"foundations/runtime-model/#Discovery-options","page":"The Runtime Model","text":"ROSNode mirrors rmw_zenoh, whose discovery is router-based. The peers keyword on @context selects the router to connect to:\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    # ...\nend\n\nEnvironment discovery is the default: without peers, ROSNode scouts via multicast, and the localhost_only / domain_id knobs shape its reach:\n\nKnob Effect Default\npeers Sets the routers/peers to connect to (Zenoh connect/endpoints). none (environment discovery)\nlocalhost_only true disables multicast scouting and, absent explicit peers, connects to the loopback router tcp/localhost:7447. false (reaches peers on other hosts)\ndomain_id Partitions discovery so contexts on different domains stay isolated. ROS_DOMAIN_ID, else 0\n\nROSNode stamps domain_id into every topic keyexpr and the liveliness subscription, so contexts on different domains never meet — see Addressing & Key Expressions for how names become keyexprs.\n\n@context(localhost_only = true, domain_id = 0) do ctx\n    # ...\nend\n\nSee Getting Started for the zenohd command that brings up the router these options point at.","title":"Discovery options"},{"category":"section","location":"foundations/runtime-model/#Next","page":"The Runtime Model","text":"Interface Types\n\nOnce the skeleton is in place, fill the entity step with a topic.","title":"Next"},{"category":"section","location":"foundations/runtime-model/#API-reference","page":"The Runtime Model","text":"","title":"API reference"},{"category":"section","location":"foundations/runtime-model/#Context","page":"The Runtime Model","text":"","title":"Context"},{"category":"section","location":"foundations/runtime-model/#Node","page":"The Runtime Model","text":"","title":"Node"},{"category":"section","location":"foundations/runtime-model/#Settlement-and-shutdown","page":"The Runtime Model","text":"","title":"Settlement and shutdown"},{"category":"section","location":"foundations/runtime-model/#Lifecycle-nodes","page":"The Runtime Model","text":"","title":"Lifecycle nodes"},{"category":"macro","location":"foundations/runtime-model/#ROSNode.@context","page":"The Runtime Model","text":"@context([kwargs...]) do ctx\n    ...\nend\n\nCreate a Context whose type-resolution home is the calling module, then run the do-block body with it and close on exit. Sugar for Context(f; home=@__MODULE__, kwargs...).\n\nBinding the calling module as home makes every keyexpr-only subscription on the Context resolve wire types to that module's @ros_import structs, giving one consistent, load-order-independent picture across the session. Any keyword arguments inside the parentheses pass through to the Context constructor (peers, localhost_only, domain_id, ...).\n\nThrows at macro-expansion time if invoked without a trailing do-block. For a different resolution lens use Context(...; home=Mod); for content-canonical resolution only use a plain Context().\n\n@context(peers = [\"tcp/localhost:7447\"], domain_id = 0) do ctx\n    node = Node(ctx, \"listener\")\n    spin(ctx)\nend\n\n\n\n\n\n","title":"ROSNode.@context"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Context","page":"The Runtime Model","text":"Context(; domain_id=nothing, namespace=nothing, enclave=nothing,\n        config=nothing, format=RmwZenoh(), localhost_only=false,\n        peers=String[], drain_timeout=5.0, shm_clients=nothing, home=nothing,\n        weak_types=false)\nContext(f::Function; kwargs...)\n\nThe process-level container shared by every node in a program — one Context holds exactly one Zenoh Session, and N nodes draw on it. It owns the session and its hex z_id, an atomic entity-id counter, the domain_id, the default namespace/enclave, the keyexpr formatter, the type registry, the discovery index with its @ros2_lv/<domain>/** liveliness subscriber, a clock, the on_shutdown hooks, and the shutdown state machine. It is the RAII/shutdown root: close(ctx) runs the drain, undeclares entities, and closes the session (the rclcpp::init/shutdown bracket).\n\nConfiguration is two layers. The Zenoh session config is Zenoh.jl's Config (config=, default a fresh peer-mode Config); localhost_only and peers layer the ROS transport-shaping env into it:\n\nlocalhost_only=true — disables multicast scouting and, when no explicit peers are given, connects to the loopback router (tcp/localhost:7447).\npeers — sets connect/endpoints.\nshm_clients — passes through to the session open.\n\nThe ROS settings domain_id/namespace/enclave each fall back to an environment variable when left nothing, with the kwarg winning:\n\nkwarg env var fallback\ndomain_id ROS_DOMAIN_ID\nnamespace ROS_NAMESPACE\nenclave ROS_ENCLAVE\n\nnamespace, domain_id, and format shape how names become Zenoh key expressions — namespace normalization, domain scoping, and the wire dialect (RmwZenoh(), the primary target, or Ros2DDS()). See Addressing & Key Expressions.\n\nhome binds the module whose baked __ros_resolve__ table this Context resolves wire types through, so every keyexpr-only subscription resolves against one consistent picture; leave it nothing to resolve types by content alone (see Runtime Type Discovery).\n\nweak_types sets the process-wide type-revision trust. Left false (the default), a pinned type (registered via @ros_import/@ros_cache or authored with @ros_message/@ros_service/@ros_action) enforces its RIHS01: a peer advertising the same name with a different hash is reported with a diagnostic and not bound, so a definition-revision skew surfaces instead of silently rebinding. Set weak_types=true to opt into the dynamic fallback — the mismatched local pin is set aside and the peer's actual revision is wire-discovered and bound for that endpoint. This is independent of the per-subscription weak keyexpr flag, which only widens keyexpr matching.\n\nThe constructor opens the session, registers the canonical bootstrap and statically-generated types, and starts the discovery consumer task before returning.\n\nThe do-block form opens a Context, runs f(ctx), and closes it on exit (even on throw). @context is the sugar that also binds the caller's module as home.\n\nContext(localhost_only = true) do ctx\n    node = Node(ctx, \"talker\")\n    # ... build entities ...\n    spin(ctx)\nend\n\n\n\n\n\n","title":"ROSNode.Context"},{"category":"function","location":"foundations/runtime-model/#ROSNode.spin","page":"The Runtime Model","text":"spin(ctx; handle_signals=false) -> nothing\n\nPark the calling (typically main) task until the Context shuts down, keeping the process alive while Julia's scheduler delivers messages and runs callbacks on their own tasks (the scheduler fills the executor role). Returns once the drain has fully completed.\n\nhandle_signals selects the Ctrl-C behavior:\n\nvalue behavior\nfalse (the default) exactly wait(ctx); Julia's default Ctrl-C handling applies.\ntrue a Ctrl-C (and, when deployed, SIGTERM) drains gracefully (see below).\n\nUnder handle_signals=true, exit_on_sigint(false) makes Ctrl-C a catchable InterruptException, and the spin task catches it: the first interrupt requests a graceful drain (and keeps spinning until it finishes), a second forces exit(130). The spin task polls rather than parking, so it is reliably the task Julia delivers the interrupt to — the drain therefore runs while the runtime is fully alive (the same way in the REPL and deployed), never through process exit, where the teardown scheduler can deadlock. The drain runs every on_shutdown hook and member cleanup. An atexit hook is the backstop for the other exit paths (a normal return, an error, or SIGTERM — which ros2 launch escalates to). Signal state is restored when spin returns, so a library embedding ROSNode keeps its own handling.\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"listener\")\n    Subscription(node, \"/chatter\") do msg\n        @info \"heard\" msg.data\n    end\n    spin(ctx; handle_signals = true)   # Ctrl-C drains gracefully\nend\n\n\n\n\n\n","title":"ROSNode.spin"},{"category":"function","location":"foundations/runtime-model/#ROSNode.session","page":"The Runtime Model","text":"session(ctx) -> Session\n\nThe Context's Zenoh Session, shared by all of its nodes (one session per Context). Entity constructors use it to declare publishers, subscribers, and queryables.\n\n\n\n\n\n","title":"ROSNode.session"},{"category":"function","location":"foundations/runtime-model/#ROSNode.registry","page":"The Runtime Model","text":"registry(ctx) -> TypeRegistry\n\nThe Context's type registry — the per-Context table keyed by (type_name, RIHS01 hash) so evolved versions of a type coexist. The Context owns the storage and lock; the typesupport layer populates and reads entries (static bootstrap types at construction, dynamically-discovered types over the wire). Shared by all of the Context's nodes.\n\n\n\n\n\n","title":"ROSNode.registry"},{"category":"function","location":"foundations/runtime-model/#ROSNode.graph","page":"The Runtime Model","text":"graph(ctx) -> GraphIndex\n\nThe Context's discovery index: the live EndpointInfo set fed by the @ros2_lv/<domain>/** liveliness subscriber and by our own injected entities. The graph-query and graph-wait surface (endpoints, topic_names_and_types, wait_for_service, on_graph_change) are views over this one index.\n\n\n\n\n\n","title":"ROSNode.graph"},{"category":"function","location":"foundations/runtime-model/#ROSNode.local_graph","page":"The Runtime Model","text":"local_graph(node) -> Vector{EndpointInfo}\nlocal_graph(cn::ComponentNode) -> Vector{EndpointInfo}\n\nThe endpoints this node declares for itself — its own is_local slice of the discovery graph (publishers, subscriptions, services, the node-presence shell, parameter services, …), each with its gid. The live counterpart of node_endpoint_descs: a composed node's members' ports are populated a-priori (the local graph is a static function of the plan), while imperative/dynamic endpoints appear as they are created. Reads the local half of the index directly, so it never includes a discovered remote.\n\n\n\n\n\n","title":"ROSNode.local_graph"},{"category":"function","location":"foundations/runtime-model/#ROSNode.describe_graph","page":"The Runtime Model","text":"describe_graph([io], node_or_cn)\n\nPrint the node's own discovery graph — every endpoint it declares, by kind, resolved topic, and type — the discovery-side companion of describe_wiring (which shows the per-member wire-name expansion). Useful for answering \"what does this node advertise?\".\n\n\n\n\n\n","title":"ROSNode.describe_graph"},{"category":"function","location":"foundations/runtime-model/#ROSNode.node_endpoint_descs","page":"The Runtime Model","text":"node_endpoint_descs(cn::ComponentNode) -> Vector{EndpointDesc}\nnode_endpoint_descs(node::Node) -> Vector{EndpointDesc}\n\nThe node's local discovery graph as EndpointDescs — the wire endpoints the node declares for itself, derived a-priori from the node identity and (the ComponentNode form) its constructed members + resolved wire map, before any materialise. The bare Node form covers what any node creates (presence shell, ~/get_type_description, parameter services); the ComponentNode form adds each member's ports (one per pub/sub/service, five per action, none per timer).\n\nEndpoints created imperatively or from runtime data — dynamic subscriptions, the lazy /rosout publisher, runtime clients — are NOT included: they are genuine runtime, not a static function of the plan.\n\n\n\n\n\n","title":"ROSNode.node_endpoint_descs"},{"category":"function","location":"foundations/runtime-model/#ROSNode.resolve_name","page":"The Runtime Model","text":"resolve_name(node_or_ctx, name; kind=:topic) -> String\n\nResolve a user-supplied name to a fully-qualified name, the form the ROSZenoh keyexpr builders expect. Three name forms, following ROS 2 name resolution:\n\nabsolute \"/foo\" — used as-is.\nrelative \"foo\" — prepended with the node's namespace.\nprivate \"~/foo\" (or bare \"~\") — prepended with the node's FQN (namespace/name).\n\nThe resolved name then passes through the remap rules (an ordered exact-match from => to table on the node/context, honored when populated and empty by default) and is charset/structure-validated: leading slash, no empty // token, no trailing slash, each token starting with a letter or underscore and containing only letters, digits, and underscores.\n\nReturns the validated FQN. Throws ArgumentError for:\n\nan empty name.\na private name resolved against a bare Context (no node FQN to anchor ~).\na resolved name that fails validation.\n\nkind (:topic/:service/:node) is carried for kind-scoped remap rules and does not change the resolution algorithm.\n\nThe first argument may be a Context (relative names resolve against its default namespace; private ~ names need a node) or a node exposing namespace/name/fqn.\n\n\n\n\n\n","title":"ROSNode.resolve_name"},{"category":"function","location":"foundations/runtime-model/#ROSNode.on_shutdown","page":"The Runtime Model","text":"on_shutdown(f, ctx) -> f\non_shutdown(f, node) -> f\n\nRegister a cleanup callback f() to run during the Context drain — after blocked waits are woken and before the session closes (the rclcpp::on_shutdown analog). Each hook runs under its own drain_timeout bound: the drain moves on once a hook's timeout elapses, leaving the overrunning hook running detached (so N slow hooks can extend the drain to roughly N × drain_timeout). Exceptions a hook throws are logged, not propagated.\n\nReturns f. The node form recovers the owning Context. Hooks fire in registration order.\n\non_shutdown(ctx) do\n    flush(logfile)\nend\n\n\n\n\n\n","title":"ROSNode.on_shutdown"},{"category":"function","location":"foundations/runtime-model/#ROSNode.deregister_on_shutdown!","page":"The Runtime Model","text":"deregister_on_shutdown!(ctx, f) -> nothing\n\nRemove a hook registered with on_shutdown, matched by identity. Lets a node on a borrowed, long-lived Context (one loaded onto a composition container) drop its teardown hook when it closes, so the Context carries hooks only for its live nodes. Idempotent — a no-op when f is not registered.\n\n\n\n\n\n","title":"ROSNode.deregister_on_shutdown!"},{"category":"function","location":"foundations/runtime-model/#ROSNode.request_shutdown","page":"The Runtime Model","text":"request_shutdown(ctx; reason=\"\") -> Bool\n\nBegin a graceful drain of the Context. Idempotent and task-safe (safe to call from any task, including the Ctrl-C interrupt path): the first caller flips the state running -> shutting_down and spawns the drain task; later calls are no-ops. Returns true if this call initiated shutdown, false if a drain was already underway.\n\nReturns at once — the drain (wake blocked waits, run on_shutdown hooks, undeclare entities, close the session) runs on its own task. To block for completion use close(ctx), wait(ctx), or spin(ctx). A non-empty reason is logged at info level.\n\n\n\n\n\n","title":"ROSNode.request_shutdown"},{"category":"function","location":"foundations/runtime-model/#ROSNode.is_shutdown","page":"The Runtime Model","text":"is_shutdown(ctx) -> Bool\n\nTrue once the Context has begun shutting down — state shutting_down or shutdown_done. The loop-guard predicate behind patterns like while !is_shutdown(ctx); isopen(ctx) is its positive complement. Reads the atomic shutdown state, so it is safe to poll from any task.\n\n\n\n\n\n","title":"ROSNode.is_shutdown"},{"category":"function","location":"foundations/runtime-model/#ROSNode.next_entity_id!","page":"The Runtime Model","text":"next_entity_id!(ctx) -> Int\n\nAllocate the next entity id from the Context's atomic counter and return it. Nodes and endpoints draw from this one counter — the value populates a ROSZenoh entity's id field, which must be unique within the session. The first allocation returns 1 (the counter starts at 0 and increments before reading). Thread-safe: the increment-and-fetch is a single atomic operation.\n\n\n\n\n\n","title":"ROSNode.next_entity_id!"},{"category":"type","location":"foundations/runtime-model/#ROSNode.EndpointDesc","page":"The Runtime Model","text":"EndpointDesc(kind, topic, type_info, qos)\n\nThe wire identity of one endpoint a node declares — kind, resolved topic FQN, type_info, and qos — known before its Entity/Zenoh route exists. The pre-materialisation half of an EndpointInfo; the gid follows from the reserved id at construction. Each pattern derives its own (_publisher_desc, _service_desc, _action_descs, …) as the ONE source for that endpoint's identity; make_entity(node, ::EndpointDesc) turns one into a live entity, and node_endpoint_descs enumerates a node's whole set from the same functions — so materialisation and enumeration share a single derivation, not two kept in sync.\n\n\n\n\n\n","title":"ROSNode.EndpointDesc"},{"category":"type","location":"foundations/runtime-model/#ROSNode.EndpointInfo","page":"The Runtime Model","text":"EndpointInfo\n\nA single discovered or locally-injected endpoint — the atom of the discovery graph. Each record names:\n\nthe owning node (node_name, namespace)\nthe endpoint kind (Publisher/Subscription/Service/Client)\nthe resolved topic FQN\nits type (a TypeInfo: ROS 2 name plus RIHS01 hash)\nthe qos profile\nthe 16-byte gid\nis_local\n\nEach record carries the RIHS01 hash and gid, so a caller can run its own type and QoS compatibility checks against the same data the auto-detectors use. type === nothing for a type-less, liveliness-only endpoint. is_local true marks one of our own injected entities (authoritative, visible ahead of the liveliness round-trip); false marks a liveliness-discovered remote (eventually consistent).\n\nFor a remote endpoint whose liveliness token carries no node identity (the ros2dds bridge-compat case), node_name/namespace are empty and gid is zero-filled.\n\n\n\n\n\n","title":"ROSNode.EndpointInfo"},{"category":"type","location":"foundations/runtime-model/#ROSNode.NodeInfo","page":"The Runtime Model","text":"NodeInfo(name, namespace, enclave)\n\nIdentity of a discovered node: its name, namespace, and security enclave. This is the shape node_names returns from a graph query. node_names reports enclave as \"\": the enclave is unrecoverable from rmw_zenoh liveliness tokens, which always carry % on the wire.\n\n\n\n\n\n","title":"ROSNode.NodeInfo"},{"category":"type","location":"foundations/runtime-model/#ROSNode.GraphIndex","page":"The Runtime Model","text":"GraphIndex()\n\nThe Context's discovery index: the observed EndpointInfo set keyed by liveliness keyexpr, behind a lock, plus a Condition that graph waits park on and a list of user on_graph_change listeners.\n\nThe single @ros2_lv/<domain>/** liveliness subscriber drives it — a token PUT inserts or updates a record, a DELETE removes it by the same key — and our own entities are injected directly by the entity layer. The changed condition is notified on every change so waiters (wait_for_service, on_graph_change) re-check their predicate. This is the one change stream every discovery detector and wait is a view over.\n\nThe no-argument constructor builds an empty index; the Context creates exactly one.\n\n\n\n\n\n","title":"ROSNode.GraphIndex"},{"category":"type","location":"foundations/runtime-model/#ROSZenoh.PublisherKind","page":"The Runtime Model","text":"Publisher(node::Node, topic, ::Type{T};\n          qos=default_qos(), congestion_control=nothing, priority=nothing,\n          warmup=nothing, warmup_sync=nothing, warmup_sample=nothing) -> PublisherHandle{T}\n\nDeclare a publisher for ROS 2 message type T on topic and return a long-lived PublisherHandle{T}. Send messages with publish.\n\nPublisher is the EndpointKind singleton instance (re-exported from ROSZenoh); this call-method gives it the constructor spelling Publisher(node, topic, T), paralleling Timer(f, …). The kind is fixed by the spelling; a bare Subscription(node, topic, T) in this 3-arg position raises an ArgumentError demanding a do-block handler, and the other kinds raise too.\n\nConstruction resolves topic against the node's namespace, builds the ROSZenoh.EndpointEntity (id, kind, topic, type identity, QoS), declares its liveliness token so peers discover the endpoint, records it in the local discovery graph, and declares the Zenoh data route on the topic keyexpr. The handle is tracked on node, so close(node) reaps it; close(pub) undeclares the route and withdraws the token on its own.\n\nKeyword arguments shaping the route:\n\nqos — the ROS 2 QoS profile. Reliability and durability are fields of this profile, e.g. qos=QosProfile(reliability=:reliable, durability=:transient_local); a :transient_local durability latches history to late-joining subscribers (an AdvancedPublisher).\ncongestion_control — passes through to the Zenoh publisher.\npriority — passes through to the Zenoh publisher.\n\nwarmup, warmup_sync, and warmup_sample select the warm-up policy (a WarmupPolicy) that pre-JITs the encode/decode dispatch chain so the first real publish runs at full speed. Under Execute the synthesized publish compiles the path with the wire put suppressed. Each defaults to the node's policy.\n\npub = Publisher(node, \"/chatter\", std_msgs.msg.String)\npublish(pub, std_msgs.msg.String(data = \"hello\"))\n\n\n\n\n\n","title":"ROSZenoh.PublisherKind"},{"category":"type","location":"foundations/runtime-model/#ROSZenoh.SubscriptionKind","page":"The Runtime Model","text":"Subscription(node::Node, topic;\n             qos=default_qos(), view=Owned(), concurrency=Serial(),\n             warmup=nothing, warmup_sync=nothing) do msg … end\n    -> DynamicSubscriptionHandle\n\nDeclare a dynamic subscription whose message type is resolved at runtime from each incoming sample, returning a DynamicSubscriptionHandle. Use it when the topic's type is unknown ahead of time (a recorder, a bridge); graduate to Subscription(node, topic, T) once the type is known for the min-copy fast path.\n\nThis call-method is the type-less spelling of the Subscription kind, distinct from the typed Subscription(node, topic, T) do … end; Subscription is the only kind with a type-less form — a Service needs its type to know request/response. The data route wildcard-matches every type published on the topic; each sample carries its own (name, hash), resolved by walking these sources in order until one resolves:\n\nthe registry,\nthe project cache,\nament,\na wire GetTypeDescription query.\n\nThe handler receives the real generated type msg::T, not a boxed Any. Decode and the handler call cross the world-age boundary via invokelatest, so only that dispatch boundary pays the hop while the handler body stays type-stable. The first sample of each type pays type discovery plus codegen; subsequent samples are fast registry lookups. Resolution and codegen run on a dispatch worker task off the receive thread, so codegen's GC completes while the receiver keeps draining.\n\nview is a ViewMode, with true/false shorthand:\n\nOwned — the default.\nChecked\nUnchecked\n\nHere the resulting CDRView aliases the runtime-resolved type's payload. concurrency is a Concurrency (Serial or Parallel) as for the typed form. Warm-up is deferred to first sight of each runtime type, and Execute degrades to Precompile here (running a discovered handler on a synthesized sample is the manifest's job).\n\nsub = Subscription(node, \"/chatter\") do msg\n    @info \"heard a $(typeof(msg))\" msg\nend\n\n\n\n\n\n","title":"ROSZenoh.SubscriptionKind"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Node","page":"The Runtime Model","text":"Node(ctx, name; namespace=nothing, enclave=nothing, serve_type_description=true,\n     warmup=:off, warmup_sync=false)\nNode(ctx, name, ::Type{P}; overrides=(;), allow_undeclared=false, kwargs...) -> Node\n\nA ROS 2 node sharing Context ctx's Zenoh session. The node is the identity under which publishers, subscriptions, services, clients, timers, and parameters are created, and the naming root for relative/private name resolution; see https://docs.ros.org/en/rolling/Concepts/Basic/About-Nodes.html.\n\nname is the bare node name and must be non-empty. namespace defaults to the Context's namespace; the fully-qualified name is namespace/name, validated at construction. enclave defaults to the Context's enclave (SROS2 security enclave).\n\nConstruction performs these steps:\n\nAllocates an entity id from ctx.\nBuilds the wire identity (ROSZenoh.NodeEntity).\nDeclares the node's liveliness token (the NN node-liveliness kind) so peers discover it.\nInjects the node into the local discovery index so self-queries see it immediately.\nRegisters the node with ctx, so a Context drain closes it.\nWith serve_type_description=true, declares the ~/get_type_description service (matching real ROS 2 nodes on Jazzy and later) so peers can resolve the types this node advertises; a failure declaring it is logged and construction continues.\n\nwarmup and warmup_sync set the node-default WarmupPolicy that pre-JITs the encode/decode dispatch chain for entities created on the node. Default :off: the deployment path is to bake the dispatch chain into the package image at precompile with precompile_node, which is paid once offline and — unlike the runtime warm — does not spawn background compilation that contends with the node's own bring-up. Opt into a runtime warm (:precompile/:execute) for an un-precompiled node (REPL/script), a parametric component whose concrete instantiation precompile_node can't bake, or a dynamic subscription's manifest replay.\n\nKeyword Accepted values Effect\nwarmup a WarmupMode (or :precompile/:execute/:off) Warm-up mode; default :off.\nwarmup_sync false, true Runs an opted-in warm-up asynchronously or synchronously.\n\nEach entity constructor's own warmup/warmup_sync keywords override per-endpoint.\n\nParameter schema form\n\nNode(ctx, name, ::Type{P}) builds a node with a declared parameter schema P (a @parameters struct). It is equivalent to Node(ctx, name; kwargs...) plus a ParameterServer{P} attached at node.parameters, with the six standard parameter services and /parameter_events wired so the node is driveable by any ROS 2 parameter client. overrides overlays startup values (CLI/launch/YAML) onto the schema defaults; allow_undeclared opens the dynamic tier. Remaining kwargs forward to the base constructor. The two-positional-arg form leaves node.parameters === nothing.\n\nEvery entity created against the node is tracked and dies with it. close(node) tears down in order:\n\nLeaves the Context's sim-time set.\nCloses each owned entity in reverse creation order; failures are logged and teardown continues.\nWithdraws the node liveliness token.\nDrops the node from the discovery index.\n\nclose is idempotent and isopen(node) reports liveness. Clock access (clock(node), now(node), Timer(node, …)) reads through the node's per-source clock table, routed to the Context's session or sim time source.\n\nctx = Context()\nnode = Node(ctx, \"sensor_driver\"; namespace=\"/robot1\")\n# node.fqn == \"/robot1/sensor_driver\"\nclose(node)\n\n\n\n\n\n","title":"ROSNode.Node"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Entity","page":"The Runtime Model","text":"Entity\n\nThe generic close-able endpoint handle behind every endpoint pattern (publisher, subscription, service, client). It owns:\n\nthe wire entity (ROSZenoh.EndpointEntity: id, kind, topic, type info, QoS),\nthe entity's liveliness token,\nthe 16-byte source_gid stamped on its attachments,\nits data route, which varies by pattern:\nPublisher: a Zenoh publisher.\nSubscription: a FIFO-channel Zenoh subscriber (the advanced variant for transient_local, whose declaration issues the latched-history query) plus a consumer task.\nService/Client: nothing — its queryable/querier is wired by the pattern layer into the wire slot.\n\nAn Entity is built and registered by make_entity, which allocates the id, declares the liveliness token, and tracks the entity on its node; the pattern layer then attaches the data route. Pattern types (the user-facing publisher/ subscription/service/client objects) hold an Entity so the id/token/route/graph lifecycle lives in one place.\n\nclose(entity) undeclares the route (which terminates a Subscription's consumer task), detaches any pattern-layer wiring in wire, withdraws the liveliness token, and drops the endpoint from the discovery index; it is idempotent, and isopen(entity) reports liveness. A transient_local subscription's Entity also carries a re-latch thunk, fired on a managed node's Inactive→Active transition to re-run its latched-history query, deduplicated against already-delivered samples.\n\nA Subscription Entity's per-sample delivery is the skip-delivery point of the managed-node dispatch gate (isactive): a sample is dropped unless the Node is Active. Use dispose to release a single entity before its node closes.\n\n\n\n\n\n","title":"ROSNode.Entity"},{"category":"function","location":"foundations/runtime-model/#ROSNode.dispose","page":"The Runtime Model","text":"dispose(node::Node, entity::Entity)\n\nClose entity and remove it from node's tracked set, releasing it before the node itself is closed. Use this to reclaim a single publisher/subscription/service/ client while keeping the node alive:\n\nUndeclare its route.\nWithdraw its liveliness token.\nDrop it from the discovery index.\n\nTo tear down every entity still tracked at that point, use close(node) instead.\n\nThe entity is removed from node.entities first, then closed. dispose is a no-op that leaves the entity open when node does not track entity:\n\nIt is owned by another node.\nIt was already disposed.\n\nReturns nothing.\n\n\n\n\n\n","title":"ROSNode.dispose"},{"category":"type","location":"foundations/runtime-model/#ROSNode.ShutdownException","page":"The Runtime Model","text":"ShutdownException()\n\nRaised into every blocked wait — clock sleeps, call, wait_for_service, graph waits — when the owning Context begins to drain. Catching it is the cooperative signal to unwind that wait cleanly and let the Context tear down; the drain path treats it as an expected shutdown and logs it as such. A subscription dispatch loop that sees it returns quietly, treating it as teardown rather than a handler failure.\n\nROSNode's analogue of the rclpy/rclcpp shutdown signal that interrupts blocking calls once rclpy.shutdown() / context invalidation occurs.\n\n\n\n\n\n","title":"ROSNode.ShutdownException"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Cancelled","page":"The Runtime Model","text":"Cancelled()\n\nThrown by the action cancellation checkpoints — checkpoint(goal) and feedback!(goal, …) — when the goal has moved to the CANCELING action goal state. A goal handler that lets it propagate settles cleanly through the fail-safe settlement:\n\nCancelled — fills the result cell with canceled(default_result()), a CANCELED result.\nany other exception — maps to ABORTED.\n\nCancellation then unwinds structurally through the handler's call stack, with no manual status polling.\n\nModels the ROS 2 action goal-state path {ACCEPTED, EXECUTING} → CANCELING → CANCELED.\n\n\n\n\n\n","title":"ROSNode.Cancelled"},{"category":"type","location":"foundations/runtime-model/#ROSNode.SettlementStatus","page":"The Runtime Model","text":"SettlementStatus\n\nSealed abstract supertype of the outcome tokens passed to respond! to settle a service request or an action goal. The singleton instances split by call site:\n\nToken Call site Terminal?\nsuccess service yes\nfailure (alias failed) service yes\nsucceeded action yes\ncanceled action yes\naborted action yes\nfeedback action no — a stream verb, not a terminal cell write\n\nrespond! dispatches on this abstract type, so it accepts any token; the non-terminal feedback passed where a result is due raises an ArgumentError — only terminal tokens fill the result cell. The distinct service and action spellings document each call site rather than enforce a boundary.\n\n\n\n\n\n","title":"ROSNode.SettlementStatus"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.success","page":"The Runtime Model","text":"success :: Success\n\nService-handler success token. respond!(req, success, resp) settles the in-flight request by delivering resp as a Zenoh reply-ok, the normal successful path of a ROS 2 service. Returning a response value from the handler reaches the same outcome — settle_handler! fills the result cell with success when the handler returns without having already settled. A terminal token (fills the write-once result cell).\n\n\n\n\n\n","title":"ROSNode.success"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.failure","page":"The Runtime Model","text":"failure :: Failure\n\nService-handler failure token (spelled failed at some call sites — the same singleton). respond!(req, failure, msg) settles the request as a Zenoh query error reply carrying msg, so the client's call raises ServiceError. A terminal token. The service counterpart of an action goal's aborted, kept a distinct spelling so the service and action call sites read for their own lifecycle.\n\n\n\n\n\n","title":"ROSNode.failure"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.failed","page":"The Runtime Model","text":"failed :: Failure\n\nAlias for failure — the identical singleton, spelled to read naturally at the service call site respond!(req, failed, msg). Settles the request as a Zenoh query error reply, so the client's call raises ServiceError. A terminal token.\n\n\n\n\n\n","title":"ROSNode.failed"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.succeeded","page":"The Runtime Model","text":"succeeded :: Succeeded\n\nAction goal success token. respond!(goal, succeeded, result) — or returning the result from the goal handler — fills the write-once result cell as the SUCCEEDED action goal state, completing the goal normally. A terminal token. The action counterpart of a service's success.\n\n\n\n\n\n","title":"ROSNode.succeeded"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.canceled","page":"The Runtime Model","text":"canceled :: Canceled\n\nAction goal cancellation token. respond!(goal, canceled, result) fills the result cell as the CANCELED action goal state. The fail-safe settlement also synthesizes this outcome when a goal handler unwinds via a thrown Cancelled. A terminal token.\n\n\n\n\n\n","title":"ROSNode.canceled"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.aborted","page":"The Runtime Model","text":"aborted :: Aborted\n\nAction goal abort token. respond!(goal, aborted, result) fills the result cell as the ABORTED action goal state, the explicit failure verb for a goal. Also the fail-safe outcome: a goal handler that throws any exception other than Cancelled, or that returns leaving the result cell empty, is settled as aborted so the client never hangs. A terminal token.\n\n\n\n\n\n","title":"ROSNode.aborted"},{"category":"constant","location":"foundations/runtime-model/#ROSNode.feedback","page":"The Runtime Model","text":"feedback :: Feedback\n\nAction feedback token — a stream verb. respond!(goal, feedback, fb) publishes one feedback message on the goal's feedback topic (the sugar behind feedback!(goal, fb)) and leaves the goal running. It never fills the result cell, so the settlement layer rejects it where a terminal result is due (feedback is not a terminal token).\n\nMirrors ROS 2 action feedback, the progress stream published while a goal executes.\n\n\n\n\n\n","title":"ROSNode.feedback"},{"category":"type","location":"foundations/runtime-model/#ROSNode.ReactionErrorPolicy","page":"The Runtime Model","text":"abstract type ReactionErrorPolicy end\n\nHow a node responds when one of its fire-and-forget reactions — a subscription handler or a every/timer callback — throws an exception it did not catch. Set per Context with Context(; on_reaction_error = …) (or run(…; on_reaction_error = …)); the default is ShutdownOnError. Services and actions are unaffected — a throwing request/goal handler replies with an error, it does not bring the node down.\n\n\n\n\n\n","title":"ROSNode.ReactionErrorPolicy"},{"category":"type","location":"foundations/runtime-model/#ROSNode.ShutdownOnError","page":"The Runtime Model","text":"ShutdownOnError()\n\nThe default ReactionErrorPolicy: an uncaught reaction exception is logged once, then the Context is gracefully drained (request_shutdown) — running shutdown hooks, closing entities, and ending spin so the process exits cleanly. This stops the failing reaction instead of looping on it (a fast handler or timer would otherwise flood the log and starve the Ctrl-C poll). :shutdown is shorthand for ShutdownOnError().\n\n\n\n\n\n","title":"ROSNode.ShutdownOnError"},{"category":"type","location":"foundations/runtime-model/#ROSNode.ContinueOnError","page":"The Runtime Model","text":"ContinueOnError()\n\nA ReactionErrorPolicy for a node that must survive a bad message or tick: an uncaught reaction exception is logged (rate-limited so it cannot flood) and the reaction keeps running. :continue is shorthand for ContinueOnError().\n\n\n\n\n\n","title":"ROSNode.ContinueOnError"},{"category":"type","location":"foundations/runtime-model/#ROSNode.LifecycleNode","page":"The Runtime Model","text":"LifecycleNode(ctx::Context, name::AbstractString; namespace=nothing, enclave=nothing,\n              autostart=false, on_configure=…, on_activate=…, on_deactivate=…,\n              on_cleanup=…, on_shutdown=…, on_error=…, msgs=nothing) -> LifecycleNode\n\nA ROS 2 managed node: a distinct node type that wraps a plain Node and adds the managed-node state machine an external orchestrator drives (~/change_state) and observes (~/get_state, ~/transition_event). Construction starts the node in Unconfigured. See the ROS 2 managed-node design for the state machine.\n\nThe six transition callbacks each take the LifecycleNode and run under the action three-way settlement:\n\nSUCCESS — the callback returns normally; the node lands in the target state.\nFAILURE — the callback returns the failure token, a clean \"can't right now\"; the node reverts to the origin state.\nERROR — the callback throws; on_error runs. Its SUCCESS recovers to Unconfigured; any other outcome — or any on_shutdown error — drops the node to Finalized.\n\nOn the recovery edge, on_cleanup runs after a successful on_error, so recovery to Unconfigured is a true reset: ports close and cleanup hooks run. Keep on_error advisory and put resource release in on_cleanup, which runs on both the error-recovery and the normal teardown paths. Each callback defaults to a no-op, so a node with no behavior still transitions cleanly. A thrown ShutdownException propagates unchanged — it signals context shutdown, not a callback error.\n\nWhile the node is not Active, every application entity created on it is gated at dispatch (isactive):\n\npublishers drop;\nsubscriptions and timers stay quiet;\napplication services error-reply with \"node inactive\".\n\nThe control surface — the five lifecycle_msgs services plus the ~/transition_event topic — stays live in every state, since an external manager must reach it precisely while the node is inactive. Create application entities against inner_node(ln) so they register on the node the gate is keyed by.\n\nautostart=true runs configure! then activate! on construction for the simple single-owner case; a failed or erroring transition leaves the node in whatever state the three-way reached. The ~/transition_event topic is published transientlocal + reliable with depth 1, so a late-joining manager sees the latest transition (matching rclcpp's latched event topic). msgs is accepted for source-compatibility and ignored: the control surface always declares from the vendored `lifecyclemsgs` types.\n\nThe per-node lock serializes transitions, so two racing ~/change_state requests — or a wire request racing an in-process configure! — cannot interleave their callbacks. close (or the Context drain) runs shutdown! before tearing down the node's entities.\n\nnode = LifecycleNode(ctx, \"camera\";\n    on_configure = ln -> (open_device!(); nothing),\n    on_cleanup   = ln -> (close_device!(); nothing))\nimg = Publisher(inner_node(node), \"image\", sensor_msgs.msg.Image)  # gated until Active\nconfigure!(node)   # Unconfigured → Inactive, opens the device\nactivate!(node)    # Inactive → Active, image publishes start flowing\n\n\n\n\n\n","title":"ROSNode.LifecycleNode"},{"category":"type","location":"foundations/runtime-model/#ROSNode.LifecycleState","page":"The Runtime Model","text":"LifecycleState\n\nSealed abstract supertype of the four ROS 2 managed-node primary states: Unconfigured, Inactive, Active, and Finalized. Each is a zero-field singleton, so dispatching on state turns an unhandled state into a MethodError at the call site.\n\nMaps to the primary states of the ROS 2 managed-node lifecycle (lifecycle_msgs/msg/State ids PRIMARY_STATE_UNCONFIGURED/_INACTIVE/_ACTIVE/ _FINALIZED). The four primary states are the only reified ones: a node occupies a transition state (Configuring, Activating, …) exactly for the duration of its callback, so the callback scope is the transition.\n\n\n\n\n\n","title":"ROSNode.LifecycleState"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Unconfigured","page":"The Runtime Model","text":"Unconfigured() :: LifecycleState\n\nThe state immediately after LifecycleNode construction: no configuration loaded, no resources acquired. The ROS 2 PRIMARY_STATE_UNCONFIGURED.\n\nReachable as the initial state, after cleanup! (Inactive → Unconfigured), and as the recovery target when an error-processing callback succeeds. The valid transitions out are configure! (to Inactive) and shutdown! (to Finalized).\n\n\n\n\n\n","title":"ROSNode.Unconfigured"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Inactive","page":"The Runtime Model","text":"Inactive() :: LifecycleState\n\nConfigured and idle: the node's entities exist and stay visible in the graph, and the dispatch gate holds them shut — publishers drop, subscriptions and timers stay quiet, application services error-reply. The ROS 2 PRIMARY_STATE_INACTIVE.\n\nReached by configure! (from Unconfigured) or deactivate! (from Active). Valid transitions out: activate!, cleanup!, and shutdown!.\n\n\n\n\n\n","title":"ROSNode.Inactive"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Active","page":"The Runtime Model","text":"Active() :: LifecycleState\n\nThe live state: the gate is open and application entities dispatch normally — publishers send, subscriptions and timers fire, services serve. The ROS 2 PRIMARY_STATE_ACTIVE, and the only state for which isactive is true.\n\nReached by activate! from Inactive. Valid transitions out: deactivate! and shutdown!. On entry, activate! re-runs each transient_local subscription's latched-history fetch so the node picks up state published while it was inactive.\n\n\n\n\n\n","title":"ROSNode.Active"},{"category":"type","location":"foundations/runtime-model/#ROSNode.Finalized","page":"The Runtime Model","text":"Finalized() :: LifecycleState\n\nThe terminal state, reached once shutdown! has run from any non-terminal state. The ROS 2 PRIMARY_STATE_FINALIZED. No transitions lead out; the node is spent and is typically closed.\n\nAlso the landing when error processing fails to recover: a callback that throws runs on_error, and any on_error outcome other than success drops the node here. A throwing on_shutdown lands here even when on_error recovers — shutdown is the terminal path, so error processing cannot divert it. close runs shutdown! first, which normally lands here.\n\n\n\n\n\n","title":"ROSNode.Finalized"},{"category":"type","location":"foundations/runtime-model/#ROSNode.TransitionResult","page":"The Runtime Model","text":"TransitionResult\n\nAlias for Symbol: the outcome of a lifecycle transition, returned by configure!, activate!, deactivate!, cleanup!, and shutdown!. One of:\n\n:success — the callback returned normally; the node landed in the target state.\n:failure — the callback returned the failure token; the node stayed in its origin state (a clean decline, nothing published).\n:error — the callback threw; on_error ran, recovering to Unconfigured on its success or dropping to Finalized otherwise. A shutdown! error drops to Finalized either way (shutdown is terminal).\n\nThe wire ~/change_state reply reports success = (result === :success).\n\n\n\n\n\n","title":"ROSNode.TransitionResult"},{"category":"function","location":"foundations/runtime-model/#ROSNode.isactive","page":"The Runtime Model","text":"isactive(ln::LifecycleNode) -> Bool\nisactive(node::Node) -> Bool\nisactive(entity::Entity) -> Bool\n\nThe dispatch gate predicate: whether an entity fires right now.\n\nArgument type Active when Exception\nLifecycleNode in the Active state —\nNode not the wrapped node of some LifecycleNode, found through an identity-keyed registry (one lock-guarded lookup; an unmanaged node is absent and always active) —\nEntity its node is active the control surface of a managed node (the lifecycle_msgs services and the ~/transition_event publisher) is always active, so an external manager can drive and observe an inactive node\n\nThis single predicate is threaded into each data-plane dispatch site — publish, subscription delivery, timer tick, service handling — so while a managed node is not Active its application entities are silenced automatically:\n\npublishers drop;\nsubscriptions and timers skip the handler;\nservices error-reply.\n\nEach call recomputes from the node's current atomic state.\n\n\n\n\n\n","title":"ROSNode.isactive"},{"category":"function","location":"foundations/runtime-model/#ROSNode.inner_node","page":"The Runtime Model","text":"inner_node(ln::LifecycleNode) -> Node\n\nThe plain Node wrapped by a managed node. Create application entities against it — Publisher(inner_node(ln), \"image\", T), Timer(inner_node(ln), period) do … end, Subscription(inner_node(ln), \"cmd\", T) do msg … end — so they register on the node the dispatch gate is keyed by and are silenced automatically while the managed node is not Active.\n\nThe pattern constructors are typed on Node; unwrap with inner_node at each construction site. The returned node carries the context, fully-qualified name, namespace, and clocks; naming and clock helpers reached through the LifecycleNode forward to it.\n\n\n\n\n\ninner_node(c::Container) -> Node\n\nThe container's own management node, hosting the ~/_container/{load_node,unload_node,list_nodes} composition services. The accessor returning the wrapped Node; see inner_node(::LifecycleNode) for the analogous unwrap on a managed node.\n\n\n\n\n\n","title":"ROSNode.inner_node"},{"category":"function","location":"foundations/runtime-model/#ROSNode.configure!","page":"The Runtime Model","text":"configure!(ln::LifecycleNode) -> TransitionResult\n\nDrive the Unconfigured → Inactive transition, running the on_configure callback. The conventional place to create the node's entities (gated until Active) and acquire resources such as devices or files. Matches the ROS 2 managed-node configure transition.\n\nReturns a TransitionResult:\n\n:success — the callback lands the node in Inactive.\n:failure — the callback returns the failure token; the node stays Unconfigured.\n:error — the callback throws; on_error runs.\n\nHolds the node's transition lock so it cannot interleave with a concurrent transition. Throws ArgumentError if the node is closed, or if it is not currently Unconfigured.\n\n\n\n\n\n","title":"ROSNode.configure!"},{"category":"function","location":"foundations/runtime-model/#ROSNode.activate!","page":"The Runtime Model","text":"activate!(ln::LifecycleNode) -> TransitionResult\n\nDrive the Inactive → Active transition, running the on_activate callback. The callback is usually empty: gating is automatic, so activation is the state flip that opens the dispatch gate. Matches the ROS 2 managed-node activate transition.\n\nReturns a TransitionResult:\n\n:success — the node lands in Active.\n:failure — the callback returns the failure token; the node stays Inactive.\n:error — the callback throws.\n\nThrows ArgumentError if the node is closed or not currently Inactive.\n\nOn success, after the gate opens, each transientlocal subscription on the node re-runs its latched-history query (the ROS 2 transientlocal durability replay), so the node picks up the latest state it dropped while inactive. The replay is novelty-gated — a sample already seen is skipped across deactivate/reactivate cycles — and runs per-entity best-effort after the transition lock is released: a re-latch failure is logged without unwinding the transition, and the replay can overlap a subsequent transition.\n\n\n\n\n\n","title":"ROSNode.activate!"},{"category":"function","location":"foundations/runtime-model/#ROSNode.deactivate!","page":"The Runtime Model","text":"deactivate!(ln::LifecycleNode) -> TransitionResult\n\nDrive the Active → Inactive transition, running the on_deactivate callback. Usually empty, since gating is automatic; the state flip re-silences the node's application entities. Matches the ROS 2 managed-node deactivate transition.\n\nReturns a TransitionResult:\n\n:success — the node lands in Inactive.\n:failure — the callback returns the failure token; the node stays Active.\n:error — the callback throws.\n\nThrows ArgumentError if the node is closed or not currently Active.\n\n\n\n\n\n","title":"ROSNode.deactivate!"},{"category":"function","location":"foundations/runtime-model/#ROSNode.cleanup!","page":"The Runtime Model","text":"cleanup!(ln::LifecycleNode) -> TransitionResult\n\nDrive the Inactive → Unconfigured transition, running the on_cleanup callback. The place to release what on_configure acquired — close entities, free devices — returning the node to a fresh Unconfigured state it can be reconfigured from. Matches the ROS 2 managed-node cleanup transition.\n\nReturns a TransitionResult:\n\n:success — the node lands in Unconfigured.\n:failure — the callback returns the failure token; the node stays Inactive.\n:error — the callback throws.\n\nThrows ArgumentError if the node is closed or not currently Inactive.\n\n\n\n\n\n","title":"ROSNode.cleanup!"},{"category":"function","location":"foundations/runtime-model/#ROSNode.shutdown!","page":"The Runtime Model","text":"shutdown!(ln::LifecycleNode) -> TransitionResult\n\nDrive the terminal {Unconfigured, Inactive, Active} → Finalized transition, running the on_shutdown callback. Valid from any non-terminal state, and the transition close / the Context drain reaches when closing a managed node. Matches the ROS 2 managed-node shutdown transition; the ~/transition_event carries the origin-specific transition id (TRANSITION_UNCONFIGURED_SHUTDOWN / TRANSITION_INACTIVE_SHUTDOWN / TRANSITION_ACTIVE_SHUTDOWN).\n\nReturns a TransitionResult:\n\n:success — the node lands in Finalized. A node already Finalized is a :success no-op (idempotent), even when closed.\n:error — on_shutdown throws. on_error runs (cleanup), but the node lands in Finalized regardless of its outcome — shutdown is the terminal path, so error recovery cannot return the node to service.\n\nThrows ArgumentError if a non-Finalized node is closed.\n\n\n\n\n\n","title":"ROSNode.shutdown!"},{"category":"section","location":"foundations/interface-types/#Interface-Types","page":"Interface Types","text":"Every topic, service, and action carries a typed message. That type fixes the field layout on the wire and gives the endpoint its identity, so a publisher and subscriber agree on exactly what crosses the channel. The fastest way to get a type is to import an existing interface by name.","title":"Interface Types"},{"category":"section","location":"foundations/interface-types/#Importing-existing-interfaces","page":"Interface Types","text":"@ros_import bakes an existing interface by its fully-qualified name. The from=\"…\" argument adds a local source root, so your own .msg/.srv/.action definitions resolve alongside the vendored tree and a sourced ROS 2 environment.\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n@ros_import \"example_interfaces/srv/AddTwoInts\" from=\"interfaces\"\n@ros_import \"action_tutorials_interfaces/action/Fibonacci\" from=\"interfaces\"\n\nThe binding follows the interface kind:\n\nA message binds the bare leaf type, e.g. std_msgs.msg.String. Construct it by that name: std_msgs.msg.String(data = \"hello\").\nA service binds the bare namespace, exposing .Request and .Response. Reference it by .Request; ROSNode resolves the Response automatically.\nAn action binds the bare namespace, which serves as the action handle and exposes .Goal, .Result, and .Feedback.\n\npub = Publisher(node, \"/chatter\", std_msgs.msg.String)\n\nsrv = Service(node, \"/add_two_ints\", AddTwoInts.Request) do req\n    AddTwoInts.Response(sum = req.a + req.b)\nend\n\nserver = ActionServer(node, \"/fibonacci\", Fibonacci) do goal\n    seq = Int32[0, 1]\n    feedback!(goal, Fibonacci.Feedback(partial_sequence = seq))\n    Fibonacci.Result(sequence = seq)\nend\n\nOne @ros_import call handles several interfaces, with these knobs:\n\nfrom= accepts one directory or a vector of source roots.\nas Alias renames a message binding when leaf names would clash. It applies to messages only; a service or action has no single struct to bind, so an as on either errors.\n\nDrop from= to resolve a name from the vendored tree and the local ROS 2 environment:\n\n@ros_import \"std_msgs/msg/String\"\n\nROSNode searches three sources for each name, in order:\n\nthe from= source roots, when given;\nROSNode's vendored interface tree;\nthe local ROS 2 install, scanned in precedence order:\nroots registered with add_search_path!;\nAMENT_PREFIX_PATH (a sourced workspace);\nCMAKE_PREFIX_PATH;\nthe /opt/ros/<distro> system installs (rolling, jazzy, kilted, lyrical, humble).\n\nBecause from= roots come first, a local package can supply a new type or override a vendored or environment one.","title":"Importing existing interfaces"},{"category":"section","location":"foundations/interface-types/#Two-more-ways-to-get-a-type","page":"Interface Types","text":"Beyond importing, two paths give you a usable type:\n\nAuthor one directly in Julia source with @ros_message/@ros_service/@ros_action — see Authoring Interfaces in Julia.\nDiscover one over the wire and resolve it at runtime — see Runtime Type Discovery.","title":"Two more ways to get a type"},{"category":"section","location":"foundations/interface-types/#Wire-identity-(RIHS01)","page":"Interface Types","text":"A ROS interface's wire type is identified by its name plus its RIHS01 hash, derived from the field layout. Two definitions sharing both the interface name and the RIHS01 hash are the same wire type. This identity is what lets independently defined types interoperate: an authored @ros_message/@ros_service/@ros_action carries the same RIHS01 as the equivalent .msg/.srv/.action, so it speaks the same wire type as its imported twin and as C++/Python ROS 2 nodes.\n\nThat shared identity is what makes ROSNode wire-compatible with ROS 2 — see Interoperating with ROS 2 for the ros2 CLI commands that exercise it.\n\nas casts a value to a sibling type that shares its RIHS01, copying the field values into the target type:","title":"Wire identity (RIHS01)"},{"category":"section","location":"foundations/interface-types/#Next","page":"Interface Types","text":"Carry one of these types onto a channel in Topics. For how a type's value reaches your handler, see The Runtime Model.","title":"Next"},{"category":"function","location":"foundations/interface-types/#ROSNode.as","page":"Interface Types","text":"as(x, ::Type{T}) -> T\n\nCast a ROS message value x to the layout-compatible message type T by round-tripping it through the CDR wire codec. Two distinct Julia structs that share one wire type carry equal RIHS01 type hashes, hence identical CDR field layout — for example two modules' separate aliases of sensor_msgs/msg/Image. as encodes x to its CDR bytes and decodes those bytes as T, handing the value across the nominal-type boundary with the same round-trip invariant the transport relies on.\n\ntypeof(x) === T — returns x itself (identity short-circuit).\notherwise — a fully-owned T; the decode copies every field out (see decode_owned).\n\nLayout compatibility is the caller's contract: pass only equal-RIHS01 types. The decode is positional, so a mismatched T fails one of two ways:\n\noverruns the payload — an EOFError/BoundsError.\ndecodes silently into garbage — when its fields happen to fit the bytes.\n\nUse it when a value built as one alias reaches code expecting another. Type resolution warns when two distinct Julia structs settle on one RIHS01 wire type at a common dependent; handing such a value across that boundary is the canonical case: f(as(msg, ThatType)).\n\nA borrowed CDRView (from decode(...; view=true)) takes its own method:\n\nit stores its fields in one NamedTuple, so it cannot walk the encoder directly — it is copied out of its buffer first (decode_owned) and the owned value cast.\nas on a view therefore always returns an owned T, including when T is the view's own tag.\nfor a zero-copy cast of a view to a sibling type use CDRSerialization.retag.\n\n# Illustrative: ImageA and ImageB stand for two modules' aliases of\n# sensor_msgs/msg/Image (equal RIHS01).\nimg_b = as(img_a, ImageB)   # owned ImageB carrying img_a's field values\nas(img_a, ImageA)           # returns img_a itself (identity short-circuit)\n\n\n\n\n\n","title":"ROSNode.as"},{"category":"section","location":"foundations/addressing/#Addressing-and-Key-Expressions","page":"Addressing & Key Expressions","text":"By default, ROSNode mirrors rmw_zenoh on the wire: every topic, service, and action maps to a Zenoh key expression (keyexpr), and every node and endpoint announces itself with a liveliness token that peers read to build their discovery graph. This page covers the machinery behind Context and the topic, service, and action constructors — how a name becomes a keyexpr, how the domain scopes it, and which wire dialect spells it.","title":"Addressing & Key Expressions"},{"category":"section","location":"foundations/addressing/#Name-resolution","page":"Addressing & Key Expressions","text":"resolve_name turns a user-supplied name into the fully-qualified name (FQN) a keyexpr is built from, applying ROS 2 name resolution to the three name forms:\n\nabsolute \"/chatter\" — used as-is.\nrelative \"chatter\" — prepended with the node's namespace.\nprivate \"~/chatter\" — prepended with the node's FQN (namespace then name).\n\nIt then, in order:\n\nnormalizes the namespace — a leading slash, no trailing slash; the empty namespace becomes /;\napplies an ordered exact-match from => to remap table (empty by default);\nvalidates the result.\n\nA valid FQN:\n\nis absolute — starts with /;\ncontains no empty // token;\nhas no trailing slash;\nspells each token with a leading letter or underscore, then letters, digits, and underscores.","title":"Name resolution"},{"category":"section","location":"foundations/addressing/#Domain-scoping","page":"Addressing & Key Expressions","text":"domain_id (the Context keyword, falling back to ROS_DOMAIN_ID, then 0) scopes both topic traffic and graph discovery to one domain: two contexts on different domains share a router but each sees only the endpoints within its own domain. Under the default RmwZenoh dialect it leads every topic keyexpr and liveliness token, so the @ros2_lv/<domain>/** discovery subscription itself stays in-domain. Reaching a peer means matching its domain_id — including the ROS_DOMAIN_ID of stock ROS 2 nodes (see Interoperating with ROS 2).","title":"Domain scoping"},{"category":"section","location":"foundations/addressing/#Topic-key-expressions","page":"Addressing & Key Expressions","text":"Under the default RmwZenoh dialect, a topic's data keyexpr is four slash-separated components:\n\n<domain>/<topic>/<type>/<hash>\n\nComponent Value\n<domain> the integer domain_id\n<topic> the resolved FQN with its leading slash stripped, internal slashes preserved (/ns/chatter becomes ns/chatter)\n<type> the message type in DDS form — std_msgs/msg/String becomes the single component std_msgs::msg::dds_::String_\n<hash> the type's RIHS01 identity (see Interface Types)\n\nA type-less subscription routes on a wildcard data keyexpr (<domain>/<topic>/**), so every type published on the topic reaches it; its own liveliness token carries an empty-type placeholder in the <type>/<hash> slots. See Runtime Type Discovery.","title":"Topic key expressions"},{"category":"section","location":"foundations/addressing/#Discovery:-liveliness-tokens","page":"Addressing & Key Expressions","text":"Every node and endpoint declares a Zenoh liveliness token under the admin space @ros2_lv/<domain>/, and one @ros2_lv/<domain>/** subscriber on the Context feeds the discovery graph as tokens arrive and depart. The token is slash-structured, so it escapes any / inside a component (<ns>, <name>, <topic>, <type>) to %:\n\n@ros2_lv/<domain>/<zid>/<nid>/<eid>/<kind>/<enclave>/<ns>/<name>/<topic>/<type>/<hash>/<qos>\n\nThe <enclave> slot is always the placeholder %, because rmw_zenoh omits the enclave from the token, and the QoS profile rides in the final component as a colon-separated field set. ROSNode reads these tokens to answer graph queries — which publishers and subscribers exist on a topic — and to gate type matching.","title":"Discovery: liveliness tokens"},{"category":"section","location":"foundations/addressing/#Format-dialects","page":"Addressing & Key Expressions","text":"format (the Context keyword) selects the keyexpr dialect:\n\nRmwZenoh() — the default and primary target, matching the native rmw_zenoh_cpp RMW (the shapes above).\nRos2DDS() — the zenoh-plugin-ros2dds DDS bridge: bare topic-name keyexprs with no type or hash, and § as the liveliness slash-escape.\n\nEach dialect interoperates with stock C++/Python ROS 2 nodes running the matching middleware; see Interoperating with ROS 2. The ROS 2 middleware-vendor concept is documented upstream at About Different Middleware Vendors.","title":"Format dialects"},{"category":"section","location":"advanced/precompilation/#Precompilation-and-Warm-up","page":"Precompilation & Warm-up","text":"The first message on a topic, the first request to a service, or the first goal to an action compiles the whole type-specialized chain — decode, your handler, and everything the handler calls. That one-time JIT cost lands as a startup spike, or as a stall on the first live message. Two mechanisms compile that chain ahead of the first live message: warm-up compiles it at endpoint construction, and the node bake folds a component's first-run path into its package's precompiled image.","title":"Precompilation & Warm-up"},{"category":"section","location":"advanced/precompilation/#Two-tiers","page":"Precompilation & Warm-up","text":"Tier Runs at Covers How\nWarm-up endpoint construction each endpoint's decode→handler chain, recompiled each process opt-in per node via warmup = :precompile/:execute; node default :off\nNode bake the consuming package's precompile a component's typed carriers, lifecycle fan-out, and reaction/serve bodies, baked into its pkgimage one line: precompile_node in a @compile_workload\n\nThe two compose: warm-up can be enabled on any node (opt-in, off by default), and the bake additionally makes a precompiled component start from an already-compiled image, so even its first process is hot.","title":"Two tiers"},{"category":"section","location":"advanced/precompilation/#Warm-up","page":"Precompilation & Warm-up","text":"Every endpoint warms its own dispatch chain at construction under a WarmupPolicy — a mode paired with a sync flag.\n\nThe mode chooses how deep warming reaches, cheapest first:\n\n:precompile precompile-anchors the decode→handler chain: it caches the inference tree and codegens the type-specialized frame, side-effect-free and with no message instance.\n:execute additionally runs the handler once on a synthesized sample message, reaching full native depth. Outbound ROS operations (publish/call) null-route, and side effects marked with @effectful are skipped, so warming emits no real traffic and touches no external state.\n:off (the node default) skips warm-up.\n\nThe sync flag chooses when warming happens:\n\nwarmup_sync = false (the default) warms on a background task — zero construction latency.\nwarmup_sync = true blocks the constructor until the chain is compiled — for hard real-time bring-up, where the first message must already be hot.\n\nSet the policy as the node default that every endpoint inherits, or override it per endpoint:\n\nnode = Node(ctx, \"vehicle\"; warmup = :precompile, warmup_sync = true)\n\nSubscription(node, \"/cmd_vel\", geometry_msgs.msg.Twist;\n             warmup = :execute, warmup_sync = true) do msg\n    drive(msg)\nend\n\nA functor node passes the same warmup/warmup_sync to run — run(Vehicle; warmup = :precompile, warmup_sync = true) — and every member endpoint inherits it.\n\nA handler written for live data may reject a synthesized sample, so an :execute warm-up tolerates one:\n\nit runs the handler under a warm scope where is_warming is true, and\na throw degrades that handler to compile-only with a warning.\n\nPass warmup_sample a representative message when the default builder can't synthesize a realistic one.\n\nWarm-up reaches your handler only when the chain is monomorphic — a static, typed subscription. A dynamic subscription resolves its type per sample, so it warms the codec and replays a discovered-type manifest at startup instead; see Runtime Type Discovery.","title":"Warm-up"},{"category":"section","location":"advanced/precompilation/#The-node-bake","page":"Precompilation & Warm-up","text":"A node kind declared in a precompiled package bakes its first-run path into that package's image with precompile_node, so a later run skips first-touch codegen entirely. Place it inside a PrecompileTools @compile_workload, after the node(…) schema and every reaction handler are defined:\n\nmodule Vehicle\nusing ROSNode, PrecompileTools\nimport ROSNode: member_schema\n@ros_import \"std_msgs/msg/Bool\"\n\nmutable struct Drive{Name} <: Component{Name} end\nDrive{Name}() where {Name} = Drive{Name}()\n@parameters struct DriveParams\n    max_speed::Float64 = 2.0\nend\nbrake(node, d::Drive, msg::std_msgs.msg.Bool) = msg.data && stop!(d)\nmember_schema(::Type{Drive}) = component(Drive, DriveParams,\n    hears(:brake, std_msgs.msg.Bool, brake))\n\nconst Rig = node(\"drive\" => Drive; name = \"Rig\")\n\n# Re-register the const schema at LOAD — node(…; name=…) defers the registry mutation\n# under precompilation, so a precompiled package must re-register from __init__.\n__init__() = register_node_kind!(\"Rig\", Rig)\n\n@compile_workload precompile_node(Rig)\nend\n\nprecompile_node(Rig) combines precompile(run, (typeof(Rig),)) with a walk over the schema's frozen descriptors. It bakes, into the consuming package's precompile image:\n\nthe node's run specialization, the @generated carrier builders, and the lifecycle fan-out (construct → materialise → configure → activate → teardown), anchored on the node's concrete ComponentNode type;\neach port's codec (encode/decode), wire resolution, and per-handle close;\nthe spawned consume/serve/dispatch bodies — the subscription receive loop, the service request→handler→response serve tree, and the intra-process delivery leg — keyed on the member's concrete type and each reaction's handler.\n\nPrefer the value form precompile_node(Rig) over the type form precompile_node(typeof(Rig)): the value form anchors reaction bodies from the node's frozen handlers, so inline component(…) members and rebound/remapped handlers bake correctly. The type form is best-effort: with no handler values it (1) falls back to a guarded member_schema(base) lookup, and (2) skips an inline component(…) member instead of aborting the precompile.\n\nThe bake reaches a member whose ports have statically derivable handle types. Action ports are anchored too, but an action whose handle materialises only at run keeps the generic accessor and warms at first run instead.\n\nprecompile_node is opt-in and side-effect-free: it runs only precompile and codegen, never your reactions, and fires only at precompile. A node without it still works — the same specializations generate lazily on the first run.","title":"The node bake"},{"category":"section","location":"advanced/precompilation/#Registering-schemas-at-load","page":"Precompilation & Warm-up","text":"node(…; name = …) registers the kind in the process-global registry so a container can load_node it by name. Under precompilation that registry mutation is discarded (it would not survive the package cache), so a precompiled package re-registers its const schemas at load. The minimal form is an explicit __init__:\n\n__init__() = register_node_kind!(\"Rig\", Rig)\n# or, for several kinds:  register_node_kinds!(Rig, Counter, …)\n\nFor the common case @register_nodes handles it — it rosters the kinds and auto-installs an __init__ (the same hook that absorbs @ros_message/@service types) when the module has none:\n\n@register_nodes Rig\n\nIn the REPL, scripts, and runtime, registration is immediate and needs no __init__.","title":"Registering schemas at load"},{"category":"section","location":"advanced/precompilation/#See-also","page":"Precompilation & Warm-up","text":"Components — authoring nodes as components and composing them with node.\nMessage Delivery — the view modes and concurrency a warmed handler runs under.\nRuntime Type Discovery — warm-up and manifest replay for dynamic subscriptions.\nJulia manual: Module initialization and precompilation.\nPrecompileTools.jl — the workload mechanism precompile_node builds on.","title":"See also"},{"category":"section","location":"advanced/precompilation/#API-reference","page":"Precompilation & Warm-up","text":"","title":"API reference"},{"category":"type","location":"advanced/precompilation/#ROSNode.WarmupPolicy","page":"Precompilation & Warm-up","text":"WarmupPolicy(mode, sync)\n\nPer-entity warm-up policy. mode is a WarmupMode (or its :precompile / :execute / :off shorthand) selecting how the dispatch chain is warmed; sync selects when:\n\nfalse (default) — warms on a background task; zero construction latency.\ntrue — blocks the constructor until warm; first message guaranteed compiled (hard real-time).\n\n\n\n\n\n","title":"ROSNode.WarmupPolicy"},{"category":"type","location":"advanced/precompilation/#ROSNode.WarmupMode","page":"Precompilation & Warm-up","text":"WarmupMode\n\nSealed abstract supertype selecting how an entity's dispatch chain is warmed — the mode of a WarmupPolicy, on a depth-versus-cost curve:\n\nNoWarmup (:off) — no warm-up; the node default (warming is baked offline by precompile_node instead of run at construction).\nPrecompile (:precompile) — a side-effect-free precompile-anchor of the dispatch chain.\nExecute (:execute) — additionally run the handler once on a sample message at full native depth, side effects suppressed.\n\n\n\n\n\n","title":"ROSNode.WarmupMode"},{"category":"type","location":"advanced/precompilation/#ROSNode.Precompile","page":"Precompilation & Warm-up","text":"Precompile()\n\nA WarmupMode: a side-effect-free precompile-anchor of the dispatch chain — caches the inference tree and codegens the named frame and its inlined callees, needing no message instance. :precompile is shorthand for Precompile(). Opt-in (the node default is NoWarmup).\n\n\n\n\n\n","title":"ROSNode.Precompile"},{"category":"type","location":"advanced/precompilation/#ROSNode.Execute","page":"Precompilation & Warm-up","text":"Execute()\n\nWarmupMode that additionally runs the handler once on a synthesized sample message under the warm-up scope (is_warming true), reaching full native depth including the handler body; outbound ROS effects null-route and @effectful blocks skip. :execute is shorthand for Execute().\n\n\n\n\n\n","title":"ROSNode.Execute"},{"category":"type","location":"advanced/precompilation/#ROSNode.NoWarmup","page":"Precompilation & Warm-up","text":"NoWarmup()\n\nWarmupMode that disables warm-up: the first message JITs the dispatch chain. :off is shorthand for NoWarmup().\n\n\n\n\n\n","title":"ROSNode.NoWarmup"},{"category":"function","location":"advanced/precompilation/#ROSNode.precompile_node","page":"Precompilation & Warm-up","text":"precompile_node(schema::NodeSchema)\nprecompile_node(::Type{<:NodeSchema})\n\nBake a functor node's first-run path into the calling package's precompile image. Call it from a consumer @compile_workload (PrecompileTools) so the node's run specialization, the @generated carrier builders, the lifecycle fan-out, and the spawned consume/serve/dispatch bodies compile ahead of time — turning a cold first run into a near-warm one. Combines precompile(run, (typeof(schema),)) with the precompile_schema spawned-task/reaction walker.\n\nusing ROSNode, PrecompileTools\nconst Rig = node(\"a\" => A, \"b\" => B; name = \"Rig\")\n@compile_workload precompile_node(Rig)\n\n\n\n\n\n","title":"ROSNode.precompile_node"},{"category":"section","location":"composition/containers/#Containers-and-Dynamic-Composition","page":"Containers & Dynamic Composition","text":"A node kind runs at two scales, and its code is identical at both. Deploy-time wiring picks the scale.","title":"Containers & Dynamic Composition"},{"category":"section","location":"composition/containers/#Two-scales","page":"Containers & Dynamic Composition","text":"Run a node standalone or inside a container. Both share sessions, discovery, and registry; they differ only in namespacing: a standalone node exposes its entities under its own name, while a container namespaces each node it holds.\n\nStandalone nodes run directly — run(Vehicle; name = \"vehicle\") — exposing their entities under the given name.\n\nSeveral nodes can share one process through a container. Each add! instantiates a node on the container's single Context, so the nodes share one session, discovery, and registry, plus a direct in-process delivery path between them:\n\ncontainer(\"fleet\") do c\n    add!(c, Vehicle;       name = \"vehicle\")\n    add!(c, GroundStation; name = \"ground\")\nend\n\nThe same Vehicle schema runs either way, its code unchanged.","title":"Two scales"},{"category":"section","location":"composition/containers/#Loading-a-node-by-name","page":"Containers & Dynamic Composition","text":"node(…; name = \"Vehicle\") registers the kind in a process-global registry under that name. A container then loads it by name through load_node — the path a ros2 component load request drives over the container's ~/_container/load_node service:\n\ncontainer(\"fleet\") do c\n    load_node(c, \"drone\", \"Vehicle\"; name = \"vehicle\",\n              parameters = (var\"sensor.rate\" = 10,))\nend\n\nload_node(c, package, plugin; …) resolves the kind registered as plugin, instantiates it on the container, and returns the node with its unique id:\n\npackage — scopes the lookup; may be empty.\nplugin — the registered kind name.\nname — instance name; defaults to the registered kind name.\nparameters — overrides member parameters by their composite <member>.<field> names.\n\nThe three container operations map onto the ROS2 component verbs:\n\nFunction Behavior ROS2 path\nload_node instantiates a registered kind, returns the node and its id ros2 component load\nunload_node closes a loaded node and drops it from the container ros2 component unload\nlist_nodes returns the loaded (id, name) pairs ros2 component list","title":"Loading a node by name"},{"category":"section","location":"composition/containers/#Registration-in-a-precompiled-package","page":"Containers & Dynamic Composition","text":"node(…; name=) registers the kind by one of two paths, because a build-time registration cannot reach the loaded image:\n\nREPL, script, or runtime: it registers the kind immediately.\nPrecompiled package: it skips registration at build time, so the package re-registers its kinds at load, from its __init__:\n\nconst Vehicle = node(\"sensor\" => Sensor, \"guard\" => Guard; name = \"Vehicle\")\n\n__init__() = register_node_kinds!(@__MODULE__, Vehicle)\n\n@register_nodes Vehicle … expands to that register_node_kinds! call, and a package with its own __init__ calls ros_init!(@__MODULE__) to fold the registration into existing load-time setup.","title":"Registration in a precompiled package"},{"category":"section","location":"composition/containers/#Inspecting-a-composed-node","page":"Containers & Dynamic Composition","text":"describe_wiring (see Components) prints a built node's resolved wiring. The schema-introspection accessors read a member_schema or a built node programmatically — the members of a node, the ports a member declares, the message type a port carries:\n\nms = member_schema(Sensor)\nport_names(ms)                        # [:telemetry, :tick]\nmessage_type(port(ms, :telemetry))   # Telemetry\nmember_names(Vehicle)                 # [:sensor, :guard]","title":"Inspecting a composed node"},{"category":"section","location":"composition/containers/#API-reference","page":"Containers & Dynamic Composition","text":"","title":"API reference"},{"category":"section","location":"composition/containers/#Containers","page":"Containers & Dynamic Composition","text":"","title":"Containers"},{"category":"section","location":"composition/containers/#Node-kind-registry","page":"Containers & Dynamic Composition","text":"","title":"Node-kind registry"},{"category":"section","location":"composition/containers/#Schema-introspection","page":"Containers & Dynamic Composition","text":"","title":"Schema introspection"},{"category":"type","location":"composition/containers/#ROSNode.Container","page":"Containers & Dynamic Composition","text":"Container\n\nA set of nodes sharing one process and one Context — the deploy-time composition of run's standalone form. Built by container; add! instantiates each node on the shared Context, so the nodes share one session, discovery, type registry, and Julia scheduler, plus — when intra-process delivery is enabled (container's default) — the intra-process short-circuit, where same-Context publisher↔subscriber pairs bypass serialize/Zenoh/decode.\n\nThe container is itself a ROS node (inner_node(c), named after the container) hosting the ~/_container/{load_node,unload_node,list_nodes} services over the vendored composition_interfaces types, so ros2 component load/unload/list and the ComposableNodeContainer launch action drive it. Every loaded node is tracked under a container-unique id (1-based, monotonic), reported by list_nodes / ros2 component list and addressable by unload_node / ros2 component unload.\n\nConstruct one through container. close(c) closes the shared Context, which drains every loaded node.\n\n\n\n\n\n","title":"ROSNode.Container"},{"category":"function","location":"composition/containers/#ROSNode.container","page":"Containers & Dynamic Composition","text":"container([name=\"container\"]; namespace=nothing, peers=String[],\n          localhost_only=false, intra_process=true, block=true) do c\n    add!(c, K; name=…, overrides=…, managed=…)\n    …\nend -> Container\n\nCompose nodes into one process. Opens a single Context — one shared Zenoh session, discovery, type registry, and Julia scheduler — runs f(c) to populate it via add!, then parks on spin until the Context drains. The same node code runs standalone under run or composed here; only the deploy-time wiring differs.\n\nReturns the live Container. Behavior depends on block:\n\nblock=true (default) — spin handles signals for the duration (interactively, the first Ctrl-C drains gracefully and a second forces exit; deployed, SIGINT and SIGTERM exit and drain via an atexit hook), and the call returns once the Context has drained, closing it.\nblock=false — the call returns the running Container immediately, leaving you to drive it and close(c) it; the Context stays open until then.\nAn exception during f(c) closes the Context and propagates.\n\nThe container is itself a node named name (optionally under namespace) exposing the ~/_container/{load_node,unload_node,list_nodes} services, so a running container accepts ros2 component load/unload/list and the ComposableNodeContainer launch action.\n\nintra_process=true (default) enables the intra-process short-circuit (same-Context publisher↔subscriber pairs bypass serialize/Zenoh/decode) through set_intra_process!, a process-global switch.\n\ncontainer(\"vision\") do c\n    add!(c, Perception; name = \"perception\", overrides = (fps = 60,))\n    add!(c, Recorder;   name = \"recorder\")\nend\n\n\n\n\n\n","title":"ROSNode.container"},{"category":"function","location":"composition/containers/#ROSNode.add!","page":"Containers & Dynamic Composition","text":"add!(c::Container, K; name=…, namespace=nothing, overrides=(;), managed=false,\n     autostart=!managed) -> ComponentNode\n\nInstantiate node kind K — a component Type promoted to a node (un-prefixed parameters) or a NodeSchema composition (member-prefixed parameters) — on container c's shared Context, and track it under a container-unique id so it appears in list_nodes / ros2 component list and can be unload_node-ed. Returns the live ComponentNode. name defaults to the kind's name (a component's snake-cased type name).\n\nForwards to run(K; ctx = c.ctx, block = false, kwargs...): it constructs the components in dependency order, wires the parameter services, and brings the node up. Unmanaged (the default), the node autostarts (configure → activate) during this call; managed=true declares the lifecycle control surface and the node starts Unconfigured unless autostart=true, leaving an external orchestrator (ros2 lifecycle) to drive it. overrides is a NamedTuple of parameter values keyed by un-prefixed name (fps), or by prefixed var\"member.field\" to target one member when two members share a field name.\n\nThese keywords are rejected with an ArgumentError, each fixed by the container:\n\nctx — pinned to c.ctx; a foreign ctx would build a node the container tracks but whose Context it never closes.\nblock — pinned to false; block=true would park add! on spin.\npeers, localhost_only — transport is fixed when container opens the Context, so run's values would have no effect here.\n\ncontainer(\"fleet\") do c\n    vehicle = add!(c, Vehicle; name = \"vehicle\", overrides = (min_battery = 90.0,))\n    add!(c, GroundStation; name = \"ground\", managed = true)\nend\n\n\n\n\n\n","title":"ROSNode.add!"},{"category":"function","location":"composition/containers/#ROSNode.register_node_kind!","page":"Containers & Dynamic Composition","text":"register_node_kind!(name::AbstractString, K) -> K\n\nRegister node kind K under name in the process-global kind registry, returning K. This is the rclcpp_components_register_nodes analog: it makes a kind instantiable by name, so a container's load_node (and the ~/_container/load_node service behind ros2 component load) can build it from a load request.\n\nK is a NodeSchema (from node(…)) or a component Type used directly as a node. node(…; name=…)/@register_nodes register a kind via the dual path that survives precompilation: immediately at module-body evaluation in the REPL/script/Main case, and through the module's load hook (ros_init!) for a precompiled package. The call is thread-safe (guarded by the registry lock), and a repeated name is last-writer-wins.\n\n\n\n\n\n","title":"ROSNode.register_node_kind!"},{"category":"function","location":"composition/containers/#ROSNode.register_node_kinds!","page":"Containers & Dynamic Composition","text":"register_node_kinds!(schema, …) -> nothing\n\nEagerly register one or more named functor node-kind schemas in the process-global registry (keyed by each schema's .name), making them loadable by name (load_node / ros2 component load). Call this from a consumer's __init__ when you bring your own __init__ (it runs at package LOAD, so the registration survives precompilation). For the common case use @register_nodes, which also auto-installs __init__.\n\n\n\n\n\n","title":"ROSNode.register_node_kinds!"},{"category":"macro","location":"composition/containers/#ROSNode.@register_nodes","page":"Containers & Dynamic Composition","text":"@register_nodes Rig Counter …\n\nExpose functor node-kind schemas for load-by-name (ros2 component load / a container's load_node). Pushes each onto the module's __node_kinds__ roster (drained at load by ros_init!, so the registration survives precompilation) and installs __init__() = ros_init!(@__MODULE__) iff the module has none. If you bring your own __init__, this still rosters the kinds — call ROSNode.ros_init!(@__MODULE__) inside it (the same hook that absorbs @ros_message/@service types), or register_node_kinds!(Rig, Counter) directly.\n\n\n\n\n\n","title":"ROSNode.@register_nodes"},{"category":"function","location":"composition/containers/#ROSNode.ros_init!","page":"Containers & Dynamic Composition","text":"ros_init!(mod::Module) -> nothing\n\nROSNode's module-load initialization — the single hook the framework's macros install as __init__. Idempotent and safe to call repeatedly: it drains the module's authored types (@ros_message/@ros_service/@ros_action) and registers every node-kind schema on the module's __node_kinds__ roster (the @register_nodes list) by name.\n\n@register_nodes/the authored macros auto-install __init__() = ros_init!(@__MODULE__) only when the module has no __init__ of its own. If you bring your own __init__, call this from it so ROSNode keeps initializing:\n\nfunction __init__()\n    ROSNode.ros_init!(@__MODULE__)   # keep ROSNode's load-time setup running\n    my_own_setup()\nend\n\nSkipping it in a precompiled package leaves authored types unresolved and node-kind schemas unregistered, so load_node-by-name and dynamic wire-type resolution fail; a direct run(MyComponent) / run(schema) still works (it needs no registry).\n\n\n\n\n\n","title":"ROSNode.ros_init!"},{"category":"function","location":"composition/containers/#ROSNode.node_kind","page":"Containers & Dynamic Composition","text":"node_kind(name::AbstractString) -> Union{NodeSchema, Type, Nothing}\n\nThe node kind registered under name — the NodeSchema or component Type passed to register_node_kind! — or nothing when no kind carries that name. Thread-safe (guarded by the registry lock).\n\nLookup proceeds in two steps:\n\nthe exact registered name;\na container's load path additionally tolerates a namespaced spelling (my_pkg::Talker, pkg/Talker, pkg.Talker) by retrying on the last segment after ::, /, or ..\n\n\n\n\n\n","title":"ROSNode.node_kind"},{"category":"function","location":"composition/containers/#ROSNode.node_kinds","page":"Containers & Dynamic Composition","text":"node_kinds() -> Vector{String}\n\nThe names of every registered node kind, sorted ascending. Each name resolves through node_kind and is loadable by a container's load_node / the ~/_container/load_node service. Thread-safe (guarded by the registry lock).\n\n\n\n\n\n","title":"ROSNode.node_kinds"},{"category":"function","location":"composition/containers/#ROSNode.load_node","page":"Containers & Dynamic Composition","text":"load_node(c::Container, package_name, plugin_name;\n          name=\"\", namespace=\"\", parameters=(;)) -> (ComponentNode, unique_id::UInt64)\n\nInstantiate the registered kind named plugin_name on container c, returning the live ComponentNode and the container-unique id it is tracked under. This is the programmatic form of ros2 component load; the ~/_container/load_node service (composition_interfaces/srv/LoadNode) drives the same path from the wire.\n\nResolution matches plugin_name against the process-global registry in order:\n\nplugin_name verbatim;\nits last ::///.-separated segment.\n\npackage_name is advisory and does not shard the registry.\n\nEach keyword defaults from the kind and Context; pass either to override:\n\nname — the kind's name (the node(…; name=…) name, or the component type's snake-cased name);\nnamespace — the Context's namespace.\n\nparameters is a run overrides NamedTuple, taking two key forms:\n\nKey form Scope Example\nun-prefixed every member that declares it fps\nmember-prefixed one member, winning over the un-prefixed form var\"camera.fps\"\n\nThe prefix is the member's name within the node — a node(…) member's declared name, or the snake-cased component type for a component loaded directly as a node — independent of the node instance name.\n\nThe node is built by run(K; ctx = c.ctx, block = false, …) on the container's shared Context and autostarts (configure, then activate) like a standalone unmanaged node, then is registered for list_nodes / unload_node under a fresh 1-based id.\n\nErrors:\n\nArgumentError — no kind is registered under plugin_name;\nassembly errors propagated from the underlying run — a wire-name clobber, unsatisfied or ambiguous DI, or a construct/configure exception.\n\nThe wire ~/_container/load_node service catches both categories and returns them as a success = false response instead.\n\ncontainer(\"vision\"; block = false) do c\n    cn, uid = load_node(c, \"my_pkg\", \"Camera\"; name = \"front\",\n                        parameters = (; var\"camera.fps\" = 30))\nend\n\n\n\n\n\n","title":"ROSNode.load_node"},{"category":"function","location":"composition/containers/#ROSNode.unload_node","page":"Containers & Dynamic Composition","text":"unload_node(c::Container, unique_id::Integer) -> Bool\n\nClose and forget the loaded node tracked under unique_id on container c:\n\ntrue — a node carried that id;\nfalse — no node carried that id.\n\nThis is the programmatic form of ros2 component unload; the ~/_container/unload_node service (composition_interfaces/srv/UnloadNode) drives the same path.\n\nTeardown runs in three phases:\n\nthe id is popped from the container's loaded set and node list inside one critical section, so two concurrent unloads of the same id cannot both claim it (and double-close);\nclose happens outside the lock, running each member's cleanup in reverse dependency order and tearing down the node's entities, driving a managed node through its LifecycleNode shutdown;\nclose is idempotent, so a later Context drain that also closes the node is a no-op.\n\n\n\n\n\n","title":"ROSNode.unload_node"},{"category":"function","location":"composition/containers/#ROSNode.list_nodes","page":"Containers & Dynamic Composition","text":"list_nodes(c::Container) -> Vector{Tuple{UInt64, String}}\n\nThe nodes loaded on container c as (unique_id, full_node_name) pairs, ascending by id. This is the programmatic form of ros2 component list; the ~/_container/list_nodes service (composition_interfaces/srv/ListNodes) returns the same pairs split into the response's parallel unique_ids / full_node_names arrays. full_node_name is the node's fully-qualified name (namespace/name). Thread-safe (snapshot taken under the container lock).\n\n\n\n\n\n","title":"ROSNode.list_nodes"},{"category":"function","location":"composition/containers/#ROSNode.message_type","page":"Containers & Dynamic Composition","text":"message_type(d) — the ROS message type a publishes/hears port carries.\n\n\n\n\n\n","title":"ROSNode.message_type"},{"category":"function","location":"composition/containers/#ROSNode.port_name","page":"Containers & Dynamic Composition","text":"port_name(d) — a port descriptor's name (the key in the member's entities NamedTuple).\n\n\n\n\n\n","title":"ROSNode.port_name"},{"category":"function","location":"composition/containers/#ROSNode.port_names","page":"Containers & Dynamic Composition","text":"port_names(ms) — the declared port names of a MemberSchema (member_schema(S)), in order.\n\n\n\n\n\n","title":"ROSNode.port_names"},{"category":"function","location":"composition/containers/#ROSNode.param_names","page":"Containers & Dynamic Composition","text":"param_names(ms) — the parameter field names of a MemberSchema's @parameters schema.\n\n\n\n\n\n","title":"ROSNode.param_names"},{"category":"function","location":"composition/containers/#ROSNode.port","page":"Containers & Dynamic Composition","text":"port(ms, name) — the descriptor for port name in a MemberSchema, or nothing.\n\n\n\n\n\n","title":"ROSNode.port"},{"category":"function","location":"composition/containers/#ROSNode.member_names","page":"Containers & Dynamic Composition","text":"member_names(s) — the member names of a NodeSchema (declared order).\n\n\n\n\n\n","title":"ROSNode.member_names"},{"category":"section","location":"advanced/discovery/#Runtime-Type-Discovery","page":"Runtime Type Discovery","text":"A Subscription created without a type argument — a dynamic subscription — resolves each sample's message type at runtime, so a single subscriber handles a stream whose definition it learns over the wire. This powers generic tooling — recorders, bridges, introspection — over types the tool never imported.","title":"Runtime Type Discovery"},{"category":"section","location":"advanced/discovery/#Resolving-a-type-off-the-wire","page":"Runtime Type Discovery","text":"A Subscription written without a type argument resolves each sample's type as it arrives:\n\nsub = Subscription(node, \"/chatter\") do msg\n    @info \"heard\" typeof(msg.data) msg.data\nend\n\nmsg is the real decoded struct with type-stable field access, so msg.data is fast and concrete. The framework resolves the wire type in this order, taking the first that hits:\n\nthe home table (the calling module's imported structs)\nthe type registry\nthe project cache\nament\na wire GetTypeDescription query to the publisher\n\nThe home table is the entry point. @context binds the calling module as the Context's resolution home (see The Runtime Model), and a wire type resolves against that module's imported structs first. A module that has imported Reading lands its Reading deterministically; the later stages cover types the home has never seen.","title":"Resolving a type off the wire"},{"category":"section","location":"advanced/discovery/#Caching-discoveries-across-runs","page":"Runtime Type Discovery","text":"@ros_cache, written once in a module, opts into project-local persistence:\n\n@ros_cache\n\nIt persists discoveries project-locally so the next run starts from the cached definition:\n\nstores each discovered type description under <project>/ros_typesupport/, keyed by RIHS01\nrecords a per-node warm-up manifest under ros_typesupport/manifests/\nbakes wire-discovered types into static ones at precompile\n\nDiscovery works without it; @ros_cache is what makes a discovery stick.","title":"Caching discoveries across runs"},{"category":"section","location":"advanced/discovery/#Warm-up","page":"Runtime Type Discovery","text":"The first sample of a new type pays codegen and JIT for the decode→handler chain, a latency spike mid-stream. Warm-up drives that cost off the hot path.\n\nwarmup = :precompile compiles the decode→handler chain. The node default is :off, so opt in either on the node (Node(...; warmup = :precompile)) or per-subscription (see the recorder example below, where the Subscription is created with warmup = :precompile). With @ros_cache recording the manifest, a repeat run pre-warms it at startup:\n\nRun 1 discovers the type and warms it on first sight.\nRun 2+ warms at startup from the manifest, so the first sample arrives hot.\n\nwarmup = :execute additionally runs the handler once on a fabricated sample. A dynamic subscription degrades it to :precompile and stays compile-only: synthesizing a sample needs the static type that a discovered handler resolves at runtime.\n\nwarmup_sync = true blocks construction until the codec is warm — the choice for hard real-time, where the first sample must already be hot.\n\n@effectful expr marks a genuine side effect. An :execute warm-up run skips the marked expression while still compiling it, so warming a handler with :execute records no fabricated data. On a dynamic subscription, which stays compile-only, the guard is inert; marking effects this way keeps them safe the moment a static subscription enables :execute.","title":"Warm-up"},{"category":"section","location":"advanced/discovery/#A-recorder","page":"Runtime Type Discovery","text":"A generic recorder buffers readings off /readings without importing the type. The publisher half stands in for some other node on the graph, publishing sensor_demo/msg/Reading; split it into a second process (or a real ROS 2 node) and the subscriber discovers the type over the wire with the same code. This example ships runnable as examples/warming.jl. Before running:\n\nrun it from the ROSNode project so --project=. activates the ROSNode environment\nthe from = \"interfaces\" root resolves against the script's directory, examples/interfaces/\nstart a router first so both processes meet:\n\nzenohd -l tcp/localhost:7447 &\njulia --project=. examples/warming.jl   # then run it again to see the startup warm\n\nusing ROSNode\n\n@ros_cache\n\n@ros_import from = \"interfaces\" \"sensor_demo/msg/Reading\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"recorder\")\n\n    buffer = Any[]\n    sub = Subscription(node, \"/readings\"; warmup = :precompile) do msg\n        @effectful push!(buffer, (msg.sensor_id, msg.value))\n        @info \"recorded\" sensor = msg.sensor_id value = msg.value n = length(buffer)\n    end\n\n    pub = Publisher(node, \"/readings\", sensor_demo.msg.Reading)\n    sleep(0.4)   # let discovery match sub ↔ pub before the first publish\n    for i in 1:5\n        publish(pub, sensor_demo.msg.Reading(\n            sensor_id = \"imu_$i\", value = 9.8 + i, history = Float64[i, i + 0.5]))\n        sleep(0.3)\n    end\n    sleep(0.5)\n    close(sub); close(pub)\nend\n\nWith @ros_import present, Run 1 resolves Reading from the home table and warms its codec on first sight, recording the type into ros_typesupport/manifests/_recorder.tsv. Drop the @ros_import and split the publisher into a second process, and Run 1 instead fetches Reading over the wire on first sight. Either way, Run 2 replays that manifest at startup and warms the codec, so the first publish lands on a hot path.","title":"A recorder"},{"category":"section","location":"advanced/discovery/#Graduating-to-a-static-type","page":"Runtime Type Discovery","text":"Once the framework resolves a topic's type, it logs a one-time hint naming the discovered type and the static spelling to graduate to:\n\nsub = Subscription(node, \"/readings\", Reading) do msg\n    @info \"recorded\" sensor = msg.sensor_id value = msg.value\nend\n\nNaming the type drops the per-message invokelatest hop and unlocks the zero-copy view = true fast path (see Message Delivery).","title":"Graduating to a static type"},{"category":"section","location":"advanced/discovery/#See-also","page":"Runtime Type Discovery","text":"Topics — publishers and subscribers end to end.\nMessage Delivery — view modes and concurrency for a typed subscription.","title":"See also"},{"category":"section","location":"advanced/discovery/#API-reference","page":"Runtime Type Discovery","text":"","title":"API reference"},{"category":"section","location":"advanced/discovery/#Static-generation-and-caching","page":"Runtime Type Discovery","text":"","title":"Static generation and caching"},{"category":"section","location":"advanced/discovery/#Warm-up-2","page":"Runtime Type Discovery","text":"","title":"Warm-up"},{"category":"section","location":"advanced/discovery/#Type-description-and-introspection","page":"Runtime Type Discovery","text":"","title":"Type description and introspection"},{"category":"section","location":"advanced/discovery/#Type-support-and-resolution","page":"Runtime Type Discovery","text":"","title":"Type support and resolution"},{"category":"section","location":"advanced/discovery/#Logging","page":"Runtime Type Discovery","text":"","title":"Logging"},{"category":"macro","location":"advanced/discovery/#ROSNode.@ros_import","page":"Runtime Type Discovery","text":"@ros_import \"pkg\" | \"pkg/qual/Name\" ...\n@ros_import \"pkg/qual/Name\" as Alias\n@ros_import from=\"dir\" \"pkg/qual/Name\" ...\n\nStatically generate ROS 2 interface types (.msg/.srv/.action) resolved by name into the calling module — the static counterpart to runtime discovery. Each argument names either a package (\"sensor_msgs\", all of its interfaces) or a fully-qualified type (\"sensor_msgs/msg/Imu\"). The transitive reference closure (every referenced type, e.g. std_msgs/Header) is pulled in automatically so generation is self-contained.\n\nNames resolve against, in order, any from= roots, then ROSNode's vendored interface dir, then AMENT_PREFIX_PATH (an installed, sourced ROS 2 environment). A name that resolves to no source — explicit or transitively referenced — is warned and skipped, so the rest still generates; the macro errors when nothing resolves at all, naming the search roots it tried. Generation runs at macro expansion / precompile time: the types land at <pkg>.<qual>.<Name> and bake into the module image, so a loaded module starts with them already compiled.\n\nBinding\n\nResolution mirrors Julia's import, with one binding per argument form:\n\nArgument form Bound name(s)\nFully-qualified message (\"sensor_msgs/msg/Image\") the bare leaf struct Image (and sensor_msgs.msg.Image), like import Mod: Name\nFully-qualified service or action its request/goal namespace module (Name.Request, Name.Goal)\nBare package (\"sensor_msgs\") the module sensor_msgs, used as sensor_msgs.msg.X, like import Mod\nTransitively-pulled dependency reachable only by its pkg.qual.Name path\n\nKeeping transitive deps off the leaf namespace confines bare names to the types you named. Naming two types with the same leaf in one call errors, pointing at as.\n\n… as Alias binds the imported message type to Alias directly — for a short handle, or to disambiguate two same-named but different-RIHS01 versions. The as form accepts only a fully-qualified message type (\"pkg/msg/Name\"); a service or action has no single struct to bind and errors. The aliased type binds by origin:\n\nA generated as type is isolated in a hidden submodule, so its pkg.qual.Name path cannot collide with another import.\nA provided as type binds straight through.\n\nRegistration\n\nGenerated types are auto-registered with their real RIHS01 hash when a Context is created, so a keyexpr-only Subscription(node, \"/t\") decodes into the precompiled type with no runtime codegen, and the ~/get_type_description server can answer for them. Resolution reads ament/vendored sources — the durable source — directly, so there is no JSON cache to keep in sync (contrast @ros_cache).\n\nSingle-copy aliasing\n\nAny type whose (name, RIHS01) is already provided by a loaded module — the vendored canonical copy, or one a dependency already @ros_imported (see provided_type) — is aliased to that single struct, so one wire type maps to one Julia struct. Transitively-pulled commons are shared, a repeated import re-aliases the same struct, and a type a package C imports is reused by downstream packages A and B (provided C is loaded when they expand the macro). Actions always generate, since their implicit protocol types are not provided.\n\nBYO interfaces (from=)\n\nPass from=\"dir\" (or from=[\"a\", \"b\"]) to add local search roots for your own interface files, then reference them by name like any other type. They go through the same registration pipeline (RIHS01, single-copy aliasing, the ~/get_type_description server) that ament/vendored types do. A root is a parent-of-packages dir laid out the ROS way, <root>/<pkg>/<qual>/<Name>.<ext> (so interfaces/robot_msgs/msg/Widget.msg is @ros_import from=\"interfaces\" \"robot_msgs/msg/Widget\"). A from root precedes both vendored and ament in the search order, so it can supply types a sourced environment lacks or shadow one it has. Relative roots resolve against the source file, like @ros_msg. The roots' resolved files are registered as precompile dependencies — and a bare-package import registers the package dir — so editing a file, or adding one to an imported package, re-triggers generation on the next load.\n\nThe from= value must be a string or vector-of-strings literal; a non-literal errors, because roots are resolved at macro-expansion time.\n\n@ros_import \"sensor_msgs/msg/Imu\"            # binds `Imu`\n@ros_import \"std_msgs\"                       # binds `std_msgs`; use std_msgs.msg.Header\n@ros_import \"geometry_msgs/msg/Twist\" as Cmd\n@ros_import from=\"interfaces\" \"robot_msgs/msg/Widget\"\n\n\n\n\n\n","title":"ROSNode.@ros_import"},{"category":"macro","location":"advanced/discovery/#ROSNode.@ros_cache","page":"Runtime Type Discovery","text":"@ros_cache\n@ros_cache \"dir\"\n\nOpt a project into persistence plus static baking of types learned by runtime discovery. Written once in a module, it makes a discovered wire type stick: the next load starts from the baked static type, already compiled.\n\nThe macro splits its work across two phases.\n\nAt macro expansion / precompile time it reads the project cache — the .json TypeDescription blobs under dir (default <project>/ros_typesupport, in the active project's directory) — generates static types for each into the calling module, and registers the cache directory and every blob as precompile dependencies. The directory is created on the spot, so the dependency is tracked from the first run; a cache grown during a run therefore invalidates precompilation and bakes the new types on the next load. The cached types also join the module's resolution table, so a keyexpr-only subscription decodes into the precompiled struct with no runtime codegen.\n\nAt module load the baked registration flushes the types and the cache directory into ROSNode's static-type singleton (absorb_static_types!, via a generated __init__, or at eval for a script/REPL module). The first Context creation then registers the types under their real RIHS01 and enables project-local persistence: types discovered off the wire from that point on are written back to the cache for next time. Discovery itself works without the macro; @ros_cache is what makes a discovery durable.\n\nAcross repeated runs this converges a deployment toward fast warm-up: as discovery saturates, more types arrive already baked and fewer need a wire GetTypeDescription round-trip.\n\nThe optional argument is a string-literal directory; anything else errors. An explicit directory is baked verbatim; the default form bakes a relocatable marker recomputed against the active project at load time. A project may carry several @ros_cache directories — reads search all of them, and new discoveries persist to the lexicographically smallest.\n\nmodule RobotApp\nusing ROSNode\n@ros_cache            # persist under <project>/ros_typesupport\nend\n\n\n\n\n\n","title":"ROSNode.@ros_cache"},{"category":"function","location":"advanced/discovery/#ROSNode.flush_type_cache","page":"Runtime Type Discovery","text":"flush_type_cache(to; dir = <all cache dirs>, format = :msg) -> Vector{String}\n\nGraduate the project-local discovered-type cache out to folder to, producing a durable, user-owned export of the types learned by running against the live graph — suitable for checking into a project. Returns the paths written.\n\nEach .json blob under dir — a single directory or an iterable of them — is loaded, re-hashed from its content to recover the type's RIHS01 identity, and written in format:\n\n:msg (default) — ROS interface text in a colcon-buildable package layout (<to>/<pkg>/<qual>/<Name>.<ext>).\n:julia — precompilable .jl source, moving the once-dynamic type onto the static fast path.\n:typedesc — the raw wire TypeDescription JSON blob, language-agnostic.\n\ndir defaults to the full read search-list, in priority order:\n\nthe $ROS_TYPESUPPORT_CACHE override;\nelse every registered @ros_cache dir;\nelse the project default.\n\nSo a multi-cache project's blobs all graduate, wherever discovery persisted them. A type cached under several dirs exports once, from the first dir in search order.\n\nEach failure is isolated so the rest of the run stays intact:\n\nA nonexistent dir is warned and skipped.\nAn unparseable blob is skipped.\nA per-type export failure is logged and skipped.\nWhen no dir holds a readable blob, the result is an empty vector.\n\nThe exported identity is whatever the blob's content hashes to — the content-address revalidation that guards the discovery read path does not run here.\n\n\n\n\n\n","title":"ROSNode.flush_type_cache"},{"category":"macro","location":"advanced/discovery/#ROSNode.@effectful","page":"Runtime Type Discovery","text":"@effectful expr\n\nRun expr for its side effects on the live path, skipping it while the framework is warming the handler (is_warming). The skipped branch still compiles — the guard is a runtime value, so both arms codegen — so warm-up exercises the exact specialization the real run will use. Returns expr's value at runtime, nothing while warming. When the warm-up path itself needs a usable value, write is_warming() ? stub : real directly.\n\nWrap a non-ROS effect (hardware I/O, external calls) so it runs only on the live path; ROS effects like publish/call are null-routed by the framework during warm-up and need no wrapping.\n\nSubscription(node, \"/cmd\", Twist) do msg\n    cmd = plan(msg)                  # compiled either way\n    @effectful arm_thrusters!(cmd)   # non-ROS effect: skipped while warming, still compiled\n    publish(throttle_pub, derive(cmd))   # ROS effect: framework null-routes during warm-up\nend\n\n\n\n\n\n","title":"ROSNode.@effectful"},{"category":"function","location":"advanced/discovery/#ROSNode.is_warming","page":"Runtime Type Discovery","text":"is_warming() -> Bool\n\ntrue while the framework is running a handler purely to warm its code path — either :execute warm-up (the _WARMUP scope) or package precompilation (jl_generating_output). Lets handler code skip real side effects while the framework still compiles the handler at full native depth. Use @effectful for the common skip-this-effect-while-warming pattern.\n\nReturns a plain runtime Bool by design: both branches of a guard compile, and only the live one runs, so warm-up exercises the stub yet still codegens the real branch. Must stay non-foldable (no @pure/:foldable), or a branch is DCE'd and warm-up compiles the wrong specialization.\n\n\n\n\n\n","title":"ROSNode.is_warming"},{"category":"function","location":"advanced/discovery/#ROSNode.describe_type","page":"Runtime Type Discovery","text":"describe_type(ctx_or_node, type_name, type_hash) -> Union{TypeDescriptionMsg, Nothing}\n\nLook up the type registry (TypeRegistry) for (type_name, type_hash) and return the canonical internal TypeDescriptionMsg (the main type plus its referenced-type closure) that ~/get_type_description serves, or nothing when no entry with that exact name and version is registered. This is the server-side half of rmw_zenoh's dynamic type discovery: a peer that sees a (name, RIHS01) on the graph but lacks the type fetches its description here.\n\ntype_hash selects the version:\n\n\"RIHS01_<64-hex>\" — selects that exact version.\nempty string — matches any registered version of type_name (the \"I have the name, send me whatever you advertise\" form), resolving to the first matching entry under the registry lock.\nmalformed RIHS string — resolves to nothing (treated as not-found).\n\nThe served description depends on the matched RegistryEntry:\n\nentry storing a td — served from it directly, its closure sorted by type_name so the served description re-hashes to the RIHS01 advertised on the graph (the integrity invariant a receiving peer re-checks).\nIL-only entry (ament/static) — rebuilt: the main type from the struct AST, the closure collected from the registry by name, IL-only referenced types rebuilt the same way.\na referenced type that cannot be resolved from the registry — throws an ArgumentError, since a truncated closure would fail the receiving peer's integrity gate anyway.\n\nwire_get_type_description! converts the result to the wire form via to_wire_td; a nothing result becomes a successful=false reply.\n\n\n\n\n\n","title":"ROSNode.describe_type"},{"category":"function","location":"advanced/discovery/#ROSNode.get_type_description","page":"Runtime Type Discovery","text":"get_type_description(client, type_name; type_hash=\"\", include_sources=false,\n                     timeout_ms=2000) -> GetTypeDescription_Response\n\nClient side of ~/get_type_description: ask a remote node for the TypeDescription of (type_name, type_hash) (rmwzenoh dynamic type discovery). Blocks the calling task on the reply exactly like call — it yields while the request is in flight — and returns the decoded `GetTypeDescriptionResponse, whosesuccessfulflag distinguishes \"the remote has this type\" from \"it doesn't\". Raises [ServiceError](@ref) on an error reply, or when no reply arrives withintimeoutms(the wait bound passed tocall;timeoutms=0` waits unbounded).\n\nclient is a ServiceClient built over the statically compiled GetTypeDescription service type and pointed at the remote's ~/get_type_description. type_hash is the RIHS01 string (empty matches any advertised version); include_sources requests the regenerated interface text (type_sources) alongside the description. The higher-level fetch_type_description wraps this call with the verify-and-register landing into the node's registry.\n\n\n\n\n\n","title":"ROSNode.get_type_description"},{"category":"function","location":"advanced/discovery/#ROSNode.fetch_type_description","page":"Runtime Type Discovery","text":"fetch_type_description(node, remote, type_name, hash; timeout_ms=2000) -> Union{RegistryEntry, Nothing}\n\nThe wire step of rmwzenoh dynamic type discovery: call `<remote>/gettypedescriptionfor(typename, hash), then on a successful reply verify, lift, and register the result into the node's type registry (viaregistertypedescription!, which also persists it to the content-addressed cache for the next run). Returns the registered [RegistryEntry](@ref), ornothing` in any of:\n\nthe remote lacks the type;\nno server answered;\nthe reply failed the RIHS01 integrity gate.\n\nBlocks the calling task. First it waits up to timeout_ms for the remote's service to become routable (wait_for_service) — discovery is eventually consistent, so a query issued before the queryable is matched would silently match nothing — then issues the get_type_description call under the same budget. remote is the serving node's fully qualified name (e.g. \"/talker\"); hash is a TypeHash or a RIHS string. The client is closed before return.\n\nError and return outcomes:\n\nmalformed hash string — returns nothing immediately.\nShutdownException — propagates.\nevery other error (timeout, decode failure, hash mismatch from the gate) — caught and reported as nothing.\n\nChoosing which source to try — registry, then cache, then ament, then this wire path — is resolve_or_discover's job; this function is only the wire leg.\n\n\n\n\n\n","title":"ROSNode.fetch_type_description"},{"category":"function","location":"advanced/discovery/#ROSNode.resolve_or_discover","page":"Runtime Type Discovery","text":"resolve_or_discover(node, name, hash; ament=true, cache=true, wire=true,\n                    timeout_ms=2000) -> Union{Type, Nothing}\n\nResolve (name, hash) to a realized (generated) Julia type, trying each source in order and short-circuiting on the first hit:\n\nregistry — a type already realized this run.\nproject cache — a prior run's persisted .json blob (opt-in, @ros_cache).\nament — an installed .msg/.srv when inside a sourced ROS 2 environment.\nwire — call the advertising remote's ~/get_type_description (fetch_type_description), verify, register, and persist for next run.\n\nReturns the generated type, or nothing when no source can resolve it. Steps 1–3 run in resolve_type; step 4 runs only when wire=true and steps 1–3 missed, and blocks the calling task on discovery and the remote call (bounded by timeout_ms). The wire step is taken against whichever node advertises (name, hash) on the graph, preferring a remote advertiser over a local one. ament/cache/wire switch off the corresponding source; hash is a TypeHash or RIHS string (a malformed string returns nothing).\n\nType-revision trust (the Context's weak_types): when a pinned (:static/:authored) type is registered under name with a different hash, that pin is enforced — the wire step is suppressed and the diverging peer revision is not bound (the mismatch is already reported by resolve_type). weak_types=true opts into the dynamic fallback: the wire step runs and binds the peer's revision.\n\nThe first sample of a type pays for discovery and codegen; subsequent samples are fast registry lookups, and across runs the cache (or a baked @ros_cache static type) skips re-discovery. The returned type lives in a newer world age than the compiled dispatch path, so callers reach it through Base.invokelatest.\n\n\n\n\n\n","title":"ROSNode.resolve_or_discover"},{"category":"function","location":"advanced/discovery/#ROSNode.wire_get_type_description!","page":"Runtime Type Discovery","text":"wire_get_type_description!(node) -> ServiceHandle\n\nDeclare the hidden ~/get_type_description service on node and start serving it. The handler answers from the Context's TypeRegistry via describe_type, converting the canonical internal TypeDescriptionMsg to the wire type_description_interfaces/srv/GetTypeDescription form (to_wire_td). A request for an unknown (name, hash) gets a clean successful=false reply rather than an error. Every node serves this so peers can fetch the descriptions it advertises on the graph.\n\nReturns the ServiceHandle, tracked on node and reaped by close(node) like any other entity. The request's include_type_sources flag sets type_sources:\n\ntrue — carries one regenerated IL.unparse text per served type (via _type_sources).\nfalse — empty, since the description alone is enough to decode.\n\n\n\n\n\n","title":"ROSNode.wire_get_type_description!"},{"category":"type","location":"advanced/discovery/#ROSNode.TypeRegistry","page":"Runtime Type Discovery","text":"TypeRegistry\nTypeRegistry()\n\nThe per-Context type table that backs runtime type support. Keyed by (type_name, RIHS01 hash), so several evolved versions of one interface coexist under distinct keys — the hash (the ROS Interface Hashing Standard, REP 2011) is the decode-safety identity, not just a version tag (see RegistryEntry for the entry shape).\n\nThe Context owns the storage and the guarding lock; the typesupport layer fills and reads it. Values are RegistryEntry records, held as Any so the context layer stays free of the entry shape. Every node on a Context shares one registry, so a type discovered by one node is visible to all. Use register_type! to store and lookup_type to fetch; the zero-argument constructor builds an empty registry with a fresh ReentrantLock (the Context constructor does this for you).\n\n\n\n\n\n","title":"ROSNode.TypeRegistry"},{"category":"type","location":"advanced/discovery/#ROSNode.RegistryEntry","page":"Runtime Type Discovery","text":"RegistryEntry(info::TypeInfo, il; td=nothing, provenance::Symbol=:wire)\n\nOne record in the TypeRegistry, keyed there by (type_name, RIHS01-hash). Holds everything the runtime knows about a single ROS 2 interface type (https://docs.ros.org/en/rolling/Concepts/Basic/About-Interfaces.html):\n\ninfo::TypeInfo — the qualified name plus RIHS01 hash this entry answers for.\nil — the ROSMessages.IL (RMessage / RService / RAction), the hub form every consumer (codegen, hashing, persistence, the type-description server) reads from.\ntd::Union{TypeDescriptionMsg, Nothing} — the wire type description (main type plus its referenced closure), the source of truth for persistence and the ~/get_type_description service. Its content by provenance:\nprovenance td\n:wire / :cache the received blob\n:static / :authored the baked or reflected description it was generated from\n:ament nothing — parsed straight from installed .msg / .srv / .action text, so there is no wire description\nprovenance::Symbol — where the type came from: :static, :authored, :ament, :wire, or :cache.\nmod::Union{Module, Nothing} and type — the generated module and the concrete struct within it. They are bound by one of two paths:\ndynamic entry — nothing at construction, populated by realize! at first use.\nstatically-interned entry — already bound to the compiled type on arrival.\nCallers reach them via Base.invokelatest, since codegen runs in a newer world age than the compiled dispatcher.\n\nThe convenience constructor defers codegen (mod/type start nothing) and defaults provenance to :wire.\n\n\n\n\n\n","title":"ROSNode.RegistryEntry"},{"category":"function","location":"advanced/discovery/#ROSNode.register_type!","page":"Runtime Type Discovery","text":"register_type!(reg::TypeRegistry, info::TypeInfo, entry) -> entry\n\nStore entry (a RegistryEntry, opaque to the context layer) in reg under the key (info.name, info.hash), returning entry. The store overwrites any existing value at that exact (name, RIHS01-hash) key; a different hash version of the same name lands under a separate key and coexists. Takes the registry lock for the duration of the write, so concurrent registrations are serialized.\n\n\n\n\n\n","title":"ROSNode.register_type!"},{"category":"function","location":"advanced/discovery/#ROSNode.lookup_type","page":"Runtime Type Discovery","text":"lookup_type(reg::TypeRegistry, info::TypeInfo) -> Union{Any, Nothing}\n\nFetch the value stored under (info.name, info.hash), or nothing when that exact name-and-version pair has not been registered. A nothing result is the trigger to acquire the type (cache, ament, or dynamic over-the-wire discovery). The lookup takes the registry lock for the read; the match is exact on both name and RIHS01 hash, so a peer advertising a different version of the same type misses and drives a fresh acquisition. The registry stores values as Any, so the result comes back untyped — callers check it isa RegistryEntry before trusting it (a RegistryEntry is what the typesupport layer ever puts in).\n\n\n\n\n\n","title":"ROSNode.lookup_type"},{"category":"function","location":"advanced/discovery/#ROSNode.resolve_type","page":"Runtime Type Discovery","text":"resolve_type(node_or_ctx, info::TypeInfo; ament=true, cache=true) -> Union{RegistryEntry, Nothing}\n\nReturn the RegistryEntry for info, acquiring it from a local source when it is not yet registered. This is the local acquisition front door; it tries three sources in order:\n\na registry hit (already known to this Context);\nthe content-addressed on-disk cache, when cache=true — a cached blob is re-validated against info's RIHS01 and registered with :cache provenance;\nan ament/static lookup by name in the installed workspace, when ament=true (see load_ament_type).\n\nThe ament lookup is name-only, so its result is re-validated against info's RIHS01 before it is trusted (the same revalidation the cache path runs). A real-hash mismatch is rejected with a diagnostic and returns nothing so the caller wire-discovers the peer's actual revision; the all-zero placeholder hash (an uncomparable cross-package ref) is accepted with a @debug note. The diagnostic loudness tracks the Context's trust setting:\n\ndefault pinned trust — loud (@warn).\nweak_types=true — quiet (@debug).\n\nWhen the ament path fires the diagnostic, warned[] (if given) is set true, so a wire-suppression site owns the no-ament-file pinned-conflict case without double-warning.\n\nReturns nothing when none of the local sources resolves the type. The dynamic over-the-wire path (issuing a GetTypeDescription query) stays with the discovery layer, which holds the remote node identity and async ServiceClient; on a nothing result the caller either kicks off that discovery or delivers raw bytes for the topic.\n\nnode_or_ctx is a node or a Context; the entry's Context registry is reached through it. A resolved entry stays unrealized (codegen deferred) — call realize! or registered_type at first use to obtain its concrete type.\n\n\n\n\n\n","title":"ROSNode.resolve_type"},{"category":"function","location":"advanced/discovery/#ROSNode.absorb_static_types!","page":"Runtime Type Discovery","text":"absorb_static_types!(mod::Module) -> nothing\n\nFlush a module's baked @ros_import / @ros_cache declarations and authored types (@ros_message / @ros_service / @ros_action) into ROSNode's process-wide static-type singleton, so a later Context picks up the precompiled interface types and cache opt-ins. The macros emit a call to this at module load — from a generated __init__ in a precompiled package, or a top-level call at eval in a script/REPL module. Call it yourself only when your module defines its own __init__ (which the macros otherwise generate for you). Idempotent: entries are interned by type, so repeated absorbs of the same module are no-ops.\n\nIt drains three module-local globals when present:\n\n__ros_cache_dir__ — the @ros_cache opt-in directory.\n__ros_authored_types__ — types authored with @ros_message / @ros_service / @ros_action, interned by reflection and folded into the module's resolve table.\n__ros_static_types__ — each @ros_import type paired with its canonical TypeDescription JSON.\n\nEach carries a precompiled ROS 2 interface type and its type-description bytes; registration into a Context happens later, at Context creation. A per-type failure is logged and skipped so one bad declaration cannot abort the absorb. Returns nothing.\n\n\n\n\n\n","title":"ROSNode.absorb_static_types!"},{"category":"function","location":"advanced/discovery/#ROSNode.export_typesupport","page":"Runtime Type Discovery","text":"export_typesupport(node_or_ctx, names; to=pwd(), format=:msg) -> Vector{String}\n\nWrite registered (typically discovered) types out of the in-memory registry into durable, user-owned files, returning the paths written. names is a single type name or any iterable of names; format selects the layout (the ROS 2 interface concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Interfaces.html):\n\n:msg (default) / :julia_text — IL.unparse the type into ROS interface text at <to>/<package>/<qualifier>/<Name>.<ext> (.msg / .srv / .action per the IL kind). The most portable form: colcon build turns the tree into a real interface package. A discovered type's text carries exactly the field definitions RIHS01 covers, so it is wire-faithful; constants and defaults are absent (lift drops them), so it is lossy as documentation.\n:julia — emit Generation.generate_code's output as .jl source at <to>/<package>.jl (or <to>/<Name>.jl for a package-less name) to check in and include. The strongest port-forward: the once-dynamic type becomes static, precompilable, and back on the min-copy fast path. The file carries inline import StaticArrays, CDRSerialization so it reparses standalone, and emits the entry's referenced types alongside it (the same closure realize! generates from), so nested cross-package fields resolve on include.\n:typedesc — the raw wire TypeDescription JSON bundle at <to>/<Name>.typedesc.json; language-agnostic and reloadable by ROSNode.\n\nThrows ArgumentError in three cases:\n\nA requested name is not registered — discover or load_ament_type it first.\nAn unknown format is given.\n:typedesc is requested for an :ament-acquired entry — one parsed from local interface text, which carries no wire TypeDescription. The :wire, :cache, :static, and :authored entries all carry one and export as :typedesc fine.\n\nName resolution ignores the hash version — the first registry entry matching the name is exported.\n\n\n\n\n\n","title":"ROSNode.export_typesupport"},{"category":"function","location":"advanced/discovery/#ROSNode.enable_project_cache!","page":"Runtime Type Discovery","text":"enable_project_cache!(dir = \"<active-project-dir>/ros_typesupport\") -> String\n\nTurn on project-local persistence of dynamically-discovered type descriptions. Once enabled, discovery writes .json TypeDescription blobs keyed by RIHS01 under dir, and resolve_type reads a cached blob before the wire, re-validating each blob against its hash on load and discarding a stale one. Returns the directory (created if absent).\n\nPersistence is off by default — ROSNode is a library, and discovered types are deployment-specific, so they belong to the importing project. dir defaults to ros_typesupport under the active project's directory (the current working directory when no project is active).\n\nThree paths opt a project into persistence:\n\nenable_project_cache! — this call. Sets dir as both the single write directory and the sole read directory, replacing any previously registered set.\n@ros_cache macro — the declarative alternative: a project adds the macro to a module, and the absorbed cache directories are registered at Context creation, likewise replacing the registered set. The last registration wins, so call enable_project_cache! after Context() when its directory must win over macro-registered ones.\nROS_TYPESUPPORT_CACHE environment variable — force-enables persistence at its directory (an ops/test escape hatch) regardless of this call.\n\n\n\n\n\n","title":"ROSNode.enable_project_cache!"},{"category":"function","location":"advanced/discovery/#ROSNode.disable_project_cache!","page":"Runtime Type Discovery","text":"disable_project_cache!() -> nothing\n\nTurn off project-local type-description persistence and clear the registered cache directories, returning to the default (disabled) state. Discovery then resolves types from the registry, ament, and the wire only, writing nothing to disk.\n\nThe ROS_TYPESUPPORT_CACHE environment override keeps persistence force-enabled at its directory independently of this call, since _cache_enabled treats the env var as an unconditional opt-in.\n\n\n\n\n\n","title":"ROSNode.disable_project_cache!"},{"category":"function","location":"advanced/discovery/#ROSNode.ament_prefix_paths","page":"Runtime Type Discovery","text":"ament_prefix_paths() -> Vector{String}\n\nThe AMENT_PREFIX_PATH environment entries (colon-separated, ROS's install-prefix search path), filtered to existing directories. Returns an empty vector outside a sourced workspace. This is one component of the merged ament search order; search_prefixes returns the full, de-duplicated list.\n\n\n\n\n\n","title":"ROSNode.ament_prefix_paths"},{"category":"function","location":"advanced/discovery/#ROSNode.search_prefixes","page":"Runtime Type Discovery","text":"search_prefixes() -> Vector{String}\n\nThe ordered, de-duplicated install-prefix roots scanned for ament interface files, filtered to existing directories. Precedence (the overlay rule — the first occurrence of a directory wins):\n\nroots added via add_search_path! (user-registered),\nAMENT_PREFIX_PATH entries (a sourced workspace),\nCMAKE_PREFIX_PATH entries,\nthe /opt/ros/<distro> system installs for rolling, jazzy, kilted, lyrical, and humble.\n\nUnderlies discover_ament_packages and the by-name ament lookup behind load_ament_type.\n\n\n\n\n\n","title":"ROSNode.search_prefixes"},{"category":"function","location":"advanced/discovery/#ROSNode.add_search_path!","page":"Runtime Type Discovery","text":"add_search_path!(path) -> Vector{String}\n\nRegister path as an install-prefix search root for ament type acquisition, process-wide and at the highest precedence (ahead of AMENT_PREFIX_PATH). path should be an install prefix whose interface files live under <path>/share/<pkg>/{msg,srv,action}/, the same shape as an AMENT_PREFIX_PATH entry. The path is expanded (~), normalized to an absolute path, and de-duplicated against prior registrations. Returns a snapshot of the current user-registered search-path list.\n\nThe registration is global mutable process state, lock-guarded so it is safe to call concurrently with type resolution.\n\n\n\n\n\n","title":"ROSNode.add_search_path!"},{"category":"function","location":"advanced/discovery/#ROSNode.discover_ament_packages","page":"Runtime Type Discovery","text":"discover_ament_packages() -> Dict{String, Vector{String}}\n\nScan the search_prefixes install roots for installed ROS 2 interface packages, mapping each package name to the absolute paths of its .msg / .srv / .action files, grouped by qualifier (msg, then srv, then action) and name-sorted within each group. A package counts as an interface package when its share/<pkg> directory holds at least one such file.\n\nUnder the overlay rule the first prefix to define a package wins; later prefixes' copies of the same package are shadowed. Returns an empty Dict outside a sourced workspace.\n\n\n\n\n\n","title":"ROSNode.discover_ament_packages"},{"category":"function","location":"advanced/discovery/#ROSNode.load_ament_type","page":"Runtime Type Discovery","text":"load_ament_type(reg::TypeRegistry, name; register=true) -> Union{RegistryEntry, Nothing}\n\nResolve a fully-qualified ROS 2 type name (\"<pkg>/<qualifier>/<Name>\") against the installed ament workspace: locate its .msg / .srv / .action file across search_prefixes (overlay order, first match wins), parse it to IL, compute the RIHS01 hash from the parsed definition, and — with register=true (the default) — register the resulting RegistryEntry under (name, hash) with :ament provenance. Returns the entry, or nothing when the type is not installed.\n\nThe hash is computed locally from the parsed AST so an ament-acquired type keys identically to a wire-discovered one — RIHS01 is the shared identity. Three hash outcomes by interface shape:\n\nSelf-contained message type: the hash is exact.\nType that references other packages: keys correctly by name; its hash matches a remote's once its referenced siblings are co-registered, at which point the wire/cache path supplies the exact hash.\nInterface that lowers to no struct: the entry takes the RIHS01 placeholder hash (version 01, all-zero digest), correct for keyexpr structure, with the wire/cache path supplying the exact hash for cross-version matching.\n\nThe entry is left unrealized (codegen deferred); nested references resolve once the package's siblings are co-registered, the usual whole-package acquisition shape.\n\n\n\n\n\n","title":"ROSNode.load_ament_type"},{"category":"type","location":"advanced/discovery/#ROSNode.RosoutLogger","page":"Runtime Type Discovery","text":"RosoutLogger(node; name=node.fqn, parent=nothing, console=true,\n             min_level=Logging.BelowMinLevel)\n\nAn AbstractLogger that bridges Julia logging to ROS 2's /rosout. Per record it drives up to three sinks:\n\nwhen console=true — writes the ROS console line [LEVEL] [secs.nanosec] [name]: msg to stderr.\nalways — publishes an rcl_interfaces/msg/Log on node's shared /rosout publisher.\nwhen a parent logger is supplied — forwards the raw record to that extra sink (a file or Julia console).\n\nInstalling it routes a plain @info/@warn/ @error/@logmsg to ROS 2 logging; the request dispatcher installs the node's logger around every handler so handler logs reach /rosout automatically.\n\nname is the ROS logger name (the node FQN by default; logger(node, \"child\") makes a .-separated child sharing the same publisher and console). min_level is this logger's own floor; per-name overrides come from the node's level table (set_logger_level!) and take precedence by longest matching prefix. The Julia level Logging.Error maps to ROS 2 ERROR; use Fatal for ROS 2 FATAL.\n\nEach sink is best-effort and isolated: a failure in one (a bad show, a dead /rosout route) is reported to stderr and never breaks the others or escapes the handler. Installing the logger is always safe: the console and parent sinks work unconditionally, and the /rosout sink engages whenever the node has a live session to declare the publisher on.\n\nlg = RosoutLogger(node; parent = ConsoleLogger())\nwith_logger(lg) do\n    @info \"armed\"          # ROS console, /rosout, AND the Julia console\nend\n\n\n\n\n\n","title":"ROSNode.RosoutLogger"},{"category":"function","location":"advanced/discovery/#ROSNode.logger","page":"Runtime Type Discovery","text":"logger(node) -> RosoutLogger\nlogger(node, child::AbstractString) -> RosoutLogger\n\nThe node's default ROS logger, or a .-separated child of it. logger(node) returns the one logger cached on the node (name = node FQN), building it once on first call; logger(node, child) returns a fresh RosoutLogger named \"<fqn>.child\" that shares the node's /rosout publisher and console.\n\nThe request dispatcher installs logger(node) around every handler, so handlers rarely need this directly. Reach for it to build a child logger for sub-component hierarchy, or to log outside a handler via with_logger(node) do … end. The single-argument form uses double-checked locking so the per-message dispatch path stays lock-free once the logger is cached.\n\n\n\n\n\n","title":"ROSNode.logger"},{"category":"constant","location":"advanced/discovery/#ROSNode.Fatal","page":"Runtime Type Discovery","text":"Fatal\n\nA Julia LogLevel of 3000, one step above Logging.Error (2000), giving Julia code a way to request ROS 2's FATAL severity. A RosoutLogger maps @logmsg Fatal … to the rcl_interfaces/msg/Log FATAL constant (0x32) and labels the console line [FATAL]; a plain @error stays at ROS 2 ERROR.\n\nusing Logging\n@logmsg Fatal \"reactor breach\"\n\n\n\n\n\n","title":"ROSNode.Fatal"},{"category":"function","location":"advanced/discovery/#ROSNode.set_logger_level!","page":"Runtime Type Discovery","text":"set_logger_level!(node, level::LogLevel)              # the node's own logger (FQN)\nset_logger_level!(node, name::AbstractString, level::LogLevel)  # a specific logger name\n\nSet a per-logger minimum severity in the node's level table. A name entry gates that logger and every .-separated descendant that lacks its own entry — so set_logger_level!(node, \"/n\", Logging.Warn) also gates \"/n.child\" (longest matching prefix wins). The two-argument form targets the node's own logger (its FQN). This is the programmatic form of --ros-args --log-level name:=….\n\nReturns nothing. Levels are Julia LogLevels; a RosoutLogger consults this table on every shouldlog/min_enabled_level check.\n\n\n\n\n\n","title":"ROSNode.set_logger_level!"},{"category":"function","location":"advanced/discovery/#ROSNode.with_rosout","page":"Runtime Type Discovery","text":"with_rosout(f, node; name=node.fqn, parent=nothing, console=true,\n            min_level=Logging.BelowMinLevel)\n\nRun f() with a fresh RosoutLogger (built from the keyword arguments) installed as the active logger for the dynamic extent of the call. Returns whatever f() returns. Use this to override logger options — parent=, console=, min_level=, name= — for one scope; with_logger(node) do … end is the everyday form and reuses the node's cached logger.\n\nwith_rosout(node; parent = ConsoleLogger()) do\n    @info \"armed\"          # ROS console, /rosout, AND the Julia console\nend\n\n\n\n\n\n","title":"ROSNode.with_rosout"},{"category":"function","location":"advanced/discovery/#ROSNode.bridge_zenoh_logs!","page":"Runtime Type Discovery","text":"bridge_zenoh_logs!(node; min_severity=Zenoh.LogSeverities.WARN, capacity=256) -> Zenoh.LogStream\n\nCapture Zenoh's transport logs (session, transport, liveliness) at min_severity and above, and re-emit each one through Julia logging under a <fqn>.zenoh child logger, so they ride the same path to /rosout, the ROS console, and the node's level table. Returns the Zenoh.LogStream.\n\nOpt-in and off by default. It spawns a background task that drains the stream; the node's Context registers an on_shutdown hook that closes the stream on drain, which ends the iterator and lets the task exit cleanly. capacity bounds the stream's drop-oldest ring.\n\nZenoh's logger is process-global and one-shot: call this at most once per process, and never together with Zenoh.setup_logging. Keep min_severity at WARN (the default): the WARN floor keeps steady-state forwarding self-contained, since a /rosout publish's own data-plane DEBUG/TRACE records stay below it.\n\n\n\n\n\n","title":"ROSNode.bridge_zenoh_logs!"},{"category":"section","location":"getting-started/#Getting-Started","page":"Getting Started","text":"ROSNode brings ROS 2 publish/subscribe, services, and actions to Julia over Zenoh, mirroring rmw_zenoh on the wire. This page sets up the package, starts a router, and runs the bundled examples.","title":"Getting Started"},{"category":"section","location":"getting-started/#Adding-ROSNode","page":"Getting Started","text":"ROSNode is a member of this workspace. Import it once its project is active:\n\nusing ROSNode\n\nFrom the workspace root, run any example against the ROSNode member project:\n\njulia --project=ROSNode ROSNode/examples/publisher.jl","title":"Adding ROSNode"},{"category":"section","location":"getting-started/#Starting-a-router","page":"Getting Started","text":"ROSNode is built on Zenoh and mirrors rmw_zenoh, whose discovery is router-based. Start a router once, then point examples at it:\n\nzenohd -l tcp/localhost:7447 &\n\n@context takes three discovery knobs; see the Context docstring for the full catalog:\n\npeers = [\"tcp/localhost:7447\"] adds connect endpoints, dialing that router.\nlocalhost_only = true disables multicast scouting and defaults the connect endpoint to the loopback router. The default false reaches peers on other hosts.\ndomain_id selects the ROS 2 domain.\n\nOmit all three to discover from the environment.","title":"Starting a router"},{"category":"section","location":"getting-started/#Running-the-examples","page":"Getting Started","text":"Start a router, then run the two halves in separate Julia processes pointed at the same router:\n\nzenohd -l tcp/localhost:7447 &\n\njulia --project=ROSNode ROSNode/examples/publisher.jl     # in one terminal\njulia --project=ROSNode ROSNode/examples/subscriber.jl    # in another\n\nPublisher: emits a published @info record once a second, carrying msg.data = \"hello world N\".\nSubscriber: emits a matching heard record for each one as it arrives, carrying msg.data and its String type.\n\nExamples that bundle both halves (service.jl, action.jl, parameters.jl) take a role argument to split across two processes. Set the role with the ROLE env var or the first CLI arg; it defaults to both:\n\njulia --project=ROSNode ROSNode/examples/service.jl server    # serve only (Ctrl-C to stop)\njulia --project=ROSNode ROSNode/examples/service.jl client    # call only (needs a server)\n\nRun each with no argument to drive both halves in one self-contained process: point it at the router and go.","title":"Running the examples"},{"category":"section","location":"getting-started/#The-runtime-model","page":"Getting Started","text":"Every program opens a Context, groups entities under a Node, then spins. See The Runtime Model for the full lifecycle.","title":"The runtime model"},{"category":"section","location":"getting-started/#Interoperating-with-ROS-2","page":"Getting Started","text":"ROSNode entities are wire-compatible with real ROS 2 C++/Python nodes. See Interoperating with ROS 2.","title":"Interoperating with ROS 2"},{"category":"section","location":"getting-started/#Next","page":"Getting Started","text":"The Runtime Model\nInterface Types","title":"Next"},{"category":"section","location":"composition/components/#Components","page":"Components","text":"A component is a node authored as a collection of cohesive chunks — each a typed struct of private state, the entities authored onto it, and its own lifecycle. A component assembles the node from these chunks and runs it, where the other pages wire a node imperatively — a context, a Node, and each endpoint attached by hand.\n\nComponents can be built in two ways:\n\nThe @component macro with @component — one mutable struct block declares the state, parameters, ports, reactions, and lifecycle hooks together.\nThe member schema with component and the value combinators — write the pieces separately: publishes/hears/serves/runs/every/uses author the entities, and component(State, Params, ports…) binds them to a state type through the member_schema trait. This is the primitive @component expands to; reach for it directly for dynamic or programmatic assembly.\n\nEither way, node(\"name\" => State, …) composes the components with dependency injection into one runnable NodeSchema and run(schema) brings the node up.\n\nThis page builds a Vehicle node from two explicit combinator-built components, then recaps it as one @component block. The Vehicle is built from two parts:\n\nA Sensor that publishes telemetry on a timer and provides a battery reading, and\na Guard which enforces a sensed minimum battery level and that serves a \"safe to fly?\" query. \n\nwarning: `import` the framework generics — a bare `using` silently shadows them\nThe lifecycle hooks (configure, activate, deactivate, cleanup, on_error) and the member_schema trait are ROSNode functions you add methods to. Bring them in with import, not plain using:using ROSNode\nimport ROSNode: configure, cleanup, member_schema   # the generics you extendUnder a bare using ROSNode, a definition like configure(node, m::Sensor) = … creates a new local configure that shadows ROSNode's. Your method is never called: the framework dispatches its own generic, runs the default no-op, and there is no error or warning — the node simply comes up without your setup. (Equivalently, qualify the name at the definition: ROSNode.configure(node, m::Sensor) = ….)Reaction handlers (tick, safe) are fresh functions, so they need no import. The @component macro emits its member_schema and lifecycle methods as ROSNode.… for you, so a component authored entirely with the macro needs no import either.","title":"Components"},{"category":"section","location":"composition/components/#The-component-—-state-plus-the-entities-authored-onto-it","page":"Components","text":"The components and their authored types live in a module that names the ROS package the inline-authored service belongs to; the message type is authored in its own package module. run and the ground-station client see them through Drone.* and Msgs.Telemetry:\n\nmodule Msgs\n    using ROSNode\n    @ros_package \"drone_msgs\"\n    @ros_message struct Telemetry\n        battery::Float64\n        altitude::Float64\n    end\nend\n\nmodule Drone\n    using ROSNode\n    using ..Msgs: Telemetry\n    @ros_package \"drone\"\n    import ROSNode: configure, member_schema\nend\n\nA component splits its data into two independent stores, and each datum goes to the one matching who may read it:\n\nPrivate state — the fields of the component's mutable struct. Internal to the component; touched only by its own reactions and hooks.\nPublic parameters — a separate @parameters struct. Read live and retuned from outside through the parameter services (ros2 param get/set).\n\nThe two are independent, so a component carries whatever mix it needs — both, one, or neither. The Sensor carries both: a private level and a public rate.\n\nThe state struct is parameterised by its member-path Name and subtypes Component{Name}; the entities ride the schema, not the struct. A zero-argument ctor S{Name}() builds it with no dependencies — the default the framework calls for a component with no requires:\n\nmutable struct Sensor{Name} <: Component{Name}\n    level::Float64                          # simulated\nend\nSensor{Name}() where {Name} = Sensor{Name}(100.0)\n\nThe @parameters struct holds the public side, read through parameters(node, s). The ∈ 1..50 constraint reuses the parameter schema grammar:\n\n@parameters struct SensorParams\n    rate::Int64 = 5 ∈ 1..50                 # Hz\nend\n\ncomponent(State, Params, …) (below) binds both stores onto the component; component(State, …) with the Params argument dropped is a component with private state and no public parameters.\n\nReactions and lifecycle hooks are node-first: they take (node, m, …), where m is the component instance. The two reflective accessors take both — parameters(node, s) returns the current, type-stable parameter snapshot, and entities(node, s) returns the materialised entity handles. The member's path is a constant on its type, so parameters(node, s).rate and entities(node, s).telemetry are ordinary typed field loads off the node's carriers.\n\nconfigure is a lifecycle hook — a plain method run at startup; the Component Lifecycle page covers the full set. A reaction handler is an ordinary function dispatched on the component base:\n\nconfigure(node, s::Sensor) = @info \"Sensor up\" rate = parameters(node, s).rate\n\nfunction tick(node, s::Sensor)              # fires at `rate` Hz, only while Active\n    s.level = max(0.0, s.level - 1.0)\n    publish(entities(node, s).telemetry, Telemetry(battery = s.level, altitude = 12.0))\nend","title":"The component — state plus the entities authored onto it"},{"category":"section","location":"composition/components/#The-member-schema","page":"Components","text":"member_schema(::Type{S}) is the trait carrying the component's schema. It is defined on the bare base S (never a S{Name} instantiation), and component(State, Params, ports…) ties the state and parameter types to the authored ports. Each value combinator builds one port from a name plus its specifics; the full signatures and options live in Schema combinators:\n\npublishes(:telemetry, Telemetry; on) — a publisher carrying a message type.\nhears(:odom, Odometry, on_odom; on) — a subscription running a handler per message.\nserves(:safe, SafeReq, safe; on) — a service-server answering a request.\nruns(:dock, Dock, dock!; on) — an action-server running each accepted goal.\nevery(:tick, :rate, tick) — a timer firing a reaction (a frequency in Hz, or a parameter to bind it live).\nuses(:cmd, CmdReq; on) — a persistent service/action client port.\nremap(Sensor, :telemetry => \"…\") — override the names a member's ports resolve to, at node(…).\n\nevery(:tick, :rate, tick) binds the timer's frequency to the rate parameter:\n\nmember_schema(::Type{Sensor}) = component(Sensor, SensorParams,\n    publishes(:telemetry, Telemetry; on = \"~/telemetry\"),   # node-private ⇒ /vehicle/telemetry\n    every(:tick, :rate, tick);\n    provides = (BatterySource,))\n\nThe provides = (BatterySource,) keyword is the dependency-injection evidence covered below.","title":"The member schema"},{"category":"section","location":"composition/components/#Port-names-and-topics","page":"Components","text":"A combinator's first argument, name, is the port's identity — its key in entities(node, m), and (for the reaction combinators) the handler's name. That same name is also, by default, the topic the port uses (its service or action name, for serves/runs/uses). It resolves through a short chain:\n\nthe default is the port's own name, taken as a relative name;\nan on = \"/some/name\" clause overrides that default;\na node remap overrides it again;\nthe chosen name resolves against the node's namespace to its fully-qualified form — see Inspecting the resolved wiring.\n\nTimers are the exception: they address no topic and fire locally.","title":"Port names and topics"},{"category":"section","location":"composition/components/#Inspecting-a-schema","page":"Components","text":"A schema is a plain value — build one and look at it before running anything. A member_schema, and a node(…) schema, print their structure and each port's authored name:\n\nusing ROSNode\nimport ROSNode: member_schema\nconst Time = ROSNode.Interfaces.builtin_interfaces.msg.Time\n\nmutable struct Beacon{Name} <: Component{Name}\n    seq::Int\nend\nBeacon{Name}() where {Name} = Beacon{Name}(0)\n\npulse(node, b::Beacon) = nothing\non_clock(node, b::Beacon, msg::Time) = nothing\n\nmember_schema(::Type{Beacon}) = component(Beacon,\n    publishes(:beat, Time; on = \"~/beat\"),         # private\n    hears(:clock, Time, on_clock; on = \"/clock\"),  # absolute\n    every(:pulse, 2.0, pulse))\n\nmember_schema(Beacon)\n\nComposing it into a node shows the resolved DI order and each member's ports:\n\nrig = node(\"beacon\" => Beacon; name = \"Rig\")\n\ndescribe_wiring goes one step further: on a built node it appends the fully-qualified ROS name each port resolves to (here ~/beat private → /rig/beat, /clock absolute → /clock):\n\ncn = run(rig; name = \"rig\", localhost_only = true, block = false)\ndescribe_wiring(cn)\nclose(cn)","title":"Inspecting a schema"},{"category":"section","location":"composition/components/#Services-and-actions","page":"Components","text":"Services and actions are the imperative ports: a request earns a response, a goal runs to a result. Each needs a typed request/response (or goal/result/feedback) contract, and you create one two ways:\n\nAuthor it inline — @service / @action define the contract and the handler in one place. The arguments after (node, m) are the request (or goal) fields; the @NamedTuple return is the response (or result).\nWire a pre-authored type — serves(:name, ReqType, handler) / runs(:name, Action, exec) bind a handler to a service or action type you already have, imported from another package or authored elsewhere.\n\nInline authoring keeps the whole port in one definition:\n\n@service \"~/safe_to_fly\" function safe(node, g::Guard, target_altitude::Float64)::@NamedTuple{ok::Bool, battery::Float64}\n    b = battery(g.battery_src)\n    (ok = b >= parameters(node, g).min_battery && target_altitude <= 100.0, battery = b)\nend\n\nThe leading \"~/safe_to_fly\" sets the service name; the handler keeps its function name safe, which drops straight into component(…) as a port:\n\nmember_schema(::Type{Guard}) = component(Guard, GuardParams, safe;\n    requires = (BatterySource,), ctor = make_guard)\n\n@action authors an action server the same way, with a feedback stream alongside the goal and result. Both macros generate and register the wire types for you, and either handler rebinds under another name — the @service/@action docstrings cover those forms, and Services / Actions the request/response and goal/result/feedback models.","title":"Services and actions"},{"category":"section","location":"composition/components/#Dependency-injection-between-components","page":"Components","text":"Guard needs a battery reading that a sibling owns. It asks for that capability by interface, and the framework injects a sibling that provides it.\n\nAn @interface names a capability — the generic functions a provider must define:\n\n@interface BatterySource  battery(_)::Float64\n\nSensor provides it by backing the contract method and listing the interface in its provides=:\n\nbattery(s::Sensor) = s.level\n\nProvision is Holy-trait evidence: a component declares the interfaces it provides, resolved against a consumer's requires rather than by subtyping. Declare it two ways:\n\non the component(…) call — the provides = (BatterySource,) keyword;\non the type — a @provides declaration (@provides Sensor BatterySource), sugar for a provides(::Type{Sensor}) method.\n\nThe keyword takes precedence; the method is the fallback, used when the keyword is omitted. A consumer declares its matching need with requires= (or, the same two ways, a requires(::Type{M}) method) and receives the resolved provider through its constructor. Guard holds that provider in a type parameter so reads of it are type-stable, and supplies a constructor that places its own member name:\n\nmutable struct Guard{Name, B} <: Component{Name}\n    battery_src::B                          # the injected sibling provider\nend\n@parameters struct GuardParams\n    min_battery::Float64 = 20.0\nend\nmake_guard(node, ::Val{Name}, src) where {Name} = Guard{Name, typeof(src)}(src)\n\nThe requires = (BatterySource,) plus ctor = make_guard in Guard's member_schema above wire the need to the constructor. The single dependency arrives as src, fixing the member's type to Guard{:guard, Sensor{:sensor}}; the handler reads it through the interface — battery(g.battery_src) in the safe service. Parametric Components covers the constructor contract in full, injecting a mock provider in tests, and running a dependent component standalone.","title":"Dependency injection between components"},{"category":"section","location":"composition/components/#The-@component-macro","page":"Components","text":"@component is the concise authoring tier: it emits the whole value API — the struct, the zero-arg ctor, the @parameters struct, the reaction/hook methods, and member_schema — from one mutable struct block. A struct-body edit re-runs the whole block and re-keys everything together (Revise-friendlier than the separate definitions). It is sugar over component/node; the raw combinators remain the primitive, and the two coexist in one node.\n\nThe block uses inline directives:\n\nfield = default — a private state field (a plain mutable struct field), with the default the zero-arg ctor uses.\n@param x::T = d ∈ lo..hi — a public parameter; the macro collects these into an emitted @parameters struct. Mix the two freely: a block carries any number of each.\n@provides Iface — interface(s) the component provides.\n@requires src::Marker — an injected dependency, where Marker is a concrete component type or an @interface. The macro adds the type parameter that holds the resolved sibling and the constructor that places it, so the field reads type-stably and you write no parametric struct or ctor by hand.\n@publishes out::T on \"~/topic\" — a publisher port (the on clause trails the declaration).\n@hears function h(node, m, msg::T) … end — a subscription port plus its handler (inline-only).\n@every :rate function tick(node, m) … end — a timer port plus its handler (rate = Hz or a parameter Symbol; inline-only).\n@service \"~/s\" function f(node, m, x::X)::@NamedTuple{…} … end — inline service authoring.\nconfigure(node, m) = … — lifecycle hooks live in the block too.\n\nA bare m argument is annotated m::Sensor for you, so reaction bodies stay fully typed. The handler directives @hears/@service/@action take a leading name string, while @publishes takes the trailing on \"…\" form — the split follows the name source.\n\n@component mutable struct Sensor{Name} <: Component{Name}\n    level::Float64 = 100.0                              # simulated battery %\n    @param rate::Int64 = 5 ∈ 1..50                      # Hz\n    @provides BatterySource\n    @publishes telemetry::Telemetry on \"~/telemetry\"\n    @every :rate function tick(node, m)                 # fires at the live `rate` Hz, only while Active\n        m.level = max(0.0, m.level - 1.0)\n        publish(entities(node, m).telemetry, Telemetry(battery = m.level, altitude = 12.0))\n    end\n    configure(node, m) = @info \"Sensor up\" rate = parameters(node, m).rate\nend\n# A non-node-first interface impl lives OUTSIDE the block (a struct edit would orphan it):\nbattery(s::Sensor) = s.level\n\nA DI consumer declares its dependencies with @requires — the Guard from Dependency injection between components, authored without the hand-written parametric struct or constructor:\n\n@component mutable struct Guard{Name} <: Component{Name}\n    @requires battery_src::BatterySource        # inject the sibling that provides it\n    @param min_battery::Float64 = 20.0\n    @service \"~/safe_to_fly\" function safe(node, g, target_altitude::Float64)::@NamedTuple{ok::Bool, battery::Float64}\n        b = battery(g.battery_src)               # type-stable: battery_src is the resolved sibling's type\n        (ok = b >= parameters(node, g).min_battery && target_altitude <= 100.0, battery = b)\n    end\nend\n\n@requires battery_src::Marker adds the type parameter that holds the resolved sibling and the construct that places it; Marker is an @interface (as here) or a concrete component type. A client port (uses) still uses the raw value API — @uses in the block errors clearly, pointing at it. The macro and the raw combinators compose in one node: the @component-authored Sensor and Guard above assemble unchanged.","title":"The @component macro"},{"category":"section","location":"composition/components/#Assembling-and-running-the-node","page":"Components","text":"node assembles the node kind from a list of members. Each \"name\" => State pair gives the component a namespace within the node, and the framework resolves DI once, here:\n\nconst Vehicle = node(\"sensor\" => Drone.Sensor, \"guard\" => Drone.Guard; name = \"Vehicle\")\n\nGuard requires BatterySource and Sensor provides it, so the node call:\n\ntoposorts the members by dependency and injects the Sensor into the Guard;\norders lifecycle so a dependency configures before its dependent and tears down after;\nregisters the kind under name = \"Vehicle\" in the process-global registry, so a container can load it by name (see Containers & Dynamic Composition);\nfreezes the member order and dependency edges into the returned NodeSchema's type, so construction is type-stable.\n\nrun brings the node up. Its options:\n\nblock = false — returns immediately so the caller can drive the node.\nctx = ctx — runs the node on an existing context; omit ctx to run it standalone in its own process.\noverrides = (…,) — sets each member's parameters by their local names.\n\n@context() do ctx\n    ground = Node(ctx, \"ground\")\n    Subscription(ground, \"/vehicle/telemetry\", Msgs.Telemetry) do msg\n        @info \"telemetry\" battery = round(msg.battery; digits = 1) altitude = msg.altitude\n    end\n\n    vehicle = run(Vehicle; ctx = ctx, name = \"vehicle\",\n                  overrides = (rate = 5, min_battery = 90.0), block = false)\n    sleep(1.2)                              # let a few telemetry messages arrive before the query\n\n    SafeReq = Drone.drone.srv.safe_Request\n    guard = ServiceClient(ground, \"/vehicle/safe_to_fly\", SafeReq)\n    wait_for_service(guard; timeout = 5)\n    resp = call(guard, SafeReq(target_altitude = 50.0))\n    @info \"safe to fly?\" ok = resp.ok battery = round(resp.battery; digits = 1)\n\n    close(guard)\n    close(vehicle)\nend\n\nThe ground station is a plain node subscribing to /vehicle/telemetry. The vehicle publishes telemetry at 5 Hz, and the guard answers the ~/safe_to_fly query its @service authored.","title":"Assembling and running the node"},{"category":"section","location":"composition/components/#Inspecting-the-resolved-wiring","page":"Components","text":"The last step of the chain in Port names and topics resolves a port's name against the node's namespace, by the standard ROS rules:\n\na relative name (foo) resolves under the node's namespace — /foo;\na private name (~/foo) resolves under the node's own name — /vehicle/foo;\nan absolute name (/foo) stands as written.\n\ndescribe_wiring on a built node prints each port's authored name and the resolved name beside it — the Inspecting a schema demo runs it live. It is the quick way to confirm two ports share a name before chasing a silent non-delivery. The resolved name is what the entity uses as a Zenoh key expression; see Addressing & Key Expressions.\n\nTwo ports connect only when they resolve to the same name:\n\nMismatched names silently miss. A hears(:foo, …) (relative foo → /foo) and a publishes(:p, …; on = \"~/foo\") (private → /vehicle/foo) land on different topics, so they never connect.\nColliding names error. A node call errors when two same-channel outputs resolve to one name, unless one was explicitly remapped — remap resolves the clash.","title":"Inspecting the resolved wiring"},{"category":"section","location":"composition/components/#Lifecycle","page":"Components","text":"Every component has a lifecycle: setup in configure, the matching release in cleanup, and activate/deactivate/on_error for the rest. A component runs unmanaged or managed. Component Lifecycle is the full treatment: the hook set, the managed state machine, the dispatch gate, and failure/recovery.","title":"Lifecycle"},{"category":"section","location":"composition/components/#Composition-at-two-scales","page":"Components","text":"run(Vehicle; name = \"vehicle\") gives the node its own process, with its own session, discovery, and registry. A container instead runs several nodes in one process — sharing one session, discovery, and a direct in-process delivery path between them — and loads a registered kind by name (load_node, the ros2 component load path). The same schema runs either way; deploy-time wiring picks the scale. Containers & Dynamic Composition covers containers, by-name loading, and inspecting a composed node.","title":"Composition at two scales"},{"category":"section","location":"composition/components/#See-also","page":"Components","text":"Parametric Components — type parameters beyond Name, the dependency-injection constructor contract, mocks, and standalone runs.\nComponent Lifecycle — the hook set, managed nodes, and the dispatch gate.\nContainers & Dynamic Composition — running several nodes in one process and loading a kind by name.\nParameters — the live parameter model @parameters builds on.\nPrecompilation & Warm-up — baking a component's first-run path with precompile_node.\nInteroperating with ROS 2 — driving a component's parameters, topics, and services from ros2 tooling.","title":"See also"},{"category":"section","location":"composition/components/#API-reference","page":"Components","text":"","title":"API reference"},{"category":"section","location":"composition/components/#Authoring-a-component","page":"Components","text":"","title":"Authoring a component"},{"category":"section","location":"composition/components/#Schema-combinators","page":"Components","text":"","title":"Schema combinators"},{"category":"section","location":"composition/components/#Ports-and-entities","page":"Components","text":"","title":"Ports and entities"},{"category":"section","location":"composition/components/#Interfaces-and-dependency-injection","page":"Components","text":"The dependency-injection constructor (construct) is documented under Parametric Components; the lifecycle hooks under Component Lifecycle; containers, the node-kind registry, and schema introspection under Containers & Dynamic Composition.","title":"Interfaces and dependency injection"},{"category":"section","location":"composition/components/#Assembling-a-node","page":"Components","text":"","title":"Assembling a node"},{"category":"type","location":"composition/components/#ROSNode.Component","page":"Components","text":"abstract type Component{Name} end\n\nSupertype of every component type. A component is a cohesive chunk of a node — its private state and public parameters, the ports its member_schema declares, and the lifecycle hooks dispatched on it; a node is a collection of components sharing one node-core.\n\nName is the component's relative path within its node — its member name, lifted to a type parameter so resolution against the node's typed runtime collapses through dispatch (entities(node, m) / parameters(node, m) read node.ports/node.pservers at the member named Name). A component instance holds no node reference — the node owns the runtime, the instance carries only its state plus this path — which is what keeps the node↔member relationship acyclic and fully typed.\n\nComponent carries the default lifecycle hooks (configure / activate / deactivate / cleanup / on_error, all no-ops) and the default DI evidence (provides / requires, both empty), so a bare component needs none of them spelled out. Subtype it as mutable struct M{Name} <: Component{Name} … end (the @component macro injects the Name parameter for you; a parametric one writes struct M{Name, …} <: Component{Name} itself).\n\n\n\n\n\n","title":"ROSNode.Component"},{"category":"macro","location":"composition/components/#ROSNode.@component","page":"Components","text":"@component mutable struct Name{Name} <: Component{Name}\n    field::T = default                       # private state; inline default feeds the zero-arg ctor\n    @param  rate::Int64 = 5 ∈ 1..50          # → an emitted `@parameters struct NameParams`\n    @provides Iface                          # interface(s) this component provides\n    @requires src::Sensor                    # inject a sibling (a component type or an @interface)\n    @publishes out::T on \"~/out\"             # a publisher port (no handler)\n    @hears  function on_msg(node, m, msg::T) … end       # subscription port + handler (bundled)\n    @every  :rate function tick(node, m) … end           # timer port + handler (rate = Hz or :param)\n    @service \"~/srv\" function srv(node, m, x::X)::@NamedTuple{…} … end   # inline service authoring\n    configure(node, m) = …                   # lifecycle hooks live here too (re-coupled on a struct edit)\nend\n\nAuthor a single component in one expression. Expands to the mutable struct ({Name} injected if you write just Name), a zero-arg Name{Name}() ctor from the inline field defaults, an @parameters struct NameParams from the @param fields, the reaction/hook/@service/@action method defs (a bare m annotated as m::Name), and member_schema(::Type{Name}) = component(Name, …) on the BARE base. Editing anything re-runs the whole block, so the struct and every method re-key on the new type together.\n\nConventions:\n\nWire clause. The declaration directive @publishes name::T takes the trailing on \"wire\" form; the handler directives @hears/@service/@action take a leading \"wire\" string (matching the standalone @service/@action macros). The split follows the name source: @publishes names the port, the handler directives derive it from the function.\n@hears/@every are inline-only in v1: the handler is written in place as function f(node, m, …) … end (so a struct edit re-couples it). To wire a port to an externally-defined handler, drop to the raw hears(:n, T, h) / every(:n, rate, h) combinators.\nA reaction/hook/handler is node-first (node, m, …); a non-node-first interface impl (e.g. battery(s::Sensor)) is defined OUTSIDE the block.\n\n@component is sugar over the value API — the raw component/node combinators remain the primitive for dynamic composition. v1 covers the DI-free common case (plus @provides); a DI consumer (@requires + an injected ctor) or a client port is authored with the raw component(M, …; requires=(I,), ctor=f) / uses(:n, marker) API. Use it from a module that does using ROSNode.\n\n\n\n\n\n","title":"ROSNode.@component"},{"category":"function","location":"composition/components/#ROSNode.member_schema","page":"Components","text":"member_schema(::Type{S}) -> MemberSchema — the schema associated with a member state type (the trait that lets node(\"name\" => S, …) carry one name per component).\n\n\n\n\n\n","title":"ROSNode.member_schema"},{"category":"function","location":"composition/components/#ROSNode.component","page":"Components","text":"component(State, [Params,] ports…; provides=(), requires=(), ctor=nothing) -> MemberSchema\n\nTie a component's State type and an optional Params (@parameters) type to its ports — the publishes/hears/serves/runs/every/uses descriptors, plus bare @service/@action handlers. The result is the component's member_schema. Drop Params for a component with no public parameters. provides/requires declare the component's dependency-injection evidence and ctor its injected constructor (see Parametric Components).\n\n\n\n\n\n","title":"ROSNode.component"},{"category":"function","location":"composition/components/#ROSNode.publishes","page":"Components","text":"publishes(name::Symbol, T; on=nothing) -> Pub\n\nA publisher port carrying message type T, sent with publish(entities(node, m).name, msg). name keys the handle in entities(node, m); on sets its topic (the port name by default).\n\n\n\n\n\n","title":"ROSNode.publishes"},{"category":"function","location":"composition/components/#ROSNode.hears","page":"Components","text":"hears(name::Symbol, T, handler; on=nothing) -> Sub\n\nA subscription on message type T that runs handler(node, m, msg::T) per message. on sets the topic it subscribes to (the reaction name by default).\n\n\n\n\n\n","title":"ROSNode.hears"},{"category":"function","location":"composition/components/#ROSNode.serves","page":"Components","text":"serves(name::Symbol, ReqType, handler; on=nothing) -> Srv\nserves(name::Symbol, handler; on=nothing) -> Srv          # a bare @service handler\nserves(name::Symbol, existing::Srv; on=nothing) -> Srv    # rebind under another name\n\nA service-server port answering handler(node, m, req) over request type ReqType. on sets the service name (the port name by default). Author the request/response types inline with @service (then serves takes the bare handler), or pass a pre-authored ReqType here. See Services.\n\n\n\n\n\n","title":"ROSNode.serves"},{"category":"function","location":"composition/components/#ROSNode.runs","page":"Components","text":"runs(name::Symbol, Action, exec; on=nothing) -> Act\nruns(name::Symbol, existing::Act; on=nothing) -> Act      # rebind under another name\n\nAn action-server port running exec(node, m, goal) per accepted goal, over a pre-authored @ros_action type Action. on sets the action name (the port name by default). Author the action inline with @action, or pass a pre-authored Action here. See Actions.\n\n\n\n\n\n","title":"ROSNode.runs"},{"category":"function","location":"composition/components/#ROSNode.every","page":"Components","text":"every(name::Symbol, rate, handler) -> Tmr\n\nA timer firing handler(node, m) at rate — a frequency in Hz (a Real), or a parameter Symbol to bind the period live to that parameter. A timer addresses no topic, and fires only while the node is Active.\n\n\n\n\n\n","title":"ROSNode.every"},{"category":"function","location":"composition/components/#ROSNode.uses","page":"Components","text":"uses(name::Symbol, marker; on=nothing) -> Use\n\nDeclare a persistent service/action CLIENT port. marker is a service request type (or @ros_service marker) → a ServiceClient, or an @ros_action/authored action marker → an ActionClient. The client is materialised at configure into entities(node, m).name and held for the node's life (reuse it for call/send), mirroring rclcpp's create_client. on sets the service/action name (else the port name).\n\n\n\n\n\n","title":"ROSNode.uses"},{"category":"function","location":"composition/components/#ROSNode.remap","page":"Components","text":"remap(component, port => \"name\" | (member, port), …)\n\nWrap a node(…) member with explicit name overrides for one or more of its ports. port => \"name\" retargets that port's topic (or service/action name); port => (member, port) ties it to another member's port. Use it to resolve a name clash or to connect two members onto one topic.\n\n\n\n\n\n","title":"ROSNode.remap"},{"category":"macro","location":"composition/components/#ROSNode.@service","page":"Components","text":"@service        function f(node, m::M, field::T, …)::@NamedTuple{out::U, …} … end\n@service \"wire\" function f(node, m::M, field::T, …)::@NamedTuple{out::U, …} … end\n\nAuthor a component service port in one place. The arguments after node, m are the request fields and the ::@NamedTuple{…} return is the response. The macro reuses @ros_service's codegen to generate f_Request/f_Response (package from @ros_package, else the snake-cased module name), register their RIHS01, and specialize request_type/response_type on typeof(f) — so f is a normal @ros_service marker. It then adds ONE meta-method, component_service(typeof(f)), marking f a node-first component handler with a default port name (the function name) and wire (a leading string, else ~/f). The handler stays a normal method (callable, testable); serves/component derive the Srv descriptor from the trait, splatting the request fields and wrapping the @NamedTuple return into f_Response.\n\nDrop down the hierarchy for more control: serves(:other, f; on=\"…\") reuses this handler under another name; serves(:name, f_Request, other; on=\"…\") reuses the authored TYPE with a different handler.\n\n@service \"~/safe_to_fly\" function safe(node, g::Guard, target_altitude::Float64)::@NamedTuple{ok::Bool, battery::Float64}\n    b = battery(g.battery_src)\n    (ok = b >= parameters(node, g).min_battery && target_altitude <= 100.0, battery = b)\nend\nmember_schema(::Type{Guard}) = component(Guard, GuardParams, safe; requires = (BatterySource,), ctor = make_guard)\n\n\n\n\n\n","title":"ROSNode.@service"},{"category":"macro","location":"composition/components/#ROSNode.@action","page":"Components","text":"@action        function f(node, m::M, goal::T, …, fb::FeedbackSink{@NamedTuple{…}})::@NamedTuple{…} … end\n@action \"wire\" function f(node, m::M, goal::T, …, fb::FeedbackSink{@NamedTuple{…}})::@NamedTuple{…} … end\n\nAuthor a component action port in one place — the action analog of @service. After node, m, the arguments other than the FeedbackSink are the Goal fields, the sink's @NamedTuple gives the Feedback fields, and the ::@NamedTuple{…} return is the Result. The macro reuses @ros_action's codegen (Goal/Result/Feedback + the five protocol wrappers, registration, and ActionTypeSupport(typeof(f))), so f is a normal action marker, then adds the component_action meta-method. The handler stays a named method; runs/component derive the Act descriptor, running f per accepted goal with a live FeedbackSink. The body's exit maps to the goal's terminal status, exactly as @ros_action / runs:\n\na normal return settles SUCCEEDED with the converted Result;\na thrown Cancelled (raised at a checkpoint/feedback! after a cancel request) settles CANCELED;\nany other throw (or a body that exits without responding) settles ABORTED.\n\nBecause f keeps its name (and the authored f_Goal/f_Result/f_Feedback + protocol-wrapper types stay reachable), a client uses it directly: ActionClient(node, ke, f).\n\n@action function countup(node, m::Counter, target::Int32,\n                         fb::FeedbackSink{@NamedTuple{current::Int32}})::@NamedTuple{total::Int32}\n    for i in 1:target; fb((current = Int32(i),)); end\n    (total = target,)\nend\nmember_schema(::Type{Counter}) = component(Counter, countup)\n\n\n\n\n\n","title":"ROSNode.@action"},{"category":"function","location":"composition/components/#ROSNode.entities","page":"Components","text":"entities(node, m::Component) -> NamedTuple\nentities(node::ComponentNode) -> NamedTuple\n\nThe materialised port handles as a NamedTuple. Drive each handle through its own typed methods (publish, …). The ports must be materialised first (after the node is configured); reading before then errors with a clear message, and reactions run only after materialisation.\n\nentities(node, m::Component) — one member's handles, keyed by port identifier: entities(node, m).image is the live Publisher, entities(node, m).tick the Timer, entities(node, m).<service> the Service. Read from the node's typed ports carrier at the member named _path(m), so entities(node, m).field is an inlinable typed field load — the component stays state-only and holds no node reference. Inside a reaction the node is the first argument.\nentities(node::ComponentNode) — every member's handles aggregated and namespaced by member name in declared order: entities(node).camera.image is member camera's image port, so two members' image handles don't collide. Each value is the corresponding per-member view.\n\n\n\n\n\n","title":"ROSNode.entities"},{"category":"function","location":"composition/components/#ROSNode.parameters","page":"Components","text":"parameters(node, m::Component) -> P_M\nparameters(node::ComponentNode) -> NamedTuple\n\nThe component's live parameter snapshot: a typed @parameters-style struct P_M (the member_schema(M) = component(M, P_M, …) parameter schema), read type-stably as parameters(node, m).field. Backed by the node-core's ParameterServer; the returned struct is a complete snapshot, so a reaction never sees half-applied state.\n\nRead from the node's typed pservers carrier at the member named _path(m), so parameters(node, m).fps is a typed field load — the component holds no node reference. For a single-component node the node schema is P_M and parameters are un-prefixed; a composed node member-namespaces them at the node level while parameters(node, m) stays member-local. Inside a reaction the node is the first argument.\n\n\n\n\n\n","title":"ROSNode.parameters"},{"category":"macro","location":"composition/components/#ROSNode.@interface","page":"Components","text":"@interface Name\n@interface Name  method₁(_)::T  method₂(_)  …\n\nDeclare a dependency interface: a NAME for a set of generic functions, emitted as an empty marker struct Name <: ComponentInterface. @provides / requires resolve against this marker — provision is Holy-trait evidence, not subtyping (a component may provide any number) and not method existence.\n\nThe method signatures after the name are documentation only: the macro discards them, and a provider satisfies the interface by defining the methods and declaring @provides M Name — the resolver trusts the declaration, with no check that the methods actually exist.\n\n@interface PoseSource  last_pose(_)::Pose\n\n\n\n\n\n","title":"ROSNode.@interface"},{"category":"macro","location":"composition/components/#ROSNode.@provides","page":"Components","text":"@provides M  Interface₁  Interface₂  …\n\nDeclare that component M provides the given interfaces — emits the Holy-trait evidence provides(::Type{M}) = (Interface₁, …). A component may provide any number of interfaces.\n\nThe declaration is taken on trust, with no check that M actually defines each interface's methods. The node assembler reads this evidence to resolve siblings' requires to providers.\n\nlast_pose(m::ImageCapture) = m.pose\n@provides ImageCapture PoseSource\n\n\n\n\n\n","title":"ROSNode.@provides"},{"category":"function","location":"composition/components/#ROSNode.provides","page":"Components","text":"provides(::Type{M}) -> Tuple\n\nThe interfaces component M provides — Holy-trait evidence consulted by the node assembler to resolve sibling requires edges. Defaults to the empty tuple (); set it with @provides (or component(M, …; provides=(I,))). Each element is an @interface marker type.\n\n\n\n\n\n","title":"ROSNode.provides"},{"category":"function","location":"composition/components/#ROSNode.requires","page":"Components","text":"requires(::Type{M}) -> Tuple\n\nThe dependencies component M declares. Defaults to the empty tuple (); set it by defining requires(::Type{M}) or with component(M, …; requires=(I,)) (the keyword overrides the method). Each entry is one of two kinds:\n\nAn @interface marker — resolved against sibling members' @provides evidence. Consumed through the interface's methods and typically stored in a free type parameter for type-stable access.\nA concrete component type — resolved against the sibling member that is that component (matched on its base, so a parametric component is named by its base too). Types the injected dependency concretely: the consuming ctor can annotate deps… with the component type, no free type parameter needed.\n\nAt assembly the node resolver maps each requirement to the single matching sibling (excluding M itself, so a component cannot satisfy its own requirement), forms a dependency edge, toposorts the members, and injects the resolved providers positionally into the member's ctor(node, ::Val{Name}, deps…) in requires order.\n\nZero matches is an unsatisfied-dependency error; more than one is an ambiguity error (restructure so a single sibling satisfies it). Pin-pair disambiguation (an entry I => :member or I => ComponentType) is unsupported — a Pair entry raises an error directing you to restructure.\n\n\n\n\n\n","title":"ROSNode.requires"},{"category":"function","location":"composition/components/#ROSNode.node","page":"Components","text":"node(\"name\" => StateOrSchema, …; name=nothing, register=(name !== nothing)) -> NodeSchema\n\nAssemble a node kind. Each member is \"name\" => S (a state type whose member_schema(S) gives its MemberSchema) or \"name\" => component(…) directly. DI is resolved ONCE here, in the normal world; the resolved member order + dependency edges are frozen into the returned NodeSchema's type so the construction is type-stable with no runtime Core.eval.\n\nname (a Symbol/String) is the node KIND's name — the key it is registered under in the process-global node-kind registry, and its default instance name. When name is given the kind is registered by default (the rclcpp_components_register_nodes analog, via register_node_kind!), so a container's load_node / ros2 component load can instantiate it by name; pass register=false to build the schema without registering, or name with register=true to force it.\n\nImmediate registration is skipped while a package is generating precompile output (jl_generating_output) — a top-level mutation of the registry would not survive precompilation. A plain node(…) call cannot inject the module roster + __init__ load hook, so a precompiled package must re-register its const schemas at load from its own __init__, e.g. __init__() = register_node_kind!(\"Rig\", Rig). In the REPL, scripts, and runtime (including dynamic composition), registration is immediate and needs no __init__.\n\n\n\n\n\n","title":"ROSNode.node"},{"category":"function","location":"composition/components/#ROSNode.describe_wiring","page":"Components","text":"describe_wiring([io], node::ComponentNode)\n\nPrint each member port's identifier and the fully-qualified ROS name it resolves to, so the name expansion is visible at a glance — in particular a private ~/foo resolving to /<node>/foo versus a relative foo resolving to /<namespace>/foo, the usual reason a @hears and a publisher that \"should\" match land on different topics. The middle column is the authored (post-remap) wire name; the arrow shows where it resolves. Timers carry no wire and are listed bare.\n\n\n\n\n\n","title":"ROSNode.describe_wiring"},{"category":"section","location":"interop/ros2/#Interoperating-with-ROS-2","page":"Interoperating with ROS 2","text":"ROSNode mirrors rmw_zenoh on the wire. With rmw_zenoh selected and a matching router plus ROS_DOMAIN_ID, ROSNode entities exchange messages with real C++/Python ROS 2 nodes.","title":"Interoperating with ROS 2"},{"category":"section","location":"interop/ros2/#Wire-identity-(RIHS01)","page":"Interoperating with ROS 2","text":"A shared RIHS01 hash is what lets a ROSNode entity and a stock C++/Python endpoint speak the same wire type — see Interface Types for how the hash is derived and matched across imported, authored, and runtime-discovered types.","title":"Wire identity (RIHS01)"},{"category":"section","location":"interop/ros2/#Talking-to-ROS-2-from-the-command-line","page":"Interoperating with ROS 2","text":"The ros2 CLI reaches ROSNode entities the same way it reaches any node, once the ROS 2 side joins the same Zenoh fabric and domain:\n\nSelect the Zenoh RMW: export RMW_IMPLEMENTATION=rmw_zenoh_cpp.\nStart its router so both sides have a meeting point: ros2 run rmw_zenoh_cpp rmw_zenohd.\nMatch ROS_DOMAIN_ID on both sides (default 0).\n\nA domain or RMW mismatch produces silence — no traffic, no diagnostic — because domain_id leads every keyexpr, so each side's traffic stays confined to its own domain (see Addressing & Key Expressions).\n\nTopics — echo what a Publisher sends, or feed a Subscription (Topics):\n\nros2 topic echo /chatter std_msgs/msg/String\nros2 topic pub  /chatter std_msgs/msg/String \"{data: hi}\"\n\nServices — call a Service server (Services):\n\nros2 service call /add_two_ints example_interfaces/srv/AddTwoInts \"{a: 2, b: 40}\"\n\nActions — send a goal to an ActionServer (Actions):\n\nros2 action send_goal /fibonacci action_tutorials_interfaces/action/Fibonacci \"{order: 8}\"\n\nParameters — read and write a parameterized node's live parameters (Parameters):\n\nros2 param get /client_demo_server max_speed\nros2 param set /client_demo_server max_speed 80\n\nLifecycle — drive a managed component's transitions (Components):\n\nros2 lifecycle set /vehicle configure\nros2 lifecycle get /vehicle","title":"Talking to ROS 2 from the command line"},{"category":"section","location":"interop/ros2/#Where-imported-types-come-from","page":"Interoperating with ROS 2","text":"@ros_import statically generates an interface by name, resolving sources in precedence order:\n\nLocal source roots passed via from= — searched first, so your own packages resolve ahead of (and can shadow) the rest.\nROSNode's vendored tree.\nAMENT_PREFIX_PATH, when run inside a sourced ROS 2 environment.\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\nSee Interface Types for the full import surface.","title":"Where imported types come from"},{"category":"section","location":"interop/ros2/#Reaching-peers-on-other-hosts","page":"Interoperating with ROS 2","text":"The default Context already reaches peers on other hosts: localhost_only is false, so multicast scouting stays on. Add peers=[\"tcp/host:7447\"] to dial a router directly. See Getting Started for the router command and the discovery options.","title":"Reaching peers on other hosts"},{"category":"section","location":"interop/ros2/#See-also","page":"Interoperating with ROS 2","text":"Topics\nServices\nActions\nParameters","title":"See also"},{"category":"section","location":"composition/parametric/#Parametric-Components","page":"Parametric Components","text":"A component carries its member path as the type parameter Name. A parametric component adds further type parameters — most often to hold an injected dependency in a field whose type is fixed per composition, so reactions read it through a direct, type-stable field load.\n\nComponents injects a Sensor into a Guard through the BatterySource interface. This page is the contract behind that injection: how the constructor places Name and the resolved dependencies, how the same component takes a real provider in production and a mock in a test, and how to run a dependent component on its own.","title":"Parametric Components"},{"category":"section","location":"composition/parametric/#A-dependency-held-in-a-type-parameter","page":"Parametric Components","text":"The injected provider's concrete type varies by composition — a real Sensor in the vehicle, a mock in a test rig. A free type parameter B fixes that type per composition, so battery(g.battery_src) resolves to a direct call:\n\nmutable struct Guard{Name, B} <: Component{Name}\n    battery_src::B\nend\n\nThe assembler fills B from whatever sibling satisfies the requires. In Vehicle that is the Sensor, so the member's type is Guard{:guard, Sensor{:sensor}} — the provider's own Name rides along, naming its path within the node.","title":"A dependency held in a type parameter"},{"category":"section","location":"composition/parametric/#The-construct-contract","page":"Parametric Components","text":"Name is a type parameter and the injected provider fills B, so the framework needs a constructor that places both. It calls construct with the node-core handle, the member name as Val{Name}, and one dependency per declared requires, in requires order, and builds Guard{Name, B} from them.\n\nA Guard whose only fields are its injected deps is a pure holder, and the default construct builds it from the type — Guard{Name, typeof(src)}(src) — so it needs no constructor at all:\n\nmember_schema(::Type{Guard}) = component(Guard, GuardParams, safe; requires = (BatterySource,))\n\nThe default fits when the free type parameters past Name are exactly the deps, in order, and the struct's fields are those deps. Override it for any other shape — deps stored in named fields, non-dep fields to set, a different parameter order — as either surface: a construct(::Type{Guard}, …) method on the bare type, or the ctor = keyword on component (the keyword wins if both are given):\n\nconstruct(::Type{Guard}, node, ::Val{Name}, src) where {Name} = Guard{Name, typeof(src)}(src)\nmember_schema(::Type{Guard}) = component(Guard, GuardParams, safe; requires = (BatterySource,))\n\nmake_guard(node, ::Val{Name}, src) where {Name} = Guard{Name, typeof(src)}(src)\nmember_schema(::Type{Guard}) = component(Guard, GuardParams, safe; requires = (BatterySource,), ctor = make_guard)\n\nThe @component macro's @requires battery_src::BatterySource directive emits the parametric struct and this constructor for you (see The @component macro) — the concise form when the component is authored as one block. A component whose every parameter past Name is defaulted needs none of this: its constructor is the default M{Name}().\n\nAn injected provider arrives constructed but not yet configured: the framework configures providers ahead of their consumers (see Component Lifecycle). Store it from the constructor, then read it through its interface from configure onward.","title":"The construct contract"},{"category":"section","location":"composition/parametric/#Injecting-a-mock-in-tests","page":"Parametric Components","text":"The provider rides B, so the consumer's code is identical whichever provider it gets. A test composes a mock that satisfies the interface, and the same Guard reads it type-stably:\n\nmutable struct MockBattery{Name} <: Component{Name} end\nbattery(::MockBattery) = 42.0\nmember_schema(::Type{MockBattery}) = component(MockBattery; provides = (BatterySource,))\n\nrig = node(\"batt\" => MockBattery, \"guard\" => Guard)     # guard member is Guard{:guard, MockBattery{:batt}}","title":"Injecting a mock in tests"},{"category":"section","location":"composition/parametric/#Running-a-dependent-component-standalone","page":"Parametric Components","text":"run(S) promotes a single component to its own node with un-prefixed parameters, constructing it through its zero-dependency constructor:\n\na component declaring no requires — constructed and run;\na requires-bearing component — rejected (no sibling can satisfy the dependency), with the reason named.\n\nA component you want to run both ways gets a zero-requires variant whose constructor injects a null-object stand-in — a provider that answers the interface with a safe default:\n\nstruct NullBattery end\nbattery(::NullBattery) = 100.0                          # always \"full\": the standalone default\n\nmutable struct SoloGuard{Name} <: Component{Name}\n    battery_src::NullBattery\nend\nSoloGuard{Name}() where {Name} = SoloGuard{Name}(NullBattery())\nmember_schema(::Type{SoloGuard}) = component(SoloGuard, GuardParams, safe)\n\nrun(SoloGuard; name = \"guard\")                          # un-prefixed params: ros2 param get /guard min_battery\n\nThe composed Guard and the standalone SoloGuard share their reactions and parameters; the provider wiring is all that differs.","title":"Running a dependent component standalone"},{"category":"section","location":"composition/parametric/#API-reference","page":"Parametric Components","text":"","title":"API reference"},{"category":"function","location":"composition/parametric/#ROSNode.construct","page":"Parametric Components","text":"construct(::Type{M}, node, ::Val{Name}) -> M{Name}\nconstruct(::Type{M}, node, ::Val{Name}, deps…) -> M{Name…}\n\nBuild a component instance during node assembly. Name is the member's path within the node (its name in the node(…) member list), supplied by the framework as a Val. A component that requires interfaces or sibling components receives the resolved providers as deps…, positionally in requires order, and stores them — typically a sibling dep into a field typed by that component (whose own Name parameter then carries that sibling's path).\n\nTwo defaults cover the common shapes, so most components define no construct at all:\n\nNo dependencies — construct(::Type{M}, node, ::Val{Name}) returns M{Name}(), for a component whose every field past Name is defaulted.\nPure dependency holder — construct(::Type{M}, node, ::Val{Name}, deps…) builds M{Name, typeof.(deps)…}(deps…) from the type itself, for a component whose free type parameters past Name are exactly the injected deps, in order (the M{Name, B}; src::B pattern). The injected types fix those parameters, so the reads stay type-stable.\n\nOverride either to do more — store deps into named fields, set non-dep fields, place Name in a different parameter position. Declare your override as a construct(::Type{M}, node, ::Val{Name}, deps…) method (below), or as member_schema(M)'s ctor= keyword (the ctor= overrides the method when both are given). The @component macro's @requires directive emits the override for you, threading the deps after the component's inline field defaults.\n\nnode is the node-core handle. An injected provider is constructed-but-unconfigured at this point (its configure runs later, in dependency-first order), so store it and use it from configure onward.\n\nrequires(::Type{Detector}) = (PoseSource,)                       # depend on an interface\n# default construct suffices for `Detector{Name, B}; src::B` — no method needed.\n\nrequires(::Type{Watch}) = (Sensor,)                             # …a sibling component, stored in a named field\nconstruct(::Type{Watch}, node, ::Val{Name}, s::Sensor) where {Name} = Watch{Name}(; sensor = s)\n\n\n\n\n\n","title":"ROSNode.construct"},{"category":"section","location":"advanced/delivery/#Message-Delivery","page":"Message Delivery","text":"When a sample arrives, a subscription decodes it and runs your handler. Two keyword options control that handoff: view chooses how much of the message you own, and concurrency chooses how many handlers run at once.","title":"Message Delivery"},{"category":"section","location":"advanced/delivery/#View-modes","page":"Message Delivery","text":"view chooses how your handler receives the message — a ladder from full safety to maximum throughput on Subscription. A Service takes a two-rung Boolean form:\n\nview = false (the default) owns the request.\nview = true borrows it zero-copy for the handler's duration.\n\nThe snippets below assume a live node and the generated message types (std_msgs.msg.String, sensor_msgs.msg.LaserScan) already in scope.\n\nSubscription(node, \"/chatter\", std_msgs.msg.String; view = Owned()) do msg\n    @info \"heard\" msg.data\nend\n\nOwned() (the default) decodes a fully-owned message — storable, forwardable to other tasks, spawnable, with no lifetime caveats. Pick this when the message outlives the handler: you keep it, hand it to another task, or @spawn work on it.\nChecked() (also written view = true) exposes a zero-copy CDRView aliasing the payload, valid for the handler's duration, with a runtime guard that throws BorrowError when the view escapes the handler. Pick this for high-rate streams you consume in place, while you still want the safety net.\nUnchecked() exposes the same zero-copy view and runs the handler without the borrow guard, for maximum throughput. Pick this once Checked() has proven the view stays put. An escaping view is undefined behavior.\n\nThe usual progression is to validate under Checked(), then switch to Unchecked() for the hot path.","title":"View modes"},{"category":"section","location":"advanced/delivery/#Concurrency","page":"Message Delivery","text":"concurrency controls how many handlers run at a time.\n\nSerial() (the default) delivers messages in order on one task, so a handler touches shared state without locks.\nParallel(n) fans delivery across n tasks; handlers run concurrently and synchronize shared state themselves.\n\nSubscription(node, \"/chatter\", std_msgs.msg.String; concurrency = Parallel(4)) do msg\n    process(msg)\nend","title":"Concurrency"},{"category":"section","location":"advanced/delivery/#Matching","page":"Message Delivery","text":"match decides which publishers a subscription accepts by type hash. It is a Subscription keyword.\n\nExactMatch() (the default, also written match = :exact) admits only publishers whose type hash matches the subscription's.\nWeakMatch() (also written match = :weak) additionally admits a publisher advertising the topic under a different or absent type hash, decoding best-effort.\n\nSubscription(node, \"/chatter\", std_msgs.msg.String; match = WeakMatch()) do msg\n    @info \"heard\" msg.data\nend","title":"Matching"},{"category":"section","location":"advanced/delivery/#Combining-them","page":"Message Delivery","text":"The two options compose. A hot, lock-free fan-out owns its own synchronization and reads each sample in place:\n\nSubscription(node, \"/scan\", sensor_msgs.msg.LaserScan;\n             view = Unchecked(), concurrency = Parallel(4)) do msg\n    accumulate!(msg)\nend","title":"Combining them"},{"category":"section","location":"advanced/delivery/#Quality-of-service","page":"Message Delivery","text":"Both Publisher and Subscription accept a qos = default_qos() profile that governs reliability and history. default_qos() carries the settings that match a stock ROS 2 endpoint, which suits most topics.","title":"Quality of service"},{"category":"section","location":"advanced/delivery/#Static-subscriptions-and-the-fast-path","page":"Message Delivery","text":"Both subscription flavors take the same view modes through the identical delivery leaf; they differ only in when the type is resolved:\n\na static subscription, Subscription(node, \"/topic\", T), resolves its type once at declaration, so the delivery leaf carries no per-sample type lookup;\na dynamic subscription resolves the type per sample — see Runtime Type Discovery.","title":"Static subscriptions and the fast path"},{"category":"section","location":"advanced/delivery/#See-also","page":"Message Delivery","text":"Topics\nRuntime Type Discovery","title":"See also"},{"category":"section","location":"advanced/delivery/#API-reference","page":"Message Delivery","text":"","title":"API reference"},{"category":"section","location":"advanced/delivery/#View-modes,-concurrency,-and-matching","page":"Message Delivery","text":"","title":"View modes, concurrency, and matching"},{"category":"section","location":"advanced/delivery/#Graph-introspection","page":"Message Delivery","text":"","title":"Graph introspection"},{"category":"section","location":"advanced/delivery/#Delivery-events","page":"Message Delivery","text":"","title":"Delivery events"},{"category":"type","location":"advanced/delivery/#ROSNode.ViewMode","page":"Message Delivery","text":"ViewMode\n\nSealed abstract supertype controlling how a subscription delivers each message to its handler — the single view= knob, on a safety-versus-speed curve:\n\nOwned (default; view=false) — a fully-owned message: storable, forwardable, and safe to spawn beyond the handler's return, with no lifetime caveats.\nChecked (view=true) — a zero-copy CDRView aliasing the payload, with a runtime escape guard: a view used after the handler returns throws BorrowError.\nUnchecked — the same zero-copy view with the guard removed: fastest and zero-allocation; an escaping view is undefined behavior.\n\nChecked and Unchecked borrow over the same representation, so validating under Checked exercises exactly what Unchecked runs.\n\n\n\n\n\n","title":"ROSNode.ViewMode"},{"category":"type","location":"advanced/delivery/#ROSNode.Owned","page":"Message Delivery","text":"Owned()\n\nDefault ViewMode: the handler receives a fully-owned message decoded out of the payload, with every field materialized. The message is free to store, forward, or pass to a spawned task beyond the handler's return — no borrow lifetime applies. view=false is shorthand for Owned().\n\n\n\n\n\n","title":"ROSNode.Owned"},{"category":"type","location":"advanced/delivery/#ROSNode.Checked","page":"Message Delivery","text":"Checked()\n\nZero-copy ViewMode with a runtime escape guard: the handler receives a CDRView aliasing the payload, invalidated the instant the handler returns. A CDRView or CDRString that escaped the handler throws BorrowError on its next access. The guard costs one allocation and borrows over the same representation Unchecked ships, so it validates exactly the code the unchecked path will run. view=true is shorthand for Checked(). The intended workflow: run under Checked(), confirm no BorrowError, then switch to Unchecked().\n\n\n\n\n\n","title":"ROSNode.Checked"},{"category":"type","location":"advanced/delivery/#ROSNode.Unchecked","page":"Message Delivery","text":"Unchecked()\n\nThe production fast-path ViewMode: a zero-copy CDRView over a bare isbits PayloadView, zero-allocation and with no escape checking. A CDRView or CDRString that outlives the handler is undefined behavior — it reads freed memory. Validate the same handler under Checked first; once it shows no BorrowError, switch to Unchecked() for the zero-alloc tier.\n\n\n\n\n\n","title":"ROSNode.Unchecked"},{"category":"type","location":"advanced/delivery/#ROSNode.Concurrency","page":"Message Delivery","text":"Concurrency\n\nSealed abstract supertype of the per-endpoint handler-dispatch policy: Serial (one sticky cooperative task, the default) or Parallel (up to n handlers on OS threads). Passed as the concurrency= keyword when declaring a subscription (or other handler endpoint). Mirrors the rclcpp executor choice between a single-threaded and a multi-threaded executor, scoped to a single endpoint.\n\nSee https://docs.ros.org/en/rolling/Concepts/Intermediate/About-Executors.html\n\n\n\n\n\n","title":"ROSNode.Concurrency"},{"category":"type","location":"advanced/delivery/#ROSNode.Serial","page":"Message Delivery","text":"Serial()\n\nThe default concurrency policy: one sticky worker task runs the endpoint's handlers one at a time, in arrival order, on a single OS thread (sticky, so it stays put even under julia -t N). Handlers run one at a time, so node-local handler state needs no locks or atomics; a blocking call inside a handler yields, letting the others make progress. The single-threaded-executor model from rclcpp.\n\n\n\n\n\n","title":"ROSNode.Serial"},{"category":"type","location":"advanced/delivery/#ROSNode.Parallel","page":"Message Delivery","text":"Parallel(n)\n\nDispatch up to n handlers concurrently on OS threads; completion order is independent of arrival order. n persistent non-sticky worker tasks pull from the subscriber, so n caps the in-flight handlers (and, under Checked / Unchecked views, the pinned payloads); actual parallelism is bounded by min(n, Threads.nthreads()). Opt a specific endpoint into this once its handler is thread-safe — the multi-threaded-executor model from rclcpp.\n\nParallel(Inf) spawns one task per message with no cap — an explicit foot-gun, never a default. n must be >= 1; Parallel(n) with n < 1 throws ArgumentError.\n\nSubscription(node, \"/scan\", LaserScan; concurrency=Parallel(4)) do msg\n    process(msg)   # up to 4 of these run at once, order independent of arrival\nend\n\n\n\n\n\n","title":"ROSNode.Parallel"},{"category":"type","location":"advanced/delivery/#ROSNode.MatchPolicy","page":"Message Delivery","text":"MatchPolicy\n\nSealed abstract supertype for how a subscription admits publishers of a topic — the match= knob:\n\nExactMatch (default; :exact) — admit only publishers whose type hash matches the subscription's exactly.\nWeakMatch (:weak) — also admit a publisher advertising the topic under a different (or absent) type hash, decoding best-effort.\n\n\n\n\n\n","title":"ROSNode.MatchPolicy"},{"category":"type","location":"advanced/delivery/#ROSNode.ExactMatch","page":"Message Delivery","text":"ExactMatch()\n\nDefault MatchPolicy: admit only publishers whose type hash matches the subscription's. :exact is shorthand for ExactMatch().\n\n\n\n\n\n","title":"ROSNode.ExactMatch"},{"category":"type","location":"advanced/delivery/#ROSNode.WeakMatch","page":"Message Delivery","text":"WeakMatch()\n\nMatchPolicy that also admits a publisher advertising the topic under a different or absent type hash, decoding best-effort. :weak is shorthand for WeakMatch().\n\n\n\n\n\n","title":"ROSNode.WeakMatch"},{"category":"function","location":"advanced/delivery/#ROSNode.endpoints","page":"Message Delivery","text":"endpoints(node_or_ctx; topic=nothing, kind=nothing, node=nothing,\n          namespace=nothing) -> Vector{EndpointInfo}\n\nReturn every endpoint in the discovery graph matching the given filters, the primary entry into the ROS 2 graph as this stack sees it. Each result is an EndpointInfo — the owning node, kind, topic, type, QoS, gid, and locality of one discovered endpoint.\n\nFilters combine conjunctively, and a nothing filter matches anything:\n\nFilter Matches\ntopic the resolved fully-qualified name; a relative name resolves against the node first, so endpoints(node; topic=\"chatter\") finds endpoints on /chatter\nkind an EndpointKind: Publisher, Subscription, Service, or Client\nnode the owning node's name\nnamespace the owning node's namespace\n\nThe query filters a snapshot of the index taken under the index lock, so it sees one consistent instant. Local entities are authoritative the instant they are declared (injected on construction); remote entities become visible as their Zenoh liveliness tokens arrive, which is why wait_for_service exists for code that must block until a peer appears.\n\nSee the ROS 2 discovery model: https://docs.ros.org/en/rolling/Concepts/Basic/About-Discovery.html\n\n# every remote publisher this node can see on /chatter\nfor pub in endpoints(node; topic=\"chatter\", kind=Publisher)\n    pub.is_local || @info \"remote pub\" node=pub.node_name type=pub.type\nend\n\n\n\n\n\n","title":"ROSNode.endpoints"},{"category":"function","location":"advanced/delivery/#ROSNode.publishers_info","page":"Message Delivery","text":"publishers_info(node, topic) -> Vector{EndpointInfo}\n\nReturn the publishers currently advertised on topic (resolved to its fully-qualified name), each an EndpointInfo — enough for a caller to run its own type and QoS compatibility check against its local endpoint. The ROS 2 get_publishers_info_by_topic equivalent; subscriptions_info is the dual.\n\n\n\n\n\n","title":"ROSNode.publishers_info"},{"category":"function","location":"advanced/delivery/#ROSNode.subscriptions_info","page":"Message Delivery","text":"subscriptions_info(node, topic) -> Vector{EndpointInfo}\n\nReturn the subscriptions currently present on topic (resolved to its fully-qualified name), each an EndpointInfo. The ROS 2 get_subscriptions_info_by_topic equivalent and the dual of publishers_info.\n\n\n\n\n\n","title":"ROSNode.subscriptions_info"},{"category":"function","location":"advanced/delivery/#ROSNode.count_publishers","page":"Message Delivery","text":"count_publishers(node, topic) -> Int\n\nReturn the number of publishers on topic (resolved to its fully-qualified name), equal to length(publishers_info(node, topic)). The ROS 2 count_publishers equivalent.\n\n\n\n\n\n","title":"ROSNode.count_publishers"},{"category":"function","location":"advanced/delivery/#ROSNode.count_subscribers","page":"Message Delivery","text":"count_subscribers(node, topic) -> Int\n\nReturn the number of subscriptions on topic (resolved to its fully-qualified name), equal to length(subscriptions_info(node, topic)). The ROS 2 count_subscribers equivalent.\n\n\n\n\n\n","title":"ROSNode.count_subscribers"},{"category":"function","location":"advanced/delivery/#ROSNode.topic_names_and_types","page":"Message Delivery","text":"topic_names_and_types(node_or_ctx; include_services=false) -> Dict{String, Vector{TypeInfo}}\n\nMap each topic (resolved fully-qualified name) to the distinct TypeInfos advertised on it across the graph, the ROS 2 get_topic_names_and_types equivalent. By default the map covers pub/sub topics; pass include_services=true to fold in service and client endpoints as well.\n\nTwo or more TypeInfo entries for one topic signal a type mismatch on the wire: endpoints differ either in type name or in RIHS01 hash (a version skew). Endpoints with no declared type (type === nothing) are omitted, since they carry no type to report.\n\nfor (topic, types) in topic_names_and_types(node)\n    length(types) > 1 && @warn \"mixed types on $topic\" types\nend\n\n\n\n\n\n","title":"ROSNode.topic_names_and_types"},{"category":"function","location":"advanced/delivery/#ROSNode.node_names","page":"Message Delivery","text":"node_names(node_or_ctx) -> Vector{NodeInfo}\n\nReturn the distinct nodes seen in the graph, the ROS 2 get_node_names_and_namespaces equivalent. Identity is recovered from the owning-node fields carried on each endpoint (a node advertises its own presence as a topic-less endpoint shell), deduplicated by (namespace, name).\n\nEndpoints with no node identity are skipped: the ros2dds bridge yields liveliness tokens whose parse_liveliness leaves the node blank, and there is no name to report for those.\nEach returned NodeInfo carries an empty enclave string: rmw_zenoh always writes the % (empty) sentinel into the token's enclave slot, so no non-default enclave survives the wire.\n\n\n\n\n\n","title":"ROSNode.node_names"},{"category":"function","location":"advanced/delivery/#ROSNode.service_is_ready","page":"Message Delivery","text":"service_is_ready(client) -> Bool\nservice_is_ready(node, service_name) -> Bool\n\nReturn true when a service server is present in the graph on the resolved service name, the ROS 2 Client.service_is_ready equivalent.\n\nClient form: reads the resolved name and owning node off the handle's Entity.\nName form: resolves service_name against the node.\nReadiness means a Service-kind endpoint exists on that name; local entities count, so our own service reads as ready the instant it is declared.\n\nReports graph liveliness: the same eventually-consistent view wait_for_service blocks on. For the stronger guarantee that Zenoh routing to the server has settled, await the client form of wait_for_service.\n\n\n\n\n\n","title":"ROSNode.service_is_ready"},{"category":"function","location":"advanced/delivery/#ROSNode.wait_for_service","page":"Message Delivery","text":"wait_for_service(client::ParameterClient; timeout=nothing) -> Bool\n\nBlock until the remote's parameter services are routing-matched, waiting on get_parameters as the representative service. Returns false on timeout — the same routing-plane wait as the ServiceClient form.\n\n\n\n\n\nwait_for_service(client::ServiceClient; timeout=nothing) -> Bool\nwait_for_service(node, service_name; timeout=nothing) -> Bool\n\nBlock the calling task until a matching service server is reachable, the ROS 2 Client.wait_for_service equivalent. Return true once ready, or false if timeout seconds elapse first (nothing waits forever). Raises ShutdownException if the Context begins draining while the task is parked, so a blocked wait cooperatively unwinds and lets the drain proceed.\n\nClient form: waits on a real routing match — the client's Querier has actually matched a server's queryable (service_matched), the strong guarantee that a subsequent call reaches a server.\nName form: has no querier handle, so it waits on graph liveliness (service_is_ready) — a Service endpoint on the resolved name; for a same-process server that becomes true the instant the service is declared, before routing settles.\n\nPrefer the client form whenever you hold the client.\n\nBoth forms park on the discovery change stream and re-check on each event, which is why they exist: remotes and routing are eventually-consistent, so a client created before its server must wait for it to appear.\n\nwait_for_service(client; timeout=5.0) || error(\"no server within 5s\")\nresp = call(client, request)\n\n\n\n\n\n","title":"ROSNode.wait_for_service"},{"category":"function","location":"advanced/delivery/#ROSNode.wait_for_action_server","page":"Message Delivery","text":"wait_for_action_server(client::ActionClient; timeout=nothing) -> Bool\n\nBlock the calling task until the action client is routing-matched to a server, the ROS 2 ActionClient.wait_for_server equivalent. Matching requires both the client's send_goal and get_result Queriers to have matched the server's queryables. Return true once matched, or false if timeout seconds elapse (nothing waits forever). Raises ShutdownException if the Context begins draining while parked.\n\nThe action analogue of the client form of wait_for_service, reusing the same routing-match machinery (lazily-declared Queriers plus action_server_matched). Both services must match because send retries discovery on its own, while fetch's get_result is a one-shot get with a long timeout — awaiting the get_result match keeps a send followed by fetch from blocking on an unmatched service. The cancel_goal and feedback channels are not gated: cancel is opt-in and feedback is an ordinary subscription.\n\n\n\n\n\n","title":"ROSNode.wait_for_action_server"},{"category":"function","location":"advanced/delivery/#ROSNode.wait_for_graph_change","page":"Message Delivery","text":"wait_for_graph_change(node_or_ctx; timeout=nothing) -> Bool\n\nBlock the calling task until the set of endpoints in the graph differs from the set observed at entry — an endpoint appeared or disappeared — the low-level wait behind the polling forms of the graph queries. Return true when a change is observed, or false if timeout seconds elapse (nothing waits forever). Raises ShutdownException if the Context begins draining while parked.\n\nThe predicate compares the index keyset before and after each wakeup, so a spurious notify (an in-place endpoint update with no add or remove) keeps waiting, and add/remove churn that restores the original keyset between wakeups coalesces away. For a push-based callback on every change, register an on_graph_change listener.\n\n\n\n\n\n","title":"ROSNode.wait_for_graph_change"},{"category":"function","location":"advanced/delivery/#ROSNode.on_graph_change","page":"Message Delivery","text":"on_graph_change(f, node_or_ctx) -> f\n\nRegister f(change) (change.added / change.removed :: Vector{EndpointInfo}) to fire on every discovery change. Returns f so it can be removed.\n\n\n\n\n\n","title":"ROSNode.on_graph_change"},{"category":"function","location":"advanced/delivery/#ROSNode.on_type_mismatch","page":"Message Delivery","text":"on_type_mismatch(f, node_or_ctx; scan=false) -> f\n\nRegister f(::TypeMismatch) to fire when one of our endpoints and a remote endpoint on the same topic carry incompatible types — a different type name, or the same name with a different RIHS01 version. Return f. The ROS 2 incompatible-type event surface.\n\nDetection runs as a listener on the discovery change stream: when an endpoint is added, it is compared against standing endpoints of the complementary kind (our subscription versus their publisher, and vice versa) on the same topic — a new remote against our locals, a new local endpoint against the remotes already discovered.\n\nf fires once per distinct mismatch — keyed by a (topic, kinds, reason) signature, edge-triggered with no time throttle — so a flapping or hot-per-sample topic fans out exactly once per signature.\nThe internal @warn is separately throttled (a logging concern only) and does not gate f.\nThe per-sample backstop for wildcard subscriptions (check_sample_type) shares the signature, so a mismatch seen both on the wire and in the graph fires f once.\nListeners run outside the detector lock; a throwing listener is logged and the others still run.\n\nDetection is change-driven and covers only endpoints that appear after registration:\n\nscan=false (default): a mismatch already standing in the graph when f is registered does not fire retroactively, so register before the endpoints exist to catch them.\nscan=true: also walks the standing graph snapshot once at registration and fires f for pre-existing mismatches (the watchdog/dashboard use case); the same per-signature edge-trigger applies, so a scanned mismatch is not fired again by a later change, and only the just-registered f receives the retroactive events.\n\non_type_mismatch(node) do tm\n    @error \"type mismatch\" topic=tm.local_endpoint.topic reason=tm.reason\nend\n\n\n\n\n\n","title":"ROSNode.on_type_mismatch"},{"category":"function","location":"advanced/delivery/#ROSNode.on_qos_incompatible","page":"Message Delivery","text":"on_qos_incompatible(f, node_or_ctx; scan=false) -> f\n\nRegister f(::QosIncompatible) to fire when one of our endpoints shares a topic with a remote endpoint whose QoS is incompatible with ours — under the DDS Request-vs-Offered rule, the offered profile ranks below the requested one on some policy. Return f. The ROS 2 incompatible-QoS event surface, covering reliability, durability, deadline, and liveliness (kind and lease).\n\nDetection runs on the discovery change stream and fires once per distinct signature, the same way on_type_mismatch does — edge-triggered, not time-throttled (only the internal @warn is throttled). The check runs in the DDS Request-vs-Offered direction for whichever role is local:\n\nLocal role Requested side Offered side\nSubscription or Client the local endpoint the remote\nPublisher or Service the remote the local endpoint\n\nListeners run outside the detector lock.\n\nDetection is change-driven:\n\nscan=false (default): an incompatibility already standing in the graph at registration does not fire retroactively, so register before the endpoints exist to catch them.\nscan=true: also walks the standing graph snapshot once at registration and fires f for pre-existing incompatibilities; as in on_type_mismatch the per-signature edge-trigger applies and only the just-registered f receives the retroactive events.\n\n\n\n\n\n","title":"ROSNode.on_qos_incompatible"},{"category":"function","location":"advanced/delivery/#ROSNode.on_message_lost","page":"Message Delivery","text":"on_message_lost(f, sub) -> f\n\nRegister f(::MessageLost) to fire when a gap opens in a publisher's per-message sequence numbers on subscription sub — one publisher's sequence number jumping by more than one between consecutive received messages. Return f. The ROS 2 message-lost event surface.\n\nRegistering the first listener flips a per-subscription gate so the dispatch path begins decoding each sample's metadata and feeding it to the detector (note_sequence!); with no listener the common path skips that decode entirely. Sequence tracking is per source gid, so a topic with several publishers is tracked independently per publisher. Reordered or duplicate messages (a sequence number slightly below the high-water mark) are ignored, matching best-effort delivery; a sequence number far below it is treated as a publisher restart and rebaselines tracking for that gid.\n\non_message_lost(sub) do ml\n    @warn \"dropped messages\" topic=ml.topic count=ml.count\nend\n\n\n\n\n\n","title":"ROSNode.on_message_lost"},{"category":"type","location":"advanced/delivery/#ROSNode.TypeMismatch","page":"Message Delivery","text":"TypeMismatch(local_endpoint, remote_endpoint, reason, suggestion)\n\nA detected type incompatibility between one of our endpoints and a remote endpoint on the same topic, delivered to on_type_mismatch listeners. local_endpoint and remote_endpoint are the two EndpointInfos involved. reason is :name (the two carry entirely different types — typically a wrong topic wiring) or :hash (same type name, different RIHS01 hash — a version skew that makes the remote's bytes decode-unsafe against our struct). suggestion is a human-facing fix hint.\n\n\n\n\n\n","title":"ROSNode.TypeMismatch"},{"category":"type","location":"advanced/delivery/#ROSNode.QosIncompatible","page":"Message Delivery","text":"QosIncompatible(local_endpoint, remote_endpoint, issues, suggestion)\n\nA detected QoS incompatibility between one of our endpoints and a remote endpoint on the same topic, delivered to on_qos_incompatible listeners. The two EndpointInfos are local_endpoint and remote_endpoint. issues::Vector{QosIncompatibility} lists the offending policies under the DDS Request-vs-Offered rule (computed by qos_compatible), and suggestion is a human-facing fix hint naming each offered-versus-requested gap.\n\n\n\n\n\n","title":"ROSNode.QosIncompatible"},{"category":"type","location":"advanced/delivery/#ROSNode.MessageLost","page":"Message Delivery","text":"MessageLost(topic, source_gid, expected_seq, got_seq, count)\n\nA detected gap in a publisher's per-message sequence numbers, delivered to on_message_lost listeners — the ROS 2 MessageLost event reconstructed from the rmwzenoh per-message sequence number. topic is the resolved topic; `sourcegidis the publisher whose stream gapped;expectedseqis the next sequence number we expected from it;gotseqis the one that arrived; andcountis the number of messages lost in between (gotseq - expectedseq`).\n\nROS 2 inherits this event from DDS; here it is reconstructed from the sequence_number the rmw_zenoh put attachment carries per message rather than from a DDS-native lost count.\n\n\n\n\n\n","title":"ROSNode.MessageLost"},{"category":"section","location":"advanced/authoring/#Authoring-Interfaces-in-Julia","page":"Authoring Interfaces in Julia","text":"A hand-written Julia definition becomes a fully registered ROS type. @ros_message, @ros_service, and @ros_action derive the ROS interface — real RIHS01 identity, keyexpr resolution, wire compatibility — straight from the Julia type you write.\n\n@ros_package names the package the types belong to, giving each one its wire identity. The macros derive the rest from what you write.","title":"Authoring Interfaces in Julia"},{"category":"section","location":"advanced/authoring/#Messages","page":"Authoring Interfaces in Julia","text":"A message is a struct. @ros_message turns each one into <pkg>/msg/<Name>, landing it at <pkg>.msg.<Name> with the bare name leaf-bound. Authored types compose: a struct that references another authored type by its Julia type resolves and hashes the nested ref into the parent's RIHS exactly as a .msg nested field would:\n\nmodule Msgs\n    using ROSNode\n    @ros_package \"demo_msgs\"\n    @ros_message struct Header;  stamp::Float64; frame::String;          end\n    @ros_message struct Chatter; header::Header; data::String; seq::Int32; end\nend\n\nChatter registers as demo_msgs/msg/Chatter. Publish and subscribe it by its Julia name; passing the type explicitly resolves it straight from the registry. The endpoint snippets below run inside a node from The Runtime Model:\n\npub = Publisher(node, \"/chatter\", Msgs.Chatter)\npublish(pub, Msgs.Chatter(\n    header = Msgs.Header(stamp = 1.0, frame = \"demo\"),\n    data   = \"hello world\",\n    seq    = Int32(0)))\n\nsub = Subscription(node, \"/chatter\", Msgs.Chatter) do msg\n    @info \"heard\" msg.data seq = msg.seq frame = msg.header.frame\nend","title":"Messages"},{"category":"section","location":"advanced/authoring/#Mapping-onto-an-existing-identity","page":"Authoring Interfaces in Julia","text":"The annotate form @ros_message \"pkg/msg/Name\" T keeps your Julia name T and gives it the wire type — and matching RIHS01 — of pkg/msg/Name, so T interoperates with the stock endpoint for that type. A one-field struct whose data::String matches the single string data field of std_msgs/msg/String derives the identical hash:\n\nmodule Msgs\n    using ROSNode\n    Base.@kwdef struct Chatter; data::String; end\n    @ros_message \"std_msgs/msg/String\" Chatter\nend\n\nThe annotate form registers the struct as written, so mark it Base.@kwdef yourself for keyword construction.","title":"Mapping onto an existing identity"},{"category":"section","location":"advanced/authoring/#Services","page":"Authoring Interfaces in Julia","text":"A service is a function: the parameters are the request fields, the @NamedTuple return type is the response, and the body is the handler. Throw to fail the call (the client's call then raises):\n\nmodule Srv\n    using ROSNode\n    @ros_package \"example_interfaces\"\n    @ros_service function AddTwoInts(a::Int64, b::Int64)::@NamedTuple{sum::Int64}\n        (sum = a + b,)\n    end\nend\n\nPass the authored function itself as the marker; ROSNode wires the request decode → handler → response encode from the registered service type. The client calls with the request fields as keyword arguments and receives the response as a NamedTuple:\n\nsrv = Service(node, \"/add_two_ints\", Srv.AddTwoInts)\n\nclient = ServiceClient(node, \"/add_two_ints\", Srv.AddTwoInts)\nif wait_for_service(client; timeout = 5)\n    resp = call(client; a = Int64(2), b = Int64(40))\n    @info \"response\" sum = resp.sum\nend","title":"Services"},{"category":"section","location":"advanced/authoring/#Actions","page":"Authoring Interfaces in Julia","text":"An action is a function with a FeedbackSink parameter: the goal fields are parameters, the FeedbackSink{@NamedTuple{…}} parameter is the feedback channel, and the @NamedTuple return type is the result. Calling the sink publishes a Feedback message and checkpoints cancellation. How the body ends drives the goal settlement three-way:\n\nreturn the result → :succeeded,\nlet a cancellation checkpoint in the sink throw → :canceled,\nthrow anything else → :aborted.\n\nmodule Act\n    using ROSNode\n    @ros_package \"action_tutorials_interfaces\"\n    @ros_action function Fibonacci(order::Int32,\n            fb::FeedbackSink{@NamedTuple{partial_sequence::Vector{Int32}}})::@NamedTuple{sequence::Vector{Int32}}\n        seq = Int32[0, 1]\n        for _ in 1:order\n            push!(seq, seq[end] + seq[end-1])\n            fb((partial_sequence = copy(seq),))\n        end\n        (sequence = seq,)\n    end\nend\n\nPass the authored function as the marker; ROSNode runs its body once per accepted goal and drives the accept/feedback/result protocol around it. The client sends a goal with its fields as keyword arguments and reads feedback as NamedTuples carrying partial_sequence:\n\nserver = ActionServer(node, \"/fibonacci\", Act.Fibonacci)\n\nclient = ActionClient(node, \"/fibonacci\", Act.Fibonacci)\nif wait_for_action_server(client; timeout = 5)\n    gh = send(client; order = Int32(8))       # blocks until the server accepts or rejects\n    for fb in feedback(gh)                     # yields each Feedback message; empty if the goal was rejected\n        @info \"feedback\" fb.partial_sequence\n    end\n    if state(gh) != :rejected\n        result = fetch(gh)                    # raises on a rejected goal\n        @info \"result\" result.sequence\n    end\nend","title":"Actions"},{"category":"section","location":"advanced/authoring/#Authoring-or-importing","page":"Authoring Interfaces in Julia","text":"An authored @ros_message/@ros_service/@ros_action carries the same RIHS01 as the equivalent .msg/.srv/.action, so it is wire-compatible with its imported twin and with ROS 2 nodes (see Interoperating with ROS 2). Author a type to keep its definition in Julia source alongside the code that uses it; import one to reuse a definition that already ships in a .msg/.srv/.action tree.","title":"Authoring or importing"},{"category":"section","location":"advanced/authoring/#See-also","page":"Authoring Interfaces in Julia","text":"Interface Types — importing existing interfaces and how wire identity works.\nRuntime Type Discovery — handling types you learn off the wire.","title":"See also"},{"category":"section","location":"advanced/authoring/#API-reference","page":"Authoring Interfaces in Julia","text":"","title":"API reference"},{"category":"macro","location":"advanced/authoring/#ROSNode.@ros_package","page":"Authoring Interfaces in Julia","text":"@ros_package \"pkg\"\n\nSet the module-local default ROS 2 package name for the authored macros (@ros_message, @ros_service, @ros_action). Expands to const __ros_package__ = \"pkg\" in the calling module, which those macros read so a package name is written once per module.\n\nTakes a string literal; any non-string argument is a macro-expansion error. The interface name then comes from the Julia struct/handler name and the qualifier (msg/srv/action) from which macro is used, giving the full ROS identity pkg/<qual>/Name. To override the package for one interface (a cross-package type, or a Julia name that differs from the ROS name), use a macro's explicit \"pkg/<qual>/Name\" form, which needs no @ros_package.\n\nmodule MyInterfaces\n    using ROSNode\n    @ros_package \"my_robot_msgs\"\n    @ros_message struct Pose; x::Float64; y::Float64; theta::Float64; end  # → my_robot_msgs/msg/Pose\nend\n\n\n\n\n\n","title":"ROSNode.@ros_package"},{"category":"macro","location":"advanced/authoring/#ROSNode.@ros_message","page":"Authoring Interfaces in Julia","text":"@ros_message struct Name … end          # define a new struct and register it\n@ros_message Name                        # mark an existing top-level struct\n@ros_message \"pkg/msg/Name\" struct Name … end   # explicit package (cross-package define)\n@ros_message \"pkg/msg/Name\" JuliaName    # explicit, when the Julia name ≠ the ROS name\n\nAuthor a Julia struct as the ROS 2 message pkg/msg/Name, the reverse of @ros_import: the struct is the source of truth and its RIHS01 type hash is computed by reflecting its fields. pkg comes from @ros_package, or from an explicit \"pkg/msg/Name\" first argument (which also serves cross-package definitions and renames). The explicit form holds to two constraints:\n\nthe qualifier must be msg\nin the define form the struct name must equal the ROS name\n\nA differently-named Julia struct binds through the annotate form.\n\nThe struct lives at pkg.msg.Name inside the calling module, where service and action sections can reach it via import ..msg: Name. The define form also binds the bare Name in the caller as a const alias; the annotate form adds the pkg.msg.Name alias around the user's existing top-level struct, which stays usable under its original name. Field types map per the reflection table:\n\nthe integer/float/Bool primitives and String map to their ROS scalars (UInt8 → uint8)\nVector{T} is an unbounded sequence\nan SVector/SArray/NTuple{N,T} is a fixed array [N]\na concrete nested struct becomes a reference whose own ROS name must already be registered (@ros_message or @ros_import it first) or be a vendored type with a ros_type_name\nChar, Dict, abstract, and parametric types have no ROS image and are rejected\n\nRegistration happens at module load: the macro pushes (Type, fqn) onto a module global and drains it with absorb_static_types! — right after the declaration in a script/REPL module, or from a generated __init__ in a precompiled package. The drain reflects each type to its IL, computes the real RIHS01, and folds the hash into the module's resolve table so a dynamic subscriber maps the wire RIHS back to this struct. Declare nested messages before the structs that reference them — the drain runs in source order and a struct's hash needs its nested types already registered.\n\n@ros_package \"my_robot_msgs\"\n@ros_message struct Vec3; x::Float64; y::Float64; z::Float64; end\n@ros_message struct Stamped; v::Vec3; label::String; n::Int32; end  # nested ref to Vec3\n\n\n\n\n\n","title":"ROSNode.@ros_message"},{"category":"macro","location":"advanced/authoring/#ROSNode.@ros_service","page":"Authoring Interfaces in Julia","text":"@ros_service function Name(field::T, …)::@NamedTuple{out::U, …} … end\n\nAuthor a ROS 2 service pkg/srv/Name from a Julia function: the argument list is the request fields and the @NamedTuple return annotation is the response fields (the return annotation is required). pkg comes from @ros_package. The macro generates the Name_Request and Name_Response section structs under pkg.srv (field types map per the @ros_message reflection table) and registers their RIHS01 hashes at module load.\n\nThe named function itself becomes the runtime identity. request_type/response_type are specialized on typeof(Name), so:\n\nService(node, ke, Name) serves it — the request's fields are splatted positionally into the handler and the returned @NamedTuple is rebuilt into a Name_Response (a value that is already a Name_Response is forwarded as-is).\nServiceClient(node, ke, Name) plus call(client; field=…) mirrors it, taking request fields by keyword and returning the response as a @NamedTuple.\n\nThe handler's two settlement paths, mapped from handler outcome through server to client:\n\na normal return — the returned named tuple is the reply-ok response.\na throw — the exception is caught and turned into a respond!(req, failed, message), which this package maps to a Zenoh query error reply under rmw_zenoh; the client raises a ServiceError carrying that message, so a blocked call never returns a plausible zeroed response.\n\n@ros_package \"example_interfaces\"\n@ros_service function AddTwoInts(a::Int64, b::Int64)::@NamedTuple{sum::Int64}\n    (sum = a + b,)\nend\n# server: Service(node, \"add_two_ints\", AddTwoInts)\n# client: call(ServiceClient(node, \"add_two_ints\", AddTwoInts); a=2, b=3)  # → (sum = 5,)\n\n\n\n\n\n","title":"ROSNode.@ros_service"},{"category":"macro","location":"advanced/authoring/#ROSNode.@ros_action","page":"Authoring Interfaces in Julia","text":"@ros_action function Name(goalfield::T, …, fb::FeedbackSink{@NamedTuple{ff::V, …}})::@NamedTuple{rf::W, …} … end\n\nAuthor a ROS 2 action pkg/action/Name from a Julia function. The arguments other than the FeedbackSink parameter are the Goal fields, the sink's @NamedTuple parameter gives the Feedback fields, and the @NamedTuple return annotation gives the Result fields. pkg comes from @ros_package; the return annotation and exactly one fb::FeedbackSink{…} parameter are required.\n\nThe macro generates eight structs under pkg.action — the Name_Goal/_Result/_Feedback sections (field types map per the @ros_message reflection table), then the five rosidl protocol wrappers Name_SendGoal_Request/_SendGoal_Response/_GetResult_Request/ _GetResult_Response/_FeedbackMessage — and registers all eight RIHS01 hashes nested-first at module load. The wrappers carry the vendored unique_identifier_msgs/UUID and builtin_interfaces/Time types, so their hashes match a ROS 2 peer's.\n\nThe named function is the runtime identity: ActionTypeSupport, _action_wrapper, and the feedback-parameter index are specialized on typeof(Name). ActionServer(node, ke, Name) serves it — the Goal's fields are splatted positionally into the handler and a concrete FeedbackSink{Name_Feedback} is injected at the recorded parameter position; the returned @NamedTuple is rebuilt into a Name_Result. ActionClient(node, ke, Name) plus send(client; field=…) dispatches a goal by keyword. Inside the handler, fb((ff=…,)) publishes one feedback message and acts as a cooperative cancellation checkpoint (it throws Cancelled if the goal is CANCELING); goal_handle(fb) recovers the server-side GoalHandle. How the handler ends the goal follows the respond! settlement three-way, specialized to action goals:\n\na normal return ⇒ SUCCEEDED\na propagating Cancelled ⇒ CANCELED\nany other thrown exception ⇒ ABORTED\n\n@ros_package \"my_robot_msgs\"\n@ros_action function Counter(target::Int32,\n        fb::FeedbackSink{@NamedTuple{at::Int32}})::@NamedTuple{total::Int32}\n    n = Int32(0)\n    while n < target\n        n += Int32(1)\n        fb((at = n,))            # publish feedback + cancellation checkpoint\n        sleep(0.02)\n    end\n    (total = n,)\nend\n\n\n\n\n\n","title":"ROSNode.@ros_action"},{"category":"type","location":"advanced/authoring/#ROSNode.FeedbackSink","page":"Authoring Interfaces in Julia","text":"FeedbackSink{F}\n\nThe feedback handle passed to an authored action handler (@ros_action). F is the action's generated Name_Feedback struct; the adapter constructs the sink around the live GoalHandle and injects it at the handler's fb::FeedbackSink parameter position.\n\nCalling it publishes one feedback message on the action's feedback topic and is a cooperative cancellation checkpoint, throwing Cancelled first if the goal is CANCELING (so a feedback loop is also a cancellation-observation loop). Two call forms:\n\nfb(nt::NamedTuple) builds an F from the named tuple by field name, then publishes.\nfb(msg::F) publishes an already-built Name_Feedback struct.\n\nA publish after the goal has settled is dropped. goal_handle recovers the underlying GoalHandle for the lower-level verbs (succeed, abort, state, respond!).\n\n\n\n\n\n","title":"ROSNode.FeedbackSink"},{"category":"function","location":"advanced/authoring/#ROSNode.goal_handle","page":"Authoring Interfaces in Julia","text":"goal_handle(fb::FeedbackSink) -> GoalHandle\n\nRecover the server-side GoalHandle that a FeedbackSink wraps, from inside an authored action handler (@ros_action). Use it to reach the lower-level goal verbs beyond the sink's surface: state(goal), iscancelled(goal), checkpoint(goal), and explicit settlement via succeed/abort/respond!.\n\n\n\n\n\n","title":"ROSNode.goal_handle"},{"category":"section","location":"communication/services/#Services","page":"Services","text":"A service is a request/response call: the client blocks for the reply. The handler settles each request once. A normal return replies success; a failure surfaces to the client as a ServiceError.","title":"Services"},{"category":"section","location":"communication/services/#Imported-interface","page":"Services","text":"@ros_import of a fully-qualified service binds the bare AddTwoInts namespace, exposing AddTwoInts.Request and AddTwoInts.Response. Reference the service by its .Request type — the Response resolves automatically (see Interface Types). The handler receives the decoded request and returns the response message:\n\nusing ROSNode\n\n@ros_import \"example_interfaces/srv/AddTwoInts\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"add_two_ints\")\n\n    srv = Service(node, \"/add_two_ints\", AddTwoInts.Request) do req\n        AddTwoInts.Response(sum = req.a + req.b)\n    end\n\n    spin(ctx; handle_signals = true)\nend","title":"Imported interface"},{"category":"section","location":"communication/services/#Server-options","page":"Services","text":"Service takes keywords that tune the route and the handler's execution:\n\nKeyword Default Controls\nqos default_qos() the route's ROS 2 QoS policies\nview false decode the request as a zero-copy CDRView aliasing the borrowed payload, valid only within the handler\nconcurrency Serial() handler scheduling — Serial() on the node's one cooperative thread, Parallel(n) on up to n OS threads\ndetach_timeout 60.0 seconds a detach!ed request may stay unsettled before a sweeper force-aborts it; 0 or Inf waits only on drain\nwarmup node policy warm-up policy that pre-JITs the decode → handler → encode chain\n\nThe two blocks run as a pair: the server above serves until Ctrl-C, and a second process runs the client below against it.\n\nThe client constructs a ServiceClient over the same request type, waits for a server to route-match, then calls. call blocks for the reply:\n\nusing ROSNode\n\n@ros_import \"example_interfaces/srv/AddTwoInts\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"add_two_ints\")\n    client = ServiceClient(node, \"/add_two_ints\", AddTwoInts.Request)\n\n    if wait_for_service(client; timeout = 5)\n        resp = call(client, AddTwoInts.Request(a = 2, b = 40); timeout_ms = 5000)\n        @info \"response\" sum = resp.sum   # → 42\n    end\n    close(client)\nend\n\nwait_for_service returns true once a server route-matches within the timeout, gating the call on a reachable server. For concurrent requests, call(client, req; async = true) returns a Task you fetch later, so several requests stay in flight at once.","title":"Server options"},{"category":"section","location":"communication/services/#Error-replies-—-the-settlement-three-way","page":"Services","text":"The server settles every request exactly once, and the handler reaches that settlement in one of three ways. This three-way is the same pattern action goals and the node lifecycle settle through, met here in its simplest form — step through it:\n\n<div class=\"rosnode-statechart\" data-machine=\"threeway\"></div>\n\nThe handler… Service outcome\nreturns a response reply-ok — the client's call returns it\ndeclines — respond!(req, failed, msg) a ServiceError reply, raised at the client's call\nthrows a synthesized error reply, raised at the client's call as a ServiceError\n\nAn action goal's canceled outcome maps to the throws branch of this three-way — a thrown Cancelled rather than a decline.","title":"Error replies — the settlement three-way"},{"category":"section","location":"communication/services/#Next","page":"Services","text":"Actions extend the request/response shape with progress feedback and cancellation.","title":"Next"},{"category":"section","location":"communication/services/#API-reference","page":"Services","text":"","title":"API reference"},{"category":"type","location":"communication/services/#ROSNode.ServiceHandle","page":"Services","text":"ServiceHandle{Req, Resp}\n\nA running service server for request type Req / response type Resp. Under rmw_zenoh a service is a Zenoh queryable: the request rides the query payload and the response rides the reply. The handle wraps an Entity and owns the queryable route plus the consumer task that drains it.\n\nConstruct one with the do-block spelling Service(node, name, SrvType) do req … end. isopen reports whether it is still serving; close undeclares the queryable, joins the consumer task within a bounded window (a wedged native call is detached and teardown proceeds), and reaps the entity. It also dies with its node when the node closes.\n\n\n\n\n\n","title":"ROSNode.ServiceHandle"},{"category":"type","location":"communication/services/#ROSNode.ServiceClient","page":"Services","text":"ServiceClient{Req, Resp}\nServiceClient(node, name, SrvType; qos=default_qos()) -> ServiceClient\n\nA service client for request type Req / response type Resp. Constructing one materializes a Client Entity (a liveliness token so servers discover it, plus graph tracking) and a long-lived Zenoh Querier on the service keyexpr. That querier is the transport every call queries through, and the handle behind service_matched and wait_for_service.\n\nThe querier targets :all_complete — every queryable declared complete — like rmwzenoh clients, so redundant servers on one service name see the same fan-out a native client produces; the first reply still settles a call. The client keyexpr carries the SERVICE-level type identity (service name plus service RIHS), matching the server's queryable under rmwzenoh. SrvType is the generated *_Request struct; the response is its *_Response sibling. qos maps onto the ROS 2 QoS policies.\n\nisopen reports whether the client is live; close reaps the querier, its match listener, and the entity, and the client dies with its node.\n\ncli = ServiceClient(node, \"add_two_ints\", AddTwoInts_Request)\nwait_for_service(cli)\nresp = call(cli, AddTwoInts_Request(; a = 2, b = 3))\n\n\n\n\n\n","title":"ROSNode.ServiceClient"},{"category":"function","location":"communication/services/#ROSNode.call","page":"Services","text":"call(client::ServiceClient, req; async=false, timeout_ms=60_000) -> Resp | Task\n\nInvoke the service: serialize req, query the client's Querier on the service keyexpr with the per-request rmw_zenoh attachment (sequence number, request-time wall clock, client gid), and resolve the single reply.\n\nSynchronously (async=false, the default) this blocks the calling task — it yields while the reply is in flight — and returns the decoded Resp. A failed invocation raises ServiceError:\n\nan error reply — the wire form of a handler failure;\na missing reply — no matching server;\na timeout.\n\nWith async=true the same query+drain runs on a spawned task, returned as a raw Task; fetch it for the Resp. A failed async call surfaces through fetch as a TaskFailedException wrapping the ServiceError — unwrap it (e.g. via current_exceptions(task)) to reach the original; the synchronous path does that unwrapping itself.\n\ntimeout_ms bounds the wait by arming a cancellation timer on the in-flight get; it defaults to 60_000 (60 s), so every call is bounded unless the caller opts out. For timeout_ms > 0 a Base.Timer cancels the get at the deadline, so the call resolves to a ServiceError rather than hanging; timeout_ms == 0 arms no timer and waits until the reply arrives or the Context drains.\n\nEach call runs over a pooled, reusable request/reply primitive (Zenoh.ReusableGet) declared on the client's Querier, so the per-call transport apparatus is reused rather than rebuilt — only the request encode, the reply decode, and (for a non-default timeout) a token clone allocate.\n\nThrows ArgumentError if client is closed or req is not the client's request type.\n\ncli = ServiceClient(node, \"add_two_ints\", AddTwoInts_Request)\nwait_for_service(cli)\nresp = call(cli, AddTwoInts_Request(; a = 2, b = 3); timeout_ms = 1000)\n@info \"sum\" resp.sum\n\n\n\n\n\n","title":"ROSNode.call"},{"category":"function","location":"communication/services/#ROSNode.respond!","page":"Services","text":"respond!(cell, status::SettlementStatus, payload) -> Bool\n\nThe single settlement verb. The terminal tokens — service success/failure, action succeeded/canceled/ aborted — perform the authoritative write: the first one wins, runs deliver, and latches the cell. Returns true if this call filled it.\n\nBehavior on an already-full cell depends on the path:\n\nexplicit second terminal respond! — the one hard error; it signals a double-settle bug.\nreturn-value and fail-safe paths — go through fill!, which silently ignores an already-full cell.\n\nfeedback is not terminal — it never touches the cell; the Action layer dispatches it to the feedback stream before reaching settlement.\n\n\n\n\n\nrespond!(req, status::SettlementStatus, payload) -> Bool\n\nThe service-handler form of the settlement verb (see respond! for the write-once cell contract; an action goal settles via respond!(goal, …)). This domain's two statuses:\n\nrespond!(req, failed, msg) (failed aliases failure) — a Zenoh query error reply carrying msg, so the client's call raises ServiceError;\nrespond!(req, success, resp) — replies resp.\n\nreq is the handler's argument; the result cell is taken from task-local storage, set for the handler's extent on the handler's own task.\n\nReturns true. Throws ArgumentError when called outside a service handler, or on an already-settled request (the double-settle error owned by respond!).\n\nService(node, \"validate\", Validate_Request) do req\n    req.value < 0 && return respond!(req, failed, \"value must be non-negative\")\n    Validate_Response(; ok = true)\nend\n\n\n\n\n\nrespond!(handle::ServiceRequestHandle, status::SettlementStatus, payload) -> Bool\n\nSettle a detached service request from a spawned task (see detach!). status/payload carry the same meaning as the in-handler respond!:\n\nrespond!(handle, success, resp) — replies-ok with the Resp;\nrespond!(handle, failed, msg) — a Zenoh query error reply carrying the message string.\n\nSettles the cell, then finalizes the owned Query (the final-ack) and drops the request from the detach registry. Returns true.\n\n\n\n\n\nrespond!(goal, status, payload) -> Bool\n\nSettle an action goal with a terminal status and the result payload, the action-layer form of the settlement verb (see respond! for the write-once cell contract). The action status set:\n\nsucceeded — the goal completed\ncanceled — an accepted cancel unwound the goal\naborted — the goal failed\n\nA single fill of the goal's ResultCell drives the first-wins latch, cache delivery, status publication, and get_result release. Returns true if this call filled the cell.\n\nrespond!(goal, feedback, fb) is the stream form (feedback! sugar), dispatched separately — it never touches the cell. The service tokens (success/failure) have no goal status and raise ArgumentError.\n\n\n\n\n\n","title":"ROSNode.respond!"},{"category":"function","location":"communication/services/#ROSNode.detach!","page":"Services","text":"detach!(req) -> ServiceRequestHandle\n\nTransfer settlement of the current service request off the handler frame. By default a service handler must settle its request by the time it returns (a still-empty cell is force-aborted); detach! opts out: the at-return abort is suppressed and the owned Query is held past handler return, so another task can settle it later. Returns a ServiceRequestHandle to pass to that task, which settles via respond!(handle, status, payload). Mirrors the action server's detached goal settlement.\n\nThe detached request is bounded by the service's detach_timeout (and by drain): if it is not settled by the deadline a sweeper force-aborts it with an @error, so a forgotten respond! cannot hang the client. One-shot — detach! on an already-detached request returns the existing handle.\n\nreq is the handler's argument; the cell and owning service are taken from the handler's task-local active-request slot. Throws ArgumentError when called outside a service handler.\n\nService(node, \"plan\", Plan_Request; detach_timeout = 30.0) do req\n    h = detach!(req)\n    Threads.@spawn respond!(h, success, solve(req))\n    nothing\nend\n\n\n\n\n\n","title":"ROSNode.detach!"},{"category":"type","location":"communication/services/#ROSNode.ServiceRequestHandle","page":"Services","text":"ServiceRequestHandle\n\nA first-class handle to a detached service request, returned by detach!. It carries the request's result cell (and its owning service) so a spawned task can settle the request via respond!(handle, status, payload) without the handler's task-local active-request slot, which the spawned task does not inherit. It is the service analog of an action GoalHandle. Single-reply: the handle settles exactly once (services have no feedback stream).\n\n\n\n\n\n","title":"ROSNode.ServiceRequestHandle"},{"category":"function","location":"communication/services/#ROSNode.service_matched","page":"Services","text":"service_matched(client::ServiceClient) -> Bool\n\nWhether the client's Querier is routing-matched to at least one service server right now — i.e. a call would actually reach a server. Reads the live Zenoh matching status of the querier; returns false on a closed client (its wire has been reaped).\n\nThis is the routing-plane signal behind the client form of wait_for_service. It is stronger than service_is_ready: for a same-process server, liveliness is authoritative the instant the service is declared (before Zenoh routing settles), whereas matching tracks actual reachability. Prefer this whenever you hold the client.\n\n\n\n\n\n","title":"ROSNode.service_matched"},{"category":"function","location":"communication/services/#ROSNode.request_type","page":"Services","text":"request_type(SrvType) -> Type\n\nThe request message type of a service SrvType. A generated service Foo lives as two sibling structs Foo_Request and Foo_Response under <pkg>.srv (there is no umbrella Foo type), so the service is named by its request struct: the default treats SrvType as that request struct (its name ends in _Request) and returns it unchanged.\n\nThe TypeRegistry specializes this for service markers: @ros_service emits a request_type method mapping the marker to its generated request type, so a registered service value resolves through the same call.\n\nThrows ArgumentError if SrvType is not a recognizable *_Request struct.\n\nPaired with response_type; both feed Service and ServiceClient construction.\n\n\n\n\n\n","title":"ROSNode.request_type"},{"category":"function","location":"communication/services/#ROSNode.response_type","page":"Services","text":"response_type(SrvType) -> Type\n\nThe response message type paired with request_type(SrvType). The default resolves request_type first, strips the _Request suffix from its name, and looks up the _Response sibling struct in the same module.\n\nThe TypeRegistry specializes this for service markers alongside request_type, so a registered service value resolves directly.\n\nThrows ArgumentError if the request type is unrecognizable, or if the _Response sibling is missing from the request's module (a partially generated service).\n\nThe response type is what a Service handler returns and what call decodes from the reply.\n\n\n\n\n\n","title":"ROSNode.response_type"},{"category":"type","location":"communication/services/#ROSNode.ServiceError","page":"Services","text":"ServiceError(msg)\n\nThrown by call when a service invocation fails: the server replied with a Zenoh query error (ROSNode signals a handler failure over Zenoh's query reply_err), no matching server replied, or the call timed out. A failed invocation always surfaces as this exception — a real reply is the only way call returns a response. msg carries the server's error string when there is one, otherwise a description of the missing or timed-out reply.\n\n\n\n\n\n","title":"ROSNode.ServiceError"},{"category":"section","location":"composition/lifecycle/#Component-Lifecycle","page":"Component Lifecycle","text":"A component that wraps an external resource — a device, a file, a connection — authors its setup and the matching release as lifecycle hooks. Every node runs the same lifecycle; the managed keyword on run chooses who drives it.","title":"Component Lifecycle"},{"category":"section","location":"composition/lifecycle/#The-hooks","page":"Component Lifecycle","text":"The five hooks are plain methods on the component type, each node-first (node, m) and defaulting to a no-op. Each fires on one transition:\n\nHook Transition Acquires / releases\nconfigure Unconfigured → Inactive acquire resources into the component's state\nactivate Inactive → Active open the working edge (managed nodes gate dispatch automatically)\ndeactivate Active → Inactive close the working edge\ncleanup Inactive → Unconfigured release what configure acquired\non_error a thrown transition recover the state a throwing hook left behind\n\nThe framework brackets the hooks with its own bookkeeping: a member's entities materialise immediately before its configure, its (paused) timers start immediately after a successful activate, and its entities close immediately after cleanup. A hook covers the resource-level state the framework leaves to you — everything beyond the bracketed bookkeeping above:\n\nimport ROSNode: configure, cleanup, member_schema\n\nmutable struct Recorder{Name} <: Component{Name}\n    io::Any                                  # nothing until configure opens it\nend\nRecorder{Name}() where {Name} = Recorder{Name}(nothing)\n@parameters struct RecorderParams\n    path::String = \"flight.log\"\nend\n\nconfigure(node, r::Recorder) = r.io = open(parameters(node, r).path, \"a\")\ncleanup(node, r::Recorder)   = r.io === nothing || close(r.io)\n\nfunction record(node, r::Recorder, msg::Telemetry)\n    println(r.io, msg.battery)\nend\nmember_schema(::Type{Recorder}) = component(Recorder, RecorderParams,\n    hears(:record, Telemetry, record; on = \"/vehicle/telemetry\"))","title":"The hooks"},{"category":"section","location":"composition/lifecycle/#Managed-and-unmanaged-nodes","page":"Component Lifecycle","text":"managed selects who drives the transitions:\n\nUnmanaged (the default). run / add! brings the node straight up: construction runs every member's configure, then every member's activate. cleanup runs at teardown (see Ordering and idempotent cleanup for the full trigger set).\nManaged (managed = true). The node declares the lifecycle control surface (the five lifecycle_msgs services plus the ~/transition_event topic) and starts Unconfigured. An external orchestrator (ros2 lifecycle set …) or in-process calls drive the transitions; autostart = true runs configure! then activate! during construction.\n\nvehicle = run(Vehicle; ctx = ctx, name = \"vehicle\", managed = true, block = false)\nln = ROSNode.lifecycle(vehicle)\nconfigure!(ln)      # entities materialise, each member's configure runs\nactivate!(ln)       # timers start, dispatch gating lifts\ndeactivate!(ln)     # members deactivate in reverse order, gating drops back\ncleanup!(ln)        # each cleanup runs, entities close — a configure! starts fresh\n\nThese calls and a ros2 lifecycle orchestrator drive one machine over ~/change_state. Each transition runs the members' hooks under the settlement three-way. Click a transition to step through it:\n\n<div class=\"rosnode-statechart\" data-machine=\"lifecycle\"></div>","title":"Managed and unmanaged nodes"},{"category":"section","location":"composition/lifecycle/#The-dispatch-gate","page":"Component Lifecycle","text":"A managed node holds each port shut at dispatch until it reaches Active:\n\nPort Behavior while not Active\nPublisher the publish drops\nSubscription, timer the reaction stays quiet\nService the request gets an error reply\nAction server goal, cancel, and result requests get an error reply; feedback and status publications drop\nAction client (send/fetch/cancel) the call raises NodeInactiveError — probe isactive(node) first\n\nThe gate is why most components leave activate/deactivate as no-ops: those hooks carry only work beyond the gating — pre-rolling a device, flushing a buffer. deactivate goes one step further: it cooperatively cancels any goals still in flight, so a node bounced to Inactive stops working against state the orchestrator may have invalidated. Each executing body sees the cancel signal:\n\na body that honors it settles CANCELED;\na body that ignores it runs detached, after a bounded wait.\n\nThe control surface, the parameter services, and ~/get_type_description stay live throughout, so an orchestrator drives transitions and tunes parameters on an inactive node.\n\nTwo ports sit outside the gate:\n\nThe service client issues every call regardless of state and blocks to its own timeout; guard it on isactive(node) when that matters.\nAn unmanaged node has no gate at all: it delivers from the moment a subscription materialises. A message can reach record before configure assigns r.io, so guard the field in the handler, or run the node managed, when that window matters.","title":"The dispatch gate"},{"category":"section","location":"composition/lifecycle/#Failure-and-recovery","page":"Component Lifecycle","text":"A hook signals failure two ways:\n\nReturning the failure token rolls the transition back to its pre-transition state. The fan-out stops at the failing member, and the members that already ran the forward hook unwind in reverse dependency order:\na failed activate! deactivates them and lands Inactive;\na failed configure! cleans them up, closes their ports, and lands Unconfigured.\nThe transition reports :failure and skips error processing entirely — only a thrown hook enters recovery.\nThrowing diverts a managed node into error processing:\n\n<div class=\"rosnode-statechart\" data-machine=\"recovery\"></div>\n\non_error runs across the members in dependency order, then:\n\nevery member returns cleanly → the node recovers to Unconfigured;\nan on_error throws → the node drops to Finalized (the throw is logged and the remaining members still run).\n\nError processing then runs the guarded member-cleanup fan-out — each member's cleanup plus its port close — so recovery to Unconfigured is a true reset: a later configure! reacquires from scratch, with no stale entities still dispatching and no double-acquire. An unmanaged node has no error-processing state — run / add! tears the partial node down (every member that reached configure cleans up against its partial state, entities close) and the throw propagates.","title":"Failure and recovery"},{"category":"section","location":"composition/lifecycle/#Ordering-and-idempotent-cleanup","page":"Component Lifecycle","text":"In a multi-member node the hooks fan out in dependency order: configure, activate, and on_error run providers first; deactivate and cleanup run in reverse. The Sensor in Components configures before the Guard it feeds, and cleans up after it.\n\ncleanup runs at most once per configure. Teardown has several triggers — explicit close, a container's unload_node (see Containers & Dynamic Composition), the shutdown transition, the Context drain — and a node can see more than one. The member's materialised entities are the guard: the first trigger runs cleanup, later ones are no-ops. The framework logs a throwing cleanup and continues, so the member's entities still close and the remaining members still clean up.","title":"Ordering and idempotent cleanup"},{"category":"section","location":"composition/lifecycle/#API-reference","page":"Component Lifecycle","text":"","title":"API reference"},{"category":"function","location":"composition/lifecycle/#ROSNode.configure","page":"Component Lifecycle","text":"configure(node, m::M)\n\nComponent hook you override to acquire resources (open devices, load models) from parameters read via parameters. Default no-op on Component. Runs at the configure transition, driven by configure!. cleanup is the paired release. Like the reactions, hooks are node-first — the node is the first argument.\n\nMember ports are materialised immediately before this hook, so entities is available. Override it by importing or qualifying the generic.\n\n\n\n\n\n","title":"ROSNode.configure"},{"category":"function","location":"composition/lifecycle/#ROSNode.activate","page":"Component Lifecycle","text":"activate(node, m::M)\n\nComponent hook you override to start streaming or enable behavior. Default no-op on Component. Runs at the activate transition, driven by activate!.\n\nThe member's (paused) timers start immediately after this hook returns successfully (so a tick cannot fire mid-activate or into a member whose activate failed); the isactive dispatch gate then opens and ports go live.\n\n\n\n\n\n","title":"ROSNode.activate"},{"category":"function","location":"composition/lifecycle/#ROSNode.deactivate","page":"Component Lifecycle","text":"deactivate(node, m::M)\n\nComponent hook you override to pause behavior. Default no-op on Component. Runs at the deactivate transition, driven by deactivate!.\n\nPorts stay materialised; the isactive dispatch gate closes, silencing publishes, subscription dispatch, timer ticks, and service dispatch until the node is Active again.\n\n\n\n\n\n","title":"ROSNode.deactivate"},{"category":"function","location":"composition/lifecycle/#ROSNode.cleanup","page":"Component Lifecycle","text":"cleanup(node, m::M)\n\nComponent hook you override to release what configure acquired (close devices, free handles). Default no-op on Component. Runs at the cleanup/shutdown transition, driven by cleanup! / shutdown!.\n\nFramework guarantees:\n\nFires at most once per configure, guarded on the member's materialised ports — only for a configured member, and a second teardown trigger is a no-op.\nThe framework closes the member's port handles after it.\nA throw is caught and logged, so one member's failure does not block the others' teardown.\nMaterialisation precedes configure, so a member whose own configure threw still has its cleanup run — write it to tolerate partially-acquired state (e.g. guard m.handle === nothing).\n\n\n\n\n\n","title":"ROSNode.cleanup"},{"category":"function","location":"composition/lifecycle/#ROSNode.on_error","page":"Component Lifecycle","text":"on_error(node, m::M)\n\nComponent hook you override to recover or reset member state. Default no-op on Component. Runs at the error-processing step that a throwing transition driver (configure! / activate! / deactivate! / cleanup!) enters.\n\nA throw here is caught and logged, so the remaining members still recover — but any member's throw means recovery failed and the node lands Finalized. On a recovered error the framework then runs the cleanup fan-out — each member's cleanup runs and its ports close — before the node lands Unconfigured, releasing whatever configure acquired. On the Finalized path that fan-out is deferred until the node is closed.\n\n\n\n\n\n","title":"ROSNode.on_error"},{"category":"section","location":"communication/parameters/#Parameters","page":"Parameters","text":"A parameter is a named, typed, live configuration value a node exposes. Peers read it and set it at runtime, and the node sees each change applied to its own typed state.\n\nA @parameters schema is the shared contract across the two ends, the way a message or service type is shared across its endpoints. The server bakes the schema in; a client uses it as a typed lens onto the remote.","title":"Parameters"},{"category":"section","location":"communication/parameters/#The-schema","page":"Parameters","text":"@parameters declares the schema as a struct. Each field carries a type, a default, and optional metadata:\n\nusing ROSNode\n\n@parameters struct DemoParams\n    \"maximum speed\"  max_speed::Int64 = 50 ∈ 0..100\n    \"planner mode\"   mode::String = \"auto\" ∈ (\"auto\", \"manual\")\nend\n\nThe annotations on each field shape how peers may drive it:\n\nA leading string literal is the field's description, surfaced to parameter tooling.\n∈ lo..hi constrains a numeric field to that range.\n∈ (a, b, c) constrains a field to that choice set.\n|> readonly blocks runtime sets; startup overrides still apply.\n\nLegal field types are the ROS 2 parameter value set:\n\nScalars: Bool, Int64, Float64, String, Symbol (string-with-choices sugar)\nArrays: Vector{Bool}, Vector{Int64}, Vector{Float64}, Vector{String}, Vector{UInt8} (the byte array)\n\nThe macro generates an immutable struct, so reads stay type-stable.","title":"The schema"},{"category":"section","location":"communication/parameters/#The-server","page":"Parameters","text":"Node(ctx, name, Schema) attaches a typed parameter server at node.parameters and:\n\nwires the six standard rcl_interfaces parameter services\nwires /parameter_events\nmakes the node driveable by any ROS 2 parameter client\n\noverrides overlays startup values onto the schema defaults; sources are CLI, launch, or YAML:\n\nnode = Node(ctx, \"client_demo_server\", DemoParams; overrides = (max_speed = 50,))\n@info \"serving\" max_speed = node.parameters.max_speed mode = node.parameters.mode\n\nnode.parameters is the ParameterServer{DemoParams}; field access forwards to the live DemoParams value, reading the current setting, typed.","title":"The server"},{"category":"section","location":"communication/parameters/#The-client","page":"Parameters","text":"ParameterClient drives a node's parameters from another node, in one of two modes:\n\nPass the schema to share it as a typed lens, so fetch and transaction are type-stable.\nDrop the schema to drive a node by its parameter services alone, where values come back dynamically as Any/NamedTuple.\n\nnode   = Node(ctx, \"client_demo\")\nclient = ParameterClient(node, \"/client_demo_server\", DemoParams)\n\nwait_for_service(client; timeout = 5) || return   # false on timeout, e.g. no server up\n\nwait_for_service returns once the parameter services are routing-matched, gating the calls below to a live server.","title":"The client"},{"category":"section","location":"communication/parameters/#The-low-level-verbs","page":"Parameters","text":"Three verbs map one-to-one onto the rcl_interfaces services:\n\nget_parameters(client, [:max_speed])                       # → Any[50]\nget_parameter_types(client, [:max_speed])                  # → [PARAMETER_INTEGER]\nok, reason = set_parameters_atomically(client, [:max_speed => 80])\n\nset_parameters_atomically returns (ok::Bool, reason::String): the server applies the whole batch together, and reason carries the explanation behind a rejection.","title":"The low-level verbs"},{"category":"section","location":"communication/parameters/#The-Julian-structure","page":"Parameters","text":"fetch materializes the whole remote into the shared schema in one round trip:\n\np = fetch(client)        # ::DemoParams\n@info \"snapshot\" max_speed = p.max_speed mode = p.mode\n\ntransaction mutates the remote with a do-block over a local draft. ROSNode validates the draft locally, then pushes the whole set atomically:\n\ntransaction(client) do d\n    d.max_speed = 8\n    d.mode      = \"manual\"\nend\n\nset_parameter! sets a single field. An out-of-range value raises ParameterRejection, the same exception a local server set raises, so the failure contract is uniform across local and remote:\n\nset_parameter!(client, :max_speed, 999)\n\non_parameter_event watches the remote's changes, firing the do-block on each batch the server publishes to /parameter_events:\n\non_parameter_event(client) do batch\n    @info \"changed\" batch.changed\nend","title":"The Julian structure"},{"category":"section","location":"communication/parameters/#Cross-field-validation","page":"Parameters","text":"Per-field constraints cover one field at a time. For rules spanning several fields, extend ROSNode.validate on the schema and throw ParameterRejection when a candidate violates them:\n\nROSNode.validate(p::DemoParams) =\n    p.mode == \"manual\" && p.max_speed > 50 &&\n        throw(ParameterRejection(\"manual mode caps max_speed at 50\"))\n\nThe hook runs on every set, server-side and through a client transaction, mirroring ROS 2's add_on_set_parameters_callback. A throw rejects the whole atomic set.","title":"Cross-field validation"},{"category":"section","location":"communication/parameters/#Running-it","page":"Parameters","text":"The rcl_interfaces parameter types are vendored, so a parameter example is self-contained: it needs only a router and a stock Julia environment. See Getting Started for the router. A live server is also driveable by ros2 param get/set against a matching router; see Interoperating with ROS 2.\n\nA parameter also attaches to a component: @param declares one of a component's public parameters — its live, ros2 param-driveable data, separate from the private state its struct holds. See Components.","title":"Running it"},{"category":"section","location":"communication/parameters/#See-also","page":"Parameters","text":"Interoperating with ROS 2 — drive a node's parameters from the ros2 param CLI.","title":"See also"},{"category":"section","location":"communication/parameters/#API-reference","page":"Parameters","text":"","title":"API reference"},{"category":"macro","location":"communication/parameters/#ROSNode.@parameters","page":"Parameters","text":"@parameters struct P\n    \"doc\"  field::T = default [∈ lo..hi | ∈ (choices…)] [|> readonly]\n    …\nend\n\nDeclare a typed parameter schema (ROS 2 node parameters). Generates an immutable struct P so declared reads are type-stable, a keyword constructor P(; field=default, …) that overlays startup overrides (CLI/launch/YAML) onto the defaults and coerces each to its field type, and a descriptors(::Type{P}) method the parameter services reflect over.\n\nLegal field types are the ROS 2 ParameterValue set: Bool, Int64, Float64, String, Symbol (string-with-choices sugar), and Vector of those plus Vector{UInt8} byte arrays. An illegal field type is caught lazily: the struct expands and constructs, and ArgumentError surfaces when descriptors(P) is first evaluated — i.e. when the parameter services reflect over the schema. Parametric structs are rejected at expansion.\n\nPer-field annotations, on the right of the =:\n\na leading string literal is the field description;\n∈ lo..hi is a numeric range constraint, stored as the tuple (lo, hi);\n∈ (a, b, …) (or a vector) is a choice set, stored as an untagged tuple;\nedge case: a choice set of exactly two numbers shares the range representation and is enforced as a range;\n|> readonly marks the field read-only, blocking runtime sets while still permitting a startup override.\n\nDefine ROSNode.validate(p::P) (default no-op) to add cross-field rules — ROS 2's add_on_set_parameters_callback, expressed as a method on the whole candidate. Pair the schema with ParameterServer (or Node(ctx, name, P)) to expose the six standard parameter services.\n\nSee the ROS 2 parameters concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Parameters.html\n\n@parameters struct PlannerParams\n    \"max linear speed (m/s)\"\n    max_speed::Float64 = 1.0 ∈ 0.0..10.0\n    mode::Symbol = :auto ∈ (:auto, :manual)\n    robot_id::Int64 = 7 |> readonly\nend\n\n\n\n\n\n","title":"ROSNode.@parameters"},{"category":"type","location":"communication/parameters/#ROSNode.ParameterServer","page":"Parameters","text":"ParameterServer(node, initial::P; allow_undeclared=false) -> ParameterServer{P}\nParameterServer{P}(node; overrides=(;), allow_undeclared=false) -> ParameterServer{P}\n\nThe parameter subsystem for one node, parameterized on a @parameters schema P (ROS 2 node parameters). It owns the live schema value and applies sets directly; ParameterClient is its remote dual, driving another node's parameter services asynchronously and fallibly over the wire.\n\nThe live schema value sits behind an @atomic field swapped whole on every commit, so a concurrent reader sees a complete old-or-new value and rollback is free. Read a declared field with server.field (type-stable). Mutate through transaction, the single-statement sugar server.field = v, setproperties!, or set_parameter! — all route through one commit path under the server's mutation lock.\n\nUndeclared parameters live in a separate Any-typed side dict reached via dynamic_parameters, gated by allow_undeclared (default off); with the gate off, a dynamic read or write raises ArgumentError.\n\nTwo constructor forms set the initial value:\n\ntwo-argument — takes an already-built initial::P.\n{P}-keyword — builds the initial value by overlaying overrides (startup CLI/launch/YAML) onto the schema defaults.\n\nA startup override of a read_only field is allowed — read-only blocks only runtime sets.\n\nTypically reached as node.parameters. The six standard parameter services and /parameter_events are wired generically over P by wire_parameter_services! (done for you by Node(ctx, name, P)).\n\nImplements the ROS 2 parameter model: https://docs.ros.org/en/rolling/Concepts/Basic/About-Parameters.html\n\n\n\n\n\n","title":"ROSNode.ParameterServer"},{"category":"type","location":"communication/parameters/#ROSNode.ParameterClient","page":"Parameters","text":"ParameterClient(node, target) -> ParameterClient{Nothing}\nParameterClient(node, target, ::Type{P}) -> ParameterClient{P}\n\nA client for the parameters of the remote node at FQN target (e.g. \"/planner\"). The dual of the local node.parameters server: it drives the remote's six standard parameter services and /parameter_events over the wire.\n\nSee the ROS 2 parameter concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Parameters.html\n\nA @parameters schema P (the same struct the remote baked in via Node(ctx, name, P)) selects the client kind:\n\nClient kind P fetch(client) returns usable with\nTyped lens a @parameters struct a type-stable P; transaction(client) do p … end mutates it type-stably a remote sharing schema P\nDynamic omitted (Nothing) a NamedTuple (values Any) an arbitrary ROS 2 node\n\nThe underlying ServiceClients are built lazily per service and reaped by close(client). Calls are fallible: a timeout or error reply raises ServiceError (and a rejected set raises ParameterRejection). Call wait_for_service first so the route is matched before the opening request.\n\n\n\n\n\n","title":"ROSNode.ParameterClient"},{"category":"type","location":"communication/parameters/#ROSNode.CompositeParameterServer","page":"Parameters","text":"CompositeParameterServer(node, members) -> CompositeParameterServer\n\nThe node-level parameter view of a composed node: a façade over the members' per-mixin ParameterServers that presents one flat, member-prefixed ros2 param namespace (<member>.<field>, dot-separated as ROS 2 nests parameters), one /parameter_events stream, and one set of the six standard parameter services. members is a Vector{Pair{Symbol,ParameterServer}} of member-name => its server in declared order — the order ros2 param list reflects. Reached as node.parameters for a composed node.\n\nEach member keeps its own typed ParameterServer{P_M} (so a member's own parameters(node, m) stays type-stable); the façade is only the node-level aggregate. It reuses the schema-independent service core by implementing the same six reflection handlers plus parameter_names. It has no node-level dynamic tier — dynamic_parameters on it raises ArgumentError, directing you to a member's own server. A node-level atomic set (set_parameters_atomically):\n\nvalidates every affected member first;\ncommits only if all pass;\ncoalesces the per-member commits into one /parameter_events.\n\n\n\n\n\n","title":"ROSNode.CompositeParameterServer"},{"category":"type","location":"communication/parameters/#ROSNode.ParameterDescriptor","page":"Parameters","text":"ParameterDescriptor\n\nPer-field metadata for one declared parameter (ROS 2 rcl_interfaces/msg/ParameterDescriptor): its name::Symbol, Julia type, ParameterType tag ptype, human description, optional constraint, read_only flag, and schema default. Built by @parameters and reflected over by the parameter services (describe_parameters, get_parameter_types, …).\n\nconstraint is one of:\n\nnothing — no bound.\n(lo, hi) — a numeric range tuple.\na choices tuple — the allowed values, the ∈ choices form. Untagged, so a choice set of exactly two numbers is indistinguishable from a range and is validated as one.\n\nWire encoding loses the structured constraint and the default, so a descriptor decoded by a remote ParameterClient carries:\n\nconstraint === nothing — the structured constraint collapses to the human additional_constraints string.\ndefault === nothing — the default is not transmitted.\n\n\n\n\n\n","title":"ROSNode.ParameterDescriptor"},{"category":"type","location":"communication/parameters/#ROSNode.ParameterType","page":"Parameters","text":"ParameterType\n\nThe rcl_interfaces/msg/ParameterType tag enum, UInt8-backed so it marshals straight onto the wire type byte. PARAMETER_NOT_SET (0) is the unset/unknown sentinel — never stored for a declared field, and what an unknown parameter name or unrecognized wire tag reads back as. The remaining arms mirror ROS 2's ParameterValue union: PARAMETER_BOOL, PARAMETER_INTEGER, PARAMETER_DOUBLE, PARAMETER_STRING, PARAMETER_BYTE_ARRAY, and the four *_ARRAY arms.\n\nget_parameter_types returns these, and a ParameterDescriptor carries one as its ptype.\n\nSee the ROS 2 parameter concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Parameters.html\n\n\n\n\n\n","title":"ROSNode.ParameterType"},{"category":"function","location":"communication/parameters/#ROSNode.parameter_type","page":"Parameters","text":"parameter_type(::Type) -> ParameterType\n\nThe rcl_interfaces/msg/ParameterType tag for a declared field type. Symbol maps to STRING (string-with-choices sugar). Throws ArgumentError for anything outside the legal value set; the generated descriptors body calls this, so an illegal field type surfaces when descriptors are first built.\n\n\n\n\n\n","title":"ROSNode.parameter_type"},{"category":"function","location":"communication/parameters/#ROSNode.descriptors","page":"Parameters","text":"descriptors(::Type{P}) -> Vector{ParameterDescriptor}\ndescriptors(value) -> Vector{ParameterDescriptor}\n\nThe ordered per-field metadata for a @parameters schema P — the reflection root the parameter services read. @parameters generates the ::Type{P} method; the value form forwards to descriptors(typeof(value)). A type with no generated method (a plain struct used as a schema) gets an empty descriptor list.\n\n\n\n\n\n","title":"ROSNode.descriptors"},{"category":"function","location":"communication/parameters/#ROSNode.validate","page":"Parameters","text":"validate(candidate::P)\n\nUser hook for cross-field parameter rules — ROS 2's add_on_set_parameters_callback, expressed as a method on the whole candidate value. The default is a no-op. Define ROSNode.validate(p::PlannerParams) = … to override, throwing ParameterRejection (or any exception) to reject the candidate. The hook runs inside the commit, against the fully-assembled candidate, after the per-field read-only and constraint checks. A local internal caller sees the throw; an external ParameterClient or ROS 2 client sees a rejected SetParametersResult (successful=false).\n\n\n\n\n\n","title":"ROSNode.validate"},{"category":"function","location":"communication/parameters/#ROSNode.transaction","page":"Parameters","text":"transaction(f, server::ParameterServer{P}) -> P\ntransaction(server::ParameterServer{P}) do p … end -> P\ntransaction(f, client::ParameterClient{P}; timeout_ms=2000) -> P\n\nThe parameter mutation primitive. Runs f(draft) against a mutable draft of the current parameters: assignments record pending overrides that reads inside the block observe, while nothing live is touched and nothing is published until commit.\n\nFor a ParameterServer the call holds the server's single mutation lock for the block's whole duration, so transactions serialize (the consistency boundary the foreign-thread dispatch relies on). On clean exit the commit pipeline runs in order:\n\nassemble the candidate via setproperties;\nvalidate it as a whole (the per-field read-only gate and constraints, then the user validate hook);\nswap it in with one atomic whole-struct store;\npublish one batched /parameter_events.\n\nAny throw aborts with free rollback — the live cell never held a partial state. Returns the committed schema value. A constraint, read-only, or validate violation raises ParameterRejection, which unwinds the lock with nothing committed.\n\nFor a ParameterClient the same do-block shape drives a remote: f runs against a draft of the fetched current value, the candidate is checked locally first for fast feedback, then pushed as one set_parameters_atomically. The local pre-check covers field constraints, the read-only gate, and any validate method defined in the client process; rules defined only on the remote are enforced by the remote's authoritative re-validation. Outcomes:\n\nsuccess — returns the new P.\nrejected set — raises ParameterRejection.\nschemaless ParameterClient{Nothing} — raises ArgumentError; use set_parameters_atomically instead.\n\ntransaction(node.parameters) do p\n    p.max_speed = 9.0\n    p.mode = :manual\nend\n\n\n\n\n\n","title":"ROSNode.transaction"},{"category":"function","location":"communication/parameters/#ROSNode.setproperties","page":"Parameters","text":"setproperties(base::P, overrides::NamedTuple) -> P\nsetproperties(base::P, overrides::P) -> P\n\nThe value-level rebuild primitive the transaction commit and the setproperties! sugar build on: a pure rebuild of an immutable schema value with overrides applied. The overrides argument type selects the rebuild:\n\noverrides behavior\nNamedTuple partial overlay — its named fields change, the rest copy from base — rebuilt through P's keyword constructor, so each field is re-coerced to its declared type\nP replace-all: the result is overrides itself\n\n\n\n\n\n","title":"ROSNode.setproperties"},{"category":"function","location":"communication/parameters/#ROSNode.setproperties!","page":"Parameters","text":"setproperties!(server::ParameterServer{P}, overrides::NamedTuple) -> P\nsetproperties!(server::ParameterServer{P}, full::P) -> P\n\nEffectful multi-field set as one implicit transaction. A NamedTuple is a partial overlay — only the named fields change. A full P value is replace-all — every field is staged, and validation and the event batch consider only the fields whose value actually changes, so restating a read-only field at its current value commits as a no-op rather than a rejection. Commits atomically through the same path as transaction and returns the committed value; a rejection raises ParameterRejection.\n\n\n\n\n\n","title":"ROSNode.setproperties!"},{"category":"function","location":"communication/parameters/#ROSNode.declared_names","page":"Parameters","text":"declared_names(server::ParameterServer{P}) -> NTuple{N,Symbol}\n\nThe declared parameter names of a ParameterServer, in schema order — fieldnames(P). parameter_names adds the dynamic tier for the full flat namespace.\n\n\n\n\n\n","title":"ROSNode.declared_names"},{"category":"function","location":"communication/parameters/#ROSNode.parameter_names","page":"Parameters","text":"parameter_names(server::ParameterServer) -> Vector{Symbol}\nparameter_names(server::CompositeParameterServer) -> Vector{Symbol}\nparameter_names(client::ParameterClient; kwargs...) -> Vector{Symbol}\n\nThe flat list of parameter names a node exposes — what list and /parameter_events reflect over.\n\nParameterServer — the union of declared field names (first, in schema order) and the live dynamic names (sorted, only when allow_undeclared).\nCompositeParameterServer — every member's declared names as <member>.<field> in declared order.\nParameterClient — forwards to a remote ListParameters (list_parameters).\n\n\n\n\n\n","title":"ROSNode.parameter_names"},{"category":"function","location":"communication/parameters/#ROSNode.parameter","page":"Parameters","text":"parameter(server::ParameterServer, name::Symbol)\nparameter(client::ParameterClient, name; timeout_ms=2000)\n\nRead one parameter by name across both tiers.\n\nOn a ParameterServer:\n\ndeclared field — reads from the live atomic struct.\ndynamic field — reads from the side dict, when allow_undeclared.\nundeclared name with the gate off — raises ArgumentError.\n\nOn a ParameterClient it is one remote GetParameters for the single name:\n\nnormal — returns the value.\nunset or unknown name — reads back as nothing.\ntimeout or error reply — raises ServiceError.\n\nThis is the flat-namespace read the services and the composite resolver use.\n\n\n\n\n\n","title":"ROSNode.parameter"},{"category":"function","location":"communication/parameters/#ROSNode.dynamic_parameters","page":"Parameters","text":"dynamic_parameters(server::ParameterServer) -> Dict{Symbol,Any}\ndynamic_parameters(node::Node) -> Dict{Symbol,Any}\n\nThe undeclared (dynamic) parameter side dict of a node's parameter server — the Any-typed tier for parameters outside the @parameters schema. The dict is returned by reference, so a direct write (dynamic_parameters(node)[:gain] = 2.0) adds or changes an undeclared parameter immediately, but bypasses the mutation lock and publishes no /parameter_events — listeners and remote ParameterClients never observe it. Route through set_parameter!/transaction when an event is required. Requires allow_undeclared=true; with the gate off it raises ArgumentError. A CompositeParameterServer has no node-level dynamic tier and raises ArgumentError directing you to a member's own server.\n\n\n\n\n\n","title":"ROSNode.dynamic_parameters"},{"category":"function","location":"communication/parameters/#ROSNode.set_parameter!","page":"Parameters","text":"set_parameter!(server::ParameterServer, name::Symbol, value) -> P\nset_parameter!(client::ParameterClient, name, value; timeout_ms=2000) -> value\n\nSet one parameter by name, atomically.\n\nOn a ParameterServer it runs as a single-statement transaction and returns the whole committed schema struct P:\n\ndeclared field — routes to the typed struct, value coerced to the field type.\nundeclared name — routes to the dynamic dict, when allow_undeclared; the returned struct does not carry that key, since only the declared tier is part of P.\n\nOn a ParameterClient it issues one set_parameters_atomically:\n\nsuccess — returns the passed value.\nremote rejects it (constraint, read-only, or validate) — raises ParameterRejection.\n\nThis is the same exception the local form raises, so the failure contract is uniform local↔remote.\n\n\n\n\n\n","title":"ROSNode.set_parameter!"},{"category":"function","location":"communication/parameters/#ROSNode.get_parameters","page":"Parameters","text":"get_parameters(server::ParameterServer, names) -> Vector\nget_parameters(server::CompositeParameterServer, names) -> Vector\nget_parameters(client::ParameterClient, names; timeout_ms=2000) -> Vector{Any}\n\nThe ROS 2 GetParameters service: the current value of each requested name, flat over both tiers.\n\nunset or unknown name → reads back as nothing, which the service maps to a PARAMETER_NOT_SET ParameterValue.\nParameterServer form → reads locally: declared fields from the live atomic struct, dynamic ones from the side dict.\nParameterClient form → the remote dual: one wire call decoded back to native Julia values, raising ServiceError on a timeout or error reply.\n\n\n\n\n\n","title":"ROSNode.get_parameters"},{"category":"function","location":"communication/parameters/#ROSNode.get_parameter_types","page":"Parameters","text":"get_parameter_types(server::ParameterServer, names) -> Vector{ParameterType}\nget_parameter_types(server::CompositeParameterServer, names) -> Vector{ParameterType}\nget_parameter_types(client::ParameterClient, names; timeout_ms=2000) -> Vector{ParameterType}\n\nThe ROS 2 GetParameterTypes service: the ParameterType tag of each requested name, flat over both tiers.\n\nunknown name → reads back as PARAMETER_NOT_SET.\nParameterServer form → reflects locally: the declared field type, or the runtime type of a dynamic value.\nParameterClient form → the remote dual over the wire, raising ServiceError on a timeout or error reply.\n\n\n\n\n\n","title":"ROSNode.get_parameter_types"},{"category":"function","location":"communication/parameters/#ROSNode.set_parameters","page":"Parameters","text":"set_parameters(server::ParameterServer, pairs) -> Vector{Tuple{Bool,String}}\nset_parameters(server::CompositeParameterServer, pairs) -> Vector{Tuple{Bool,String}}\nset_parameters(client::ParameterClient, params; timeout_ms=2000) -> Vector{Tuple{Bool,String}}\n\nThe ROS 2 SetParameters service: apply each name => value pair as its own independent transaction, yielding one (successful, reason) SetParametersResult per pair. Per-pair failure modes:\n\na pair that violates a constraint, hits the read-only gate, or fails validate → fails on its own while the others may succeed.\na value that fails to coerce to its declared field type → throws instead of producing a per-pair result; a wire caller sees ServiceError, with earlier pairs already committed.\n\nTo apply all pairs all-or-nothing, use set_parameters_atomically. The ParameterServer form applies locally; the ParameterClient form is the remote dual over the wire.\n\n\n\n\n\n","title":"ROSNode.set_parameters"},{"category":"function","location":"communication/parameters/#ROSNode.set_parameters_atomically","page":"Parameters","text":"set_parameters_atomically(server::ParameterServer, pairs) -> (successful::Bool, reason::String)\nset_parameters_atomically(server::CompositeParameterServer, pairs) -> (successful::Bool, reason::String)\nset_parameters_atomically(client::ParameterClient, params; timeout_ms=2000) -> (successful::Bool, reason::String)\n\nThe ROS 2 SetParametersAtomically service: apply all pairs as one transaction. Outcomes:\n\nall pairs commit → (true, \"\").\na constraint, read-only, or validate rejection (or an ArgumentError, e.g. an undeclared name) → the first rejection aborts the whole set, result (false, reason).\na value that fails to coerce to its declared field type → throws, which a wire caller sees as ServiceError rather than a SetParametersResult.\n\nForms:\n\nParameterServer form → commits through one transaction.\nCompositeParameterServer form → groups pairs by member, validates every member's candidate first, commits only if all pass, and surfaces the set as one combined /parameter_events.\nMember servers lock independently, so a concurrent mutation racing the two phases can still leave earlier members committed while the call reports (false, reason).\nParameterClient form → the remote dual; values are native Julia (the wire tag is inferred) and params may be a vector of name => value, a NamedTuple, or a Dict. Transport failure raises ServiceError.\n\n\n\n\n\n","title":"ROSNode.set_parameters_atomically"},{"category":"function","location":"communication/parameters/#ROSNode.list_parameters","page":"Parameters","text":"list_parameters(server::ParameterServer; prefixes=(), depth=0) -> Vector{Symbol}\nlist_parameters(server::CompositeParameterServer; prefixes=(), depth=0) -> Vector{Symbol}\nlist_parameters(client::ParameterClient; prefixes=String[], depth=0, timeout_ms=2000) -> Vector{Symbol}\n\nThe ROS 2 ListParameters service: the flat union of parameter names, optionally kept to those starting with one of prefixes.\n\nParameterServer form → declared names then dynamic names.\nCompositeParameterServer form → every member's <member>.<field>.\nParameterClient form → the remote dual, raising ServiceError on a timeout or error reply.\n\nFiltering semantics:\n\nprefix match → plain startswith (\"nav\" also matches navigator.max_speed), looser than rclcpp's separator-bounded match.\ndepth → how far past each matched prefix (past the root when prefixes is empty) the listing reaches; depth=0 is DEPTH_RECURSIVE, the full list.\nlevel counting → as rclcpp counts: . separators in the post-prefix tail, the joining dot excluded, so an exact prefix match always survives and depth=1 reaches a prefix's direct children.\n\n\n\n\n\n","title":"ROSNode.list_parameters"},{"category":"function","location":"communication/parameters/#ROSNode.describe_parameters","page":"Parameters","text":"describe_parameters(server::ParameterServer, names) -> Vector{ParameterDescriptor}\ndescribe_parameters(server::CompositeParameterServer, names) -> Vector{ParameterDescriptor}\ndescribe_parameters(client::ParameterClient, names; timeout_ms=2000) -> Vector{ParameterDescriptor}\n\nThe ROS 2 DescribeParameters service: a ParameterDescriptor per requested name. Each name resolves to one of:\n\ndeclared name → its schema descriptor.\nlive dynamic name → a synthesized descriptor: its runtime type, no constraint, not read-only.\nunknown name → a PARAMETER_NOT_SET descriptor.\n\nForms:\n\nParameterServer and CompositeParameterServer — reflect locally.\nParameterClient — the remote dual over the wire.\n\nWire fidelity:\n\nname, type, description, and read-only travel faithfully.\nthe numeric-range or choice-set constraint travels only as the human additional_constraints string; no default travels.\na client-decoded descriptor reads back with constraint === nothing and default === nothing.\n\n\n\n\n\n","title":"ROSNode.describe_parameters"},{"category":"function","location":"communication/parameters/#ROSNode.on_parameter_event","page":"Parameters","text":"on_parameter_event(f, server::ParameterServer) -> f\non_parameter_event(f, client::ParameterClient) -> f\n\nRegister a callback f(batch::ParameterEventBatch) for parameter changes — the in-process and remote sides of ROS 2's /parameter_events. Returns f.\n\nOn a ParameterServer the listener runs synchronously after each committed transaction, under the server's mutation lock; a throwing listener is logged and the commit proceeds. The batch carries:\n\nchanged — each moved parameter mapped to its new value;\nprevious — the prior value.\n\nOn a ParameterClient it subscribes the remote's /parameter_events and calls f per event from this client's target node, surfacing the event's new and changed parameters. Client-side caveats:\n\na typed client coerces the changed values to their field types, and batch.previous is empty (the wire event carries only current values);\na remote's deleted_parameters (a parameter undeclaration) is not reported;\nthe node filter is an exact string compare of the event's node field against the resolved target; a remote whose published FQN differs has its events silently dropped. The FQN can differ by:\na namespace mismatch;\na normalization mismatch;\nthe subscription is reaped by close(client).\n\n\n\n\n\n","title":"ROSNode.on_parameter_event"},{"category":"type","location":"communication/parameters/#ROSNode.ParameterEventBatch","page":"Parameters","text":"ParameterEventBatch\n\nPost-commit change set for one transaction, delivered to in-process listeners. changed maps each declared or dynamic parameter whose value moved to its new value; previous holds the prior value. The service layer assembles the wire rcl_interfaces/msg/ParameterEvent from this.\n\nSee the ROS 2 parameters concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Parameters.html\n\n\n\n\n\n","title":"ROSNode.ParameterEventBatch"},{"category":"function","location":"communication/parameters/#ROSNode.readonly","page":"Parameters","text":"readonly(default)\n\nMark a @parameters field read-only inside the schema DSL: field::T = default |> readonly. Read-only governs two paths:\n\nRuntime sets: raises ParameterRejection internally and returns a rejected SetParametersResult on the wire.\nStartup overrides (CLI/launch/YAML): permitted — routed through the constructor, not a transaction.\n\nThe marker is detected and stripped at macro expansion, so the stored default is the bare value; a stray readonly(x) evaluated outside the DSL just wraps x in an inert holder.\n\n\n\n\n\n","title":"ROSNode.readonly"},{"category":"type","location":"communication/parameters/#ROSNode.ParameterRejection","page":"Parameters","text":"ParameterRejection(reason::AbstractString)\n\nException signaling a rejected parameter set, raised by a per-field constraint violation, a read_only runtime set, or a user validate. Carries the human reason string that becomes the wire SetParametersResult.reason. The failure contract is uniform local↔remote — each caller context surfaces the same rejection:\n\na local caller of transaction/set_parameter! sees it raised;\nthe service layer catches it into a successful=false reply;\na ParameterClient re-raises it on a rejected set.\n\n\n\n\n\n","title":"ROSNode.ParameterRejection"},{"category":"section","location":"#ROSNode.jl","page":"Home","text":"ROSNode is a pure-Julia ROS 2 client library built on Zenoh. It mirrors rmw_zenoh on the wire, so its nodes interoperate with C++ and Python ROS 2 nodes sharing the same router and ROS_DOMAIN_ID.\n\nThe peers endpoint points at a running Zenoh router (zenohd); start one first. from=\"interfaces\" adds a local directory of interface files as a search root, so std_msgs/msg/String resolves from interfaces/std_msgs/msg/String.msg without a sourced ROS environment. This snippet shows the API shape; for a runnable talker see examples/publisher.jl.\n\nusing ROSNode\n\n@ros_import \"std_msgs/msg/String\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"talker\")\n    pub  = Publisher(node, \"/chatter\", std_msgs.msg.String)\n    publish(pub, std_msgs.msg.String(data = \"hello world\"))\nend\n\nROSNode also resolves message types at runtime. A type-less subscription reads each sample's (name, hash), resolves the concrete wire type, decodes it on the fly, and hands the handler a real typed struct — no @ros_import needed. It resolves the type from each source in order:\n\nthe type registry\nthe project cache\nament\na wire query\n\nusing ROSNode\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"listener\")\n\n    Subscription(node, \"/chatter\") do msg\n        @info \"heard\" msg.data\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThis powers generic tooling — recorders, bridges, introspection — over any type discovered at runtime.","title":"ROSNode.jl"},{"category":"section","location":"#What-it-provides","page":"Home","text":"Nodes group communication entities under a name.\nTopics carry messages from publishers to subscribers.\nServices answer a request with a response.\nActions run long-running goals that stream feedback and return a result.\nParameters expose named, typed values that peers read and set.","title":"What it provides"},{"category":"section","location":"#The-guide","page":"Home","text":"Start with Getting Started: start a router, open a context, run an example. From there the guide builds from concepts to practice.\n\nFoundations\n\nThe Runtime Model — how a context, node, and spin keep a process alive while the scheduler delivers messages.\nInterface Types — the typed messages every topic, service, and action carries, and the RIHS01 identity that makes them wire-compatible.\n\nCommunication\n\nTopics — publish and subscribe on a named channel.\nServices — answer a request with a response.\nActions — run a long-running goal that streams feedback and returns a result.\nParameters — expose named, typed values that peers read and set.\n\nGoing Further\n\nAuthoring Interfaces in Julia — define message, service, and action types in Julia source with @ros_message, @ros_service, and @ros_action.\nRuntime Type Discovery — resolve message types off the wire and persist them with @ros_cache.\nMessage Delivery — choose a view mode and concurrency for how samples reach a handler.\n\nComposition\n\nComponents — author a node as a collection of components, and compose nodes into a process.\n\nInterop\n\nInteroperating with ROS 2 — exchange topics, services, actions, and parameters with C++ and Python ROS 2 nodes.","title":"The guide"},{"category":"section","location":"#The-module","page":"Home","text":"","title":"The module"},{"category":"module","location":"#ROSNode.ROSNode","page":"Home","text":"ROSNode\n\nA Julian ROS 2 client built on Zenoh, implementing the node-centric concepts of the ROS 2 graph: https://docs.ros.org/en/rolling/Concepts/Basic.html\n\n\n\n\n\n","title":"ROSNode.ROSNode"},{"category":"section","location":"communication/actions/#Actions","page":"Actions","text":"An action runs a long goal that streams feedback while it works and ends in a result. The client tracks progress as feedback arrives and can cancel the goal mid-flight.\n\nThis page serves and calls the Fibonacci action using an imported interface. Authoring an action in Julia and resolving its type at runtime build on the same flow.","title":"Actions"},{"category":"section","location":"communication/actions/#Imported-interface","page":"Actions","text":"@ros_import of a fully-qualified action binds the bare Fibonacci namespace: a tag type that serves as the action handle and exposes the sections by short name (Fibonacci.Goal, Fibonacci.Feedback, Fibonacci.Result).\n\nThe server's handler runs once per accepted goal. goal.request is the decoded Goal, feedback! publishes a Feedback message and checkpoints cancellation, and returning a Result settles the goal :succeeded.\n\nusing ROSNode\n\n@ros_import \"action_tutorials_interfaces/action/Fibonacci\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"fibonacci\")\n\n    server = ActionServer(node, \"/fibonacci\", Fibonacci) do goal\n        seq = Int32[0, 1]\n        for i in 1:goal.request.order\n            push!(seq, seq[end] + seq[end-1])\n            feedback!(goal, Fibonacci.Feedback(partial_sequence = copy(seq)))\n            sleep(0.3)\n        end\n        Fibonacci.Result(sequence = seq)\n    end\n\n    spin(ctx; handle_signals = true)\nend\n\nThe feedback field is partial_sequence, matching action_tutorials_interfaces. The action is wire-compatible with ROS 2 (see Interoperating with ROS 2).\n\nThe client sends a goal, streams feedback until the goal settles, then reads the cached result. send blocks on the server's reply and returns a ClientGoal handle. The handle reports :accepted once the server takes the goal. It reports :rejected for any other outcome:\n\nthe server declined the goal,\nthe retry window expired before a server matched, or\na Context drain closed the window.\n\nusing ROSNode\n\n@ros_import \"action_tutorials_interfaces/action/Fibonacci\" from=\"interfaces\"\n\n@context(peers = [\"tcp/localhost:7447\"]) do ctx\n    node = Node(ctx, \"fibonacci\")\n    client = ActionClient(node, \"/fibonacci\", Fibonacci)\n\n    if wait_for_action_server(client; timeout = 5)\n        gh = send(client, Fibonacci.Goal(order = Int32(8)))\n        @info \"goal\" state = state(gh)   # :accepted (or :rejected)\n\n        for fb in feedback(gh)           # loop ends when the goal settles\n            @info \"feedback\" fb.partial_sequence\n        end\n\n        result = fetch(gh)\n        @info \"result\" sequence = result.sequence\n        @info \"final state\" state = state(gh)   # :succeeded\n    end\n    close(client)\nend\n\nwait_for_action_server returns true once a server route-matches within the timeout, gating send on a reachable server. The feedback loop drives the result protocol, so iterating it is the whole client lifecycle.","title":"Imported interface"},{"category":"section","location":"communication/actions/#Goal-states","page":"Actions","text":"state(gh) reports where a goal sits in its lifecycle. Step a goal through it — click a transition to advance, any state to read it:\n\n<div class=\"rosnode-statechart\" data-machine=\"goal\"></div>\n\nHow the handler ends the goal is the settlement three-way again, specialized:\n\nHandler ends by Goal settles\nreturning a Result :succeeded\na checkpoint raising Cancelled :canceled\nany other throw :aborted\n\n:rejected is a client-side handle state, not a settlement: the server declined the goal with reject, so it stays a client-only state with no server-side goal record.","title":"Goal states"},{"category":"section","location":"communication/actions/#Cancelling-a-goal","page":"Actions","text":"Call cancel(gh) on the client to cancel a running goal. The server's next feedback! checkpoint throws Cancelled, the handler unwinds, and the goal settles :canceled. Every feedback! call doubles as a cancellation checkpoint, so a handler that publishes feedback as it works yields cancellation points for free.\n\ngh = send(client, Fibonacci.Goal(order = Int32(8)))\ncancel(gh)                       # request cancellation\nstate(gh)                        # :canceling once the server replies\nfetch(gh)                        # drives the goal to its terminal state\nstate(gh)                        # :canceled","title":"Cancelling a goal"},{"category":"section","location":"communication/actions/#Next","page":"Actions","text":"Parameters configure a node's behavior over the same wire. See also Services for request/response calls and Interface Types for how imported and authored action types share one wire identity.","title":"Next"},{"category":"section","location":"communication/actions/#API-reference","page":"Actions","text":"","title":"API reference"},{"category":"type","location":"communication/actions/#ROSNode.ActionServer","page":"Actions","text":"ActionServer(node, name, A; on_goal=accept-all, on_cancel=accept-all, on_accepted, qos=default_qos())\nActionServer(node, name, A; concurrency=Serial(), on_goal=…, on_cancel=…) do goal … end\n\nA ROS 2 action server for action type A, declared on node at the resolved name name. Owns the three action-protocol services (send_goal, cancel_goal, get_result), the feedback and status topics, the goal table (live goals plus the settled-result cache), and the user callbacks. The three sub-messages of A — Goal (G), Result (R), Feedback (F) — drive the handler signatures.\n\nTwo construction forms share one constructor:\n\nHigh-level (do-block): the body is the goal's execution. The framework spawns it wrapped in the cancellation + fail-safe settlement machinery, scheduled per Concurrency. Serial (the default) holds goal bodies on the server's serial_lock so they run one at a time in acceptance order; Parallel(n) runs each body on its own OS thread, at most n in flight.\nLow-level: supply on_accepted=callback; you own when, whether, and where each accepted goal runs (the GoalHandle is handed to you). Pair this with SingleFlight or your own orchestrator.\n\nPass either a do-block body or on_accepted=, never both; a server with neither raises ArgumentError. The two decision callbacks:\n\ncallback returns default\non_goal(request) accept / reject / defer accept-all\non_cancel(goal) accept / reject accept\n\nThe three services key off rmwzenoh SERVICE-level type names and RIHS01 hashes — `<pkg>/action/<A>SendGoal,…GetResult, andactionmsgs/srv/CancelGoalfill the type+hash segments of each data keyexpr — matching a real ROS 2 peer;feedbackandstatuscarry their own wire types (<A>FeedbackMessage,actionmsgs/GoalStatusArray). Construction requiresAto be generated with@rosmsgsso the protocol-wrapper structs exist; a bare@rosmsgraisesArgumentError`.\n\nclose-able and idempotent. Closing:\n\nawaits outstanding goal tasks (including detached post-cancel cleanup) up to the Context drain timeout before withdrawing each sub-endpoint's liveliness token and Zenoh route;\nleaves a goal task that overruns the timeout running detached (close warns and proceeds).\n\nThe server also dies with its node.\n\nserver = ActionServer(node, \"fibonacci\", Fibonacci) do goal\n    seq = Int32[0, 1]\n    for _ in 1:goal.request.order\n        feedback!(goal, Fibonacci_Feedback(; partial_sequence = seq))\n        push!(seq, seq[end] + seq[end-1])\n    end\n    Fibonacci_Result(; sequence = seq)\nend\n\nFollows the ROS 2 actions concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Actions.html\n\n\n\n\n\n","title":"ROSNode.ActionServer"},{"category":"type","location":"communication/actions/#ROSNode.ActionClient","page":"Actions","text":"ActionClient(node, name, A; qos=default_qos()) -> ActionClient\n\nA ROS 2 action client for action type A, declared on node at the resolved name name. Dispatch a goal with send, then iterate feedback, block on fetch for the result, and cancel to request cancellation. The three sub-messages of A — Goal/Result/Feedback — type the call and reply surface.\n\nEach protocol-service call is a fresh one-shot Zenoh get carrying the rmwzenoh request attachment (`sequencenumber,sourcetimestamp,sourcegid) a real ROS 2 peer echoes into its reply. Feedback is a per-goal subscription on thefeedbacktopic, filtered to the goal's id. Routing-match probes forsendgoalandgetresultare declared lazily on the first [actionservermatched](@ref)/[waitforaction_server`](@ref) call.\n\nsend, fetch, and feedback resolve the protocol-wrapper structs @ros_msgs generates; an action generated with bare @ros_msg raises ArgumentError on the first such call.\n\nclose-able and idempotent (reaping any lazy match probes); dies with its node.\n\nFollows the ROS 2 actions concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Actions.html\n\n\n\n\n\n","title":"ROSNode.ActionClient"},{"category":"type","location":"communication/actions/#ROSNode.GoalHandle","page":"Actions","text":"GoalHandle{A, G, R, F}\n\nA single accepted action goal, server-side. Carries:\n\nthe 16-byte unique_identifier_msgs/UUID goal id\nthe decoded goal request, a Goal struct reachable as goal.request\nthe live goal state (state)\nthe write-once result cell (respond!) the handler settles\n\nThis is the user-facing verb surface for one goal; the server owns the result cache and status publication.\n\nCancellation is structured. Once an accepted cancel moves the goal to CANCELING, feedback! and checkpoint throw Cancelled at the next yield point, so a goal in CANCELING unwinds to a CANCELED result on the default path with no manual polling. This domain's handler-exit status mapping:\n\nreturn a Result ⇒ SUCCEEDED\nthrow Cancelled ⇒ CANCELED\nthrow anything else (or fall through with no settle) ⇒ ABORTED\n\nSettle explicitly with succeed/abort (or respond!(goal, canceled, result)); the exactly-once handler-exit contract is owned by respond!.\n\nConstructed by the framework on goal acceptance, never directly. A handle lives from acceptance until the result-cache TTL evicts it after settlement.\n\nFollows the ROS 2 action goal lifecycle: https://docs.ros.org/en/rolling/Concepts/Basic/About-Actions.html\n\n\n\n\n\n","title":"ROSNode.GoalHandle"},{"category":"type","location":"communication/actions/#ROSNode.ClientGoal","page":"Actions","text":"ClientGoal{A, G, R, F}\n\nA client-side handle for a dispatched goal: iterable over feedback, fetch-able for the result, cancel-able, and state-queryable. The result is held in a write-once slot the framework fills from the server's get_result reply. Returned by send once the server has accepted or rejected.\n\n\n\n\n\n","title":"ROSNode.ClientGoal"},{"category":"function","location":"communication/actions/#ROSNode.send","page":"Actions","text":"send(client, goal) -> ClientGoal\n\nDispatch goal (a Goal struct for the action) on client and return a client-side goal handle once the server replies accepted or rejected. Blocks the calling task on the send_goal service reply.\n\nThe client mints the 16-byte goal id (the correlation token reused for feedback, get_result, and cancel). Because a queryable can lag the liveliness token a peer discovers it by, send retries the one-shot get over roughly a two-second window (50 ms backoffs) while the route stays unmatched; the first reply that lands is authoritative. Inspect the outcome with state (:accepted or :rejected), iterate feedback, and block on fetch for the result.\n\n:rejected covers every no-acceptance outcome alike:\n\nan on_goal rejection,\nan error reply (server inactive),\nno server discovered within the window, and\na Context drain mid-window.\n\nA caller that must tell them apart checks action_server_matched or is_shutdown separately.\n\nThrows:\n\nArgumentError if client is already closed.\nNodeInactiveError if the client's node is a managed node that is not Active (probe isactive(node) first).\n\ngoal = send(client, Fibonacci_Goal(; order = 10))\nstate(goal) === :accepted || error(\"rejected\")\nresult = fetch(goal)\n\n\n\n\n\n","title":"ROSNode.send"},{"category":"function","location":"communication/actions/#ROSNode.state","page":"Actions","text":"state(goal) -> Symbol\n\nThe goal's current lifecycle state as a Symbol. The same accessor reads a server-side GoalHandle and a client-side dispatched-goal handle, with the symbol set differing by side:\n\nSide States\nServer (and shared) :accepted, :executing, :canceling, :succeeded, :canceled, :aborted, :unknown (action_msgs/msg/GoalStatus)\nClient only adds :rejected — the symbol the handle send returns when the server declines\n\n:rejected is a client-local symbol the server-side handle never reports: a rejected goal is never registered.\n\nThe Symbol surface keeps the wire GoalStatus enum internal and reads naturally at a call site (state(goal) === :executing).\n\nThis is the action-goal state, distinct from the same-named accessor for managed-node lifecycle: a managed node's lifecycle state is state on a LifecycleNode.\n\n\n\n\n\nstate(ln::LifecycleNode) -> LifecycleState\n\nThe managed node's current primary lifecycle state — one of Unconfigured, Inactive, Active, or Finalized, and the value an external ~/get_state query reports.\n\nRead lock-free via an atomic load, so any dispatch task can consult it cheaply; transitions latch it under the node lock. Compare or dispatch on the singleton to branch: state(ln) === Active().\n\nDistinct from the same-named action method: an action goal's progress is state on a GoalHandle (a Symbol), not a lifecycle state.\n\n\n\n\n\n","title":"ROSNode.state"},{"category":"function","location":"communication/actions/#Base.fetch","page":"Actions","text":"fetch(goal) -> result\n\nBlock until the goal settles and return its Result. Backed by a write-once slot filled from one get_result request — single-fill, like the server-side respond!: the first fetch/feedback starts it, every fetch returns that result. Raises:\n\nan error if the goal was rejected.\nan error if the server reported a failure.\na timed-out-result error if the result never arrived (a Context drain ends the get_result retry early, surfacing here).\nNodeInactiveError if the client's node is a managed node that is not Active when the get_result query starts (probe isactive(node) first).\n\n\n\n\n\nfetch(client::ParameterClient{P}; timeout_ms=2000) -> P\n\nMaterialize the whole remote into one value:\n\ntyped client — a type-stable P (one round-trip, then read fields locally).\nschemaless client — a NamedTuple of every remote parameter.\nan unset field falls back to the schema default.\n\n\n\n\n\n","title":"Base.fetch"},{"category":"function","location":"communication/actions/#ROSNode.feedback!","page":"Actions","text":"feedback!(goal, fb)\n\nPublish one feedback message fb (a Feedback struct for the action) on the action's feedback topic, after checkpointing cancellation. A CANCELING goal throws Cancelled before anything is published, so a progress-report loop doubles as a cancellation-observation loop with no extra code. Equivalent to respond!(goal, feedback, fb).\n\nA feedback publish after the goal has settled is dropped (the client already holds the result and the feedback stream is closed). The message rides the feedback topic as a <A>_FeedbackMessage (goal_id + feedback) so a client filters it to the matching goal.\n\nReturns nothing.\n\n\n\n\n\n","title":"ROSNode.feedback!"},{"category":"function","location":"communication/actions/#ROSNode.checkpoint","page":"Actions","text":"checkpoint(goal)\n\nA cancellation yield point: throw Cancelled when the goal is CANCELING, otherwise return nothing. Place it inside long-running loops so an accepted cancel request unwinds the handler structurally — the fail-safe settlement then maps the Cancelled to a CANCELED result.\n\nOnce the goal has already settled (for example, detached post-cancel cleanup has run respond!), checkpoint is a no-op so that cleanup runs to completion. The non-throwing form is iscancelled, a Bool read for handlers that branch on cancellation.\n\nReturns nothing.\n\n\n\n\n\n","title":"ROSNode.checkpoint"},{"category":"function","location":"communication/actions/#ROSNode.iscancelled","page":"Actions","text":"iscancelled(goal) -> Bool\n\nTrue once an accepted cancel request has moved the goal to CANCELING. A cheap, lock-free atomic read — the non-throwing counterpart of checkpoint, for a handler that branches on cancellation.\n\nReads the live status only: a goal that has already settled as CANCELED reads false here (it is no longer in CANCELING).\n\n\n\n\n\n","title":"ROSNode.iscancelled"},{"category":"function","location":"communication/actions/#ROSNode.succeed","page":"Actions","text":"succeed(goal, result)\n\nSettle the goal as SUCCEEDED with result (a Result struct for the action) — shorthand for respond!(goal, succeeded, result). Use it when the handler computes its result before its final expression; a plain return result from a do-block body reaches the same outcome.\n\nSettlement is single-fill via respond!; returns true if this call filled the cell.\n\n\n\n\n\n","title":"ROSNode.succeed"},{"category":"function","location":"communication/actions/#ROSNode.abort","page":"Actions","text":"abort(goal, result)\n\nSettle the goal as ABORTED with result (a Result struct for the action) — shorthand for respond!(goal, aborted, result). The explicit failure verb; a thrown non-Cancelled exception from the handler reaches the same ABORTED outcome through fail-safe settlement.\n\nSettlement is single-fill via respond!; returns true if this call filled the cell.\n\n\n\n\n\n","title":"ROSNode.abort"},{"category":"function","location":"communication/actions/#ROSNode.execute","page":"Actions","text":"execute(goal) do goal … end\nexecute(goal, body)\n\nRun body(goal) as this goal's execution, wrapped in the cancellation + settlement machinery — the low-level and defer counterpart of the high-level do-block server. Spawns the body on its own OS-thread task (tracked so close(server) joins it on drain); the task transitions the goal to EXECUTING and publishes the status change before running the body. Returns nothing immediately; the body runs concurrently.\n\nUse it from an on_accepted callback that chose to defer, or from an orchestrator such as SingleFlight. Each call spawns a fresh task, so two execute calls on different goals run simultaneously — the caller owns any one-at-a-time policy.\n\nThe body's exit maps to a goal terminal status:\n\nnormal return settles succeeded;\na thrown Cancelled settles canceled;\nany other throw (or a body that exits without responding) settles aborted and logs.\n\nrespond! owns the exactly-once settlement that guarantees this mapping fires exactly once.\n\n\n\n\n\n","title":"ROSNode.execute"},{"category":"function","location":"communication/actions/#ROSNode.cancel","page":"Actions","text":"cancel(goal)\n\nRequest cancellation of a dispatched goal: issue a cancel_goal request to the server, which runs its on_cancel and, on accept, moves the goal to CANCELING so the handler's checkpoint/feedback! throw Cancelled. Returns nothing once the server replies (a one-shot get with a 5-second timeout); the goal's terminal state is observed through fetch.\n\nThe request targets this goal's id (a zeroed id would be the server-side cancel-all sentinel). The handle's local state moves to :canceling once the reply lands, unless the goal has already settled — a terminal state(goal) is kept; read the true terminal outcome through fetch, which is unaffected.\n\nThrows NodeInactiveError if the client's node is a managed node that is not Active (probe isactive(node) first).\n\n\n\n\n\n","title":"ROSNode.cancel"},{"category":"function","location":"communication/actions/#ROSNode.accept","page":"Actions","text":"accept() -> Accept\n\nThe on_goal decision that accepts a goal and fires the server's on_accepted. Return it from an on_goal(request) callback. Execution path depends on the API level:\n\nHigh-level: the do-block body runs immediately.\nLow-level: the on_accepted callback decides when to execute.\n\nSee also:\n\nOne of the three goal-acceptance tokens, with reject and defer.\nThe default on_goal returns accept().\n\n\n\n\n\n","title":"ROSNode.accept"},{"category":"function","location":"communication/actions/#ROSNode.reject","page":"Actions","text":"reject() -> Reject\n\nThe on_goal decision that declines a goal. Return it from an on_goal(request) callback. Effects:\n\nServer: replies not-accepted.\nClient: send returns a handle in state :rejected.\n\nSee also: one of the three goal-acceptance tokens, with accept and defer.\n\n\n\n\n\n","title":"ROSNode.reject"},{"category":"function","location":"communication/actions/#ROSNode.defer","page":"Actions","text":"defer() -> Defer\n\nThe on_goal decision that accepts a goal but leaves it in the ACCEPTED state for the owner to run later with execute — the queue/orchestrator path. Return it from an on_goal(request) callback. Effects:\n\nServer: replies accepted.\non_accepted: not fired (execute the goal explicitly with execute).\n\nSee also: one of the three goal-acceptance tokens, with accept and reject.\n\n\n\n\n\n","title":"ROSNode.defer"},{"category":"function","location":"communication/actions/#ROSNode.action_server_matched","page":"Actions","text":"action_server_matched(client::ActionClient) -> Bool\n\nTrue when the client is routing-matched to an action server right now: both its send_goal and get_result Queriers have matched a server's queryables. When true, send and fetch reach the server without discovery backoff; until then they retry on discovery. false for a closed client.\n\nThe routing-plane signal behind wait_for_action_server. The first call lazily declares the two match probes (Queriers) on the client; a client that never awaits its server declares none.\n\n\n\n\n\n","title":"ROSNode.action_server_matched"},{"category":"type","location":"communication/actions/#ROSNode.SingleFlight","page":"Actions","text":"SingleFlight(; queue=4) -> SingleFlight\n\nAn action orchestrator that runs one accepted goal at a time behind a bounded queue of queue pending goals — the common single-actuator pattern. It layers over the low-level ActionServer API.\n\nWire it by setting on_accepted = g -> submit!(sched, g), then drive execution with execute!(sched) do goal … end. The on_accepted hook:\n\nfires for goals the default on_goal accepts.\nstays quiet for a deferred goal.\n\nThe orchestrator methods:\n\nsubmit! enqueues an accepted goal (blocking when the queue is full, applying backpressure).\nexecute! spawns a loop task that pulls one goal at a time, runs it through execute (the same cancellation + settlement wrapper), and waits for it to finish before the next.\npause/resume gate at the dispatch boundary — while paused the loop parks before dispatching the next goal.\nactive_goal reports the running goal or nothing.\n\nThis is a per-goal admission gate over one orchestrator, distinct from the node-lifecycle gate isactive; the two are independent. Only SingleFlight itself is exported; reach the orchestrator surface qualified:\n\nROSNode.submit!\nROSNode.execute!\nROSNode.pause\nROSNode.resume\nROSNode.active_goal\n\nsched = SingleFlight(queue = 8)\nserver = ActionServer(node, \"dock\", Dock;\n                      on_accepted = g -> ROSNode.submit!(sched, g))   # default on_goal accepts\nROSNode.execute!(sched) do goal\n    # one goal at a time …\nend\n\n\n\n\n\n","title":"ROSNode.SingleFlight"},{"category":"type","location":"communication/actions/#ROSNode.ActionTypeSupport","page":"Actions","text":"ActionTypeSupport{A, G, R, F}\n\nType-level handle on an action type A and its three sub-message structs: Goal (G), Result (R), and Feedback (F). The sub-types drive the data-route key expressions and the handler signatures. Built from the action type argument to ActionServer/ActionClient. The protocol wrappers (SendGoal/GetResult request/response, FeedbackMessage) are generated siblings of A, resolved off A on demand via _action_wrapper.\n\nSee the ROS 2 actions concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Actions.html\n\n\n\n\n\n","title":"ROSNode.ActionTypeSupport"},{"category":"type","location":"communication/actions/#ROSNode.GoalState","page":"Actions","text":"GoalState\n\nThe ROS 2 goal lifecycle (action_msgs/msg/GoalStatus). Integer values match the wire enum, so the wire-scoped states publish directly to the status topic.\n\nTransitions:\n\nACCEPTED → EXECUTING → SUCCEEDED\n   │            ├→ CANCELING → CANCELED\n   │            └→ ABORTED\n   └──────────────→ CANCELING → CANCELED\n\nState Wire value Scope\nACCEPTED 1 wire\nEXECUTING 2 wire\nCANCELING 3 wire\nSUCCEEDED 4 wire\nCANCELED 5 wire\nABORTED 6 wire\n:unknown 0 local\n:rejected — local\n\nLocal states label a handle and are never sent on the wire.\n\nSee the ROS 2 actions concept: https://docs.ros.org/en/rolling/Concepts/Basic/About-Actions.html\n\n\n\n\n\n","title":"ROSNode.GoalState"},{"category":"type","location":"communication/actions/#ROSNode.NodeInactiveError","page":"Actions","text":"NodeInactiveError(msg)\n\nRaised by an outbound action-client call — send, fetch, cancel — issued from a client owned by a managed node that is not Active. A node administratively brought down should not address a remote peer, so the call is gated at entry (an already in-flight call completes under its own timeout). Probe liveness first with isactive(node); a standalone client on an unmanaged node is always active and never gates. Service call is not gated at entry, so a request issued from an inactive node's client still goes out.\n\n\n\n\n\n","title":"ROSNode.NodeInactiveError"}]
}
