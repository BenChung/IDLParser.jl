Manifest*.toml

# Vendored ROS2 interface files — repopulate with vendor_ros2.sh.
# Only NOTICE.md (provenance) and vendor_ros2.sh (the script) stay tracked.
/ROSMessages/test/files/ros2_standard/*/
/ROSMessages/test/files/ros2_ecosystem/*/

infra/hiroz-participant/target